# v10.28 —— 详情页版式重排 + 三处交互重做（吸顶 / 置顶 / 固定底栏 / 三专区预览分页）

> 日期：2026-09-20 ｜ 上一版：v10.27（下载弹窗重构）
> 关联文件：`public/index.html`（主源）· `public/emulator.html` · `public/unpack.html`（重建）·
> `tools/test-v1028-detail.js`（新增 119 条）· `tools/preview-v1028.js`（新增实拍 43 条）·
> `tools/_counterproof-v1028.js`（新增反证 11 条）· `tools/run-all.js`（登记新套件）·
> `tools/test-related-dl.js` / `test-v1015.js` / `test-v1016.js` / `test-v1017.js` /
> `test-download.js` / `test-match-release.js`（同步改断言）·
> `tools/preview-v1014.js` / `preview-v1015.js`（同步改选择器）

---

## 用户原话（四条）

> ①详情页中的手机配置只需要手机模拟器配置，不要多个显示，且点击后我只需要品牌，型号，硬件配置
> 默认展示5个，然后有个更多，点击弹窗可以查看全部
> ②图片点击展示后，在详情页后，我需要他置顶
> ③本体，修改器，Mod我需要放在最下面，且每个均只展示前三个，点击全部后弹窗显示全部，弹窗显示十个其他的翻页
> 详情页布局如下：
> 封面 + 标题（吸顶）（合并游戏预览）
> 玩家评分  参数表（厂商/日期/大小）
> 游戏介绍 / 版本介绍
> 配置要求
> 手机模拟器配置
> 修改器+云存档
> 同分类更多
> 其他没的
> 操作按钮组固定在详情页下面

## 用户在两处岔路上圈定的选择（AskUserQuestion）

| # | 问题 | 用户选择 |
|---|---|---|
| ① | 手机配置区里原有的「本站实测 N 条」「逐条游玩参数 N 份」怎么办 | **收进「更多」弹窗**（不删数据） |
| ② | 底部那三个下载按钮怎么处理 | **「下载按钮不是操作按钮组吗，我的意思是固定在详情页下面（移动也可查看）」** ⇒ 澄清了「操作按钮组」就是那三个下载按钮，要**固定在底部**，不是删掉 |

---

## 一、改动前 → 改动后（逐项实测）

### ① 详情页版式顺序

| | 顺序 |
|---|---|
| **改造前** | 手机配置 → 配置要求 → 修改器 → 云存档 → 跨源 → 评分 → 参数表 → 介绍 → **预览** → 同分类 → 下载 |
| **改造后** | 封面+标题(吸顶) → 预览 → 评分+参数表 → 介绍/版本 → 配置要求 → 手机配置 → 修改器+云存档 → 同分类 → **本体/Mod/修改器** → 其他；**操作按钮组移出正文流、固定底部** |

实拍（`#drawerBody > .d-body` 子元素顺序，真实 DOM）：

```
.d-hint → .gal → .score-line → .kv → .block → .block → #reqSlot → #bhSlot
→ #trSvSlot → #relSlot → #dlSlot → #crossSrc → .no-dl
```

> 用户打开详情先看到的原本是「别人总结的配置」，自己的评分 / 厂商 / 日期 / 大小要翻到第 5 屏 —— 这正是重排的原因。

### ② 封面 + 标题吸顶（`dHeroSpy`）

- `.d-hero` 从 `position:relative` 改成 `sticky; top:0; z-index:8` + `transition:height .24s`
- 新增 `.d-hero.mini{height:62px}` 及六个子规则（标题缩字号、英文名收起、封面缩成小图、来源徽标收进标题行）
- 开关用**一次 `classList.toggle('mini', dr.scrollTop > 90)`** —— 写成 `if/else` 两行会在快速滚动时抖动
- 接进既有的滚动回调：`rAF(() => { railSpy(); dHeroSpy(); })`；`paintDetail` 末尾再调一次**定初态**（换游戏后不该残留上一次的收窄态）

**实测**：初始高 **250px** → 滚动后 **62px**，`top=0.0`（仍贴顶）。

### ③ 图片灯箱置顶 + 修掉「✕ 点不动」

- `.gal-lb` 的 `z-index` 从 **90 → 140**。原值低于 `.drawer`（100），点开大图**被抽屉原样盖住** ——
  而「灯箱存在 / 有图 / 可点」三项断言当时全部为真（最危险的那类假绿）。
  改后层级：灯箱 140 > 搜索弹窗 130 > 下载弹窗 110 > 抽屉 100 > 遮罩 90，仍在 toast(150) 之下。
- ★ **顺手修掉一个真 bug**：右上角 ✕ 的点击处理**根本没写进分支** ——
  document 级 click 处理里只判了「点背景」与「点图翻页」，点 ✕ 落进最后一个 `return` 什么都不做，
  用户只能按 Esc。注释写的是「点空白 / ✕ 关闭」，实现漏了一半。

### ④ 操作按钮组固定在底部（`.d-dock`）

- 三个下载按钮（下载本体 / 修改器 / Mod）+ 跨源按钮整体搬进 `.d-dock{position:sticky;bottom:0}`，
  毛玻璃底 + `padding-bottom: calc(13px + env(safe-area-inset-bottom))`（刘海屏不被手势条压住）。
- ★ **必须在 `.d-body` 之外**：`.d-body` 有 `padding-bottom`，放里面 sticky 会被那段内边距垫起来，
  滚到底时底栏与内容之间留一条白缝。实拍验证：`dock.parentElement === #drawerBody`。
- 底栏内不重复留上边距（`.d-dock .dl-strip{margin-top:0}`），否则两处都留又是一条缝。

**实测**：底栏 **712×124**，`bottom=1000`＝`innerHeight=1000`（完全贴底），`.ds ×3`。

### ⑤ 手机模拟器配置合一

| | 改造前 | 改造后 |
|---|---|---|
| 结构 | 四块并列（机型清单 + 本站实测 + 逐条参数 + 指南入口） | **一块**：机型清单（前 5 台）+ 硬件面板 + 「更多」入口 |
| 机型数 | 24 台平铺（抽屉里整整一屏半的按钮墙） | **5 台**（常量 `DL_DEV_SHOW`） |
| 实测 / 参数 | 当场铺开 | 收进「更多」弹窗（**点击时才取数**，不打开详情页就拉 100 台规格 + 100 份配置是纯浪费） |
| 硬件面板 | 核心 4 节 + 「展开其余 8 节」 | **只有「硬件配置」一组** + 头部给出型号（kalvo 有品牌字段时另带品牌 tag） |

- 三个槽位：`#bhDevSlot` / `#bhHwSlot` / `#bhMoreSlot`；删掉 `#bhRecSlot` / `#bhParamSlot`
- 弹窗内的实测与参数**复用既有渲染器**（`bhRecRow` / `bhParamCard`），不另做版式
- 机型规格**分块发**（40 台/批）：`/api/device/specs` 的机型名塞在 URL 查询串里，全量一次性发有 414 风险

**实测**：默认 **5 台**；「更多」入口文案 **`查看全部 14 台机型 与实测参数 →`**（650×33）；
点开某台命中 kalvo 的机型 → 面板单个「硬件配置」组、**13 行**参数（650×250）。

### ⑥ 底部三专区预览 + 弹窗分页

- 数据源从 `/api/mods/match` 换成 **`/api/download`** —— 后者才有**「本体」**这一类
  （`sections[].count` 是源站帖数、`items[].section` 是每条地址的归属；`/api/mods/match` 只有 Mod / 修改器两类）
- 每块露前 **3 帖**（`DL_PREV_CAP`），固定序 **本体 → Mod → 修改器**（前端自己排，不跟服务端顺序）
- 帖数超 3 的块给「全部 N 帖 →」；**不足 3 帖不给按钮**（点开跟外面一样的按钮不如没有）
- 弹窗每页 **10 帖**（`DF_PAGE`）+ 上/下页（首末页 disabled）+ 多专区时的切换标签（切块**页码归 1**）
- 结果缓存进 `dlPre`：点底部「下载本体」**秒开**，不必把 302 解析再跑一遍（那一次 3-8 秒）
  —— 复用前**必须比对 url**，否则换一款游戏后会串台
- 底部「修改器 / Mod」按钮优先开**同一份 `dlSecs`** 的专区弹窗（按钮条数与点开的内容同源），
  该专区不存在时才退回 `mods/match`（XD 源没有专区概念）

**实测**（样本：渔力全开，`jidi-2117239899`，body 29 / mod 2 / modifier 2，78 个地址）：
预览 3/2/2 行，`body:全部 29 帖 →`；弹窗 **10 帖 / 页**、`1 / 3` 页、3 个切换标签；
翻页 → `2 / 3` 且内容变了；切专区 → `mod2 → 第 1/1 页 · 2 帖`。

---

## 二、本轮抓到的三个真问题

### ★ 1. 「更多」入口被嵌进 `if (devs.length && ds)` 里

`.d-body` 里的槽位 `<div id="bhMoreSlot"></div>` 在 if 外面，**填充它的代码却在 if 里面**。
后果：库里存在「机型清单为空、但有实测记录 / 逐条配置」的游戏时，入口会被一起吞掉 ——
用户既看不到机型、也拿不到本该有的实测与参数，而**所有「元素存在」断言照样绿**。

判据（`test-v1015.js` + `test-v1028-detail.js` 各一条）：
从那个 `if` 起到 `const moreSlot` 之前，必须已经出现过「4 空格缩进的 `}`」——
块内缩进一律 ≥6 空格，所以 4 空格的 `}` 只可能是这个 if 自己的闭合。

### ★ 2. 灯箱 ✕ 点不动（见 ③）

### ★ 3. 一条**判据选错**导致的假绿（反证抓出来的）

「没有 real 的条目被过滤」原本写成「帖子 key 里有没有 `u:` 前缀」——
去掉 real 过滤后，那条脏数据仍会凭 `postId` 归进某帖（key 变成 `999`，不是 `u:`），
判据**恒真**。反证第 ⑧ 条没变红才发现。改成打在**地址**上：
`所有 links 的 real 都非空`。

> 这三条都印证同一件事：**判据要打在承载该语义的节点上**，打在旁边的字段上就等于没写。

---

## 三、验证

| 层 | 结果 |
|---|---|
| 语法闸 `tools/check-inline-syntax.js` | **3 / 3 页面通过**（`index` 3447 行 / `emulator` 4732 行 / `unpack` 3853 行）；**已挂进 `run-all.js` 作前置闸**。反证实测：往 `public/unpack.html` 注入 `*/` ⇒ `SyntaxError … 3853 行`、exit 1 |
| 静态防线 `tools/run-all.js` | **前置闸 1 + 27 套 / 1823 条 / 0 失败**（新增 `test-v1028-detail.js` 119 条，已登记 `SUITES`） |
| 反证 `tools/_counterproof-v1028.js` | **11 条全部「打坏即变红」**（含 `ranOk()` 门禁：不许把「没跑到」当「没坏」） |
| 浏览器实拍 `tools/preview-v1028.js` | **43 / 43 通过**（真实点击驱动吸顶 / 灯箱 / 底栏 / 机型 / 分页） |
| **线上验收 `tools/verify-online.js`** | **27 / 27 通过**（发布后）· 对账 `audit-apps.js` exit 0「与本地逐字节一致」 |

实拍的关键判据都不是「元素存在」，而是：

- 吸顶：**高度实测变化** 250 → 62px，且 `top=0.0`
- 灯箱：**`elementFromPoint(视口正中)` 命中的就是灯箱**（只查 z-index 数字证明不了这一件事）
- 底栏：`bottom ≈ innerHeight`，且 `parentElement === #drawerBody`
- 分页：翻页后**内容真的变了**、切专区后页码为 `1` **且该专区有行**
- 硬件面板：命中 kalvo 的机型上只有**一组**「硬件配置」、13 行、650×250

## 四、本轮新踩的坑（→ `PITFALLS.md`）

1. **块注释里不能出现 `*/`**：写「改走 `**/api/download**`」时，`**/` 提前终止了块注释，
   整段变成裸代码 → `SyntaxError: Invalid or unexpected token`。
   （与铁律 13「模板串里不能写反引号」同源：**注释里也有会提前闭合的字符序列**）
2. **`indexOf('body.innerHTML = \`')` 取到的可能不是你想那处**：全页有多处（搜索弹窗骨架也有），
   必须从 `function paintDetail(` 之后开始找，否则 14 个标记全部「找不到」⇒ 一片假红。
3. **反证值钱的地方就在「没变红」**：本轮 11 条里唯一没变红的那条，暴露的是判据本身写错（见二·3）。

## 五、同步改掉的旧断言（预期变更，不是退化）

| 套件 | 原断言 → 新断言 |
|---|---|
| `test-related-dl.js` | 桩数据 `/api/mods/match` → `/api/download`；`id+t` → `url`；4 条平铺 → 三专区 3 帖 + 「全部」按钮 |
| `test-v1015.js` | `#bhRecSlot` → `#bhMoreSlot`；`.pc-rec` 拼接写法；新增「入口在 if 外」判据 |
| `test-v1016.js` | `hwToggleRest` / `HW_KEEP` / `.hw-more` → 新的 `HW_INFO_G/HW_HW_G` 分组常量 |
| `test-v1017.js` | `const more = hiddenDev > 0 ? dFullBtn(` → `moreSlot.innerHTML = hasAny ? dFullBtn(`；24 台 → `DL_DEV_SHOW=5` |
| `test-download.js` | `setDlCounts(j && j.counts)` → `setDlCounts(cnt)`（按 `/api/download` 的 sections 现算） |
| `test-match-release.js` | `items.length <= 3195`（**硬上限**，社区库涨到 3200 就恒红）→ 改查「归并产物」：带 `alt` 的条目数 > 0 |
| `preview-v1014.js` / `preview-v1015.js` | 取实测/参数前先**驱动** `#bhMoreSlot` 入口；并补「GPU 槽位取到了样本」这条前置判据（空数组会让脏值断言恒真） |

> 最后一条 `test-match-release.js` 是**既有失败**（与本轮改动无关）：社区库条目数已涨到 3200，
> 而断言写的是「≤ 改动前的 3195」。硬上限类判据迟早恒红，改成语义判据后同时更能说明问题。

## 六、发布（2026-09-21）

| 项 | 实测 |
|---|---|
| 目标 app | `wbapp_047aLTlMY7YdDmtVpp3BYa`（覆盖 LIVE，**不新建**） |
| sandbox | `445143a7b3004d749eab6be0fe8836e5`（**复用同一个**） |
| 链接 | `https://gamehub-agg-v4.app.workbuddy.host/`（**未变**） |
| 三页 md5 | `8817fbd695` / `ead2d2fb5f` / `e230eff2dd` —— **与本地逐字节一致** |
| 对账 `audit-apps.js` | exit 0 ·「LIVE 与本地逐字节一致」 |
| 线上验收 `verify-online.js` | **27 / 27 通过** |

截图确认（实拍，不是断言）：详情页首屏=**封面+标题 / 游戏预览 / 评分 8.9 + 参数表**，
底部固定条 `下载本体 + 前往XDGAME详情 + 前往机地详情`；
点「查看全部 9 台机型」→ 弹窗标题「手机模拟器配置 · 全部」，
**9 台机型 + 本站实测记录（1）+ 逐条游玩参数（12）** —— 正是「收进更多弹窗」的口径。

### ★ 发布后暴露的第四件事：**验收工具自己误报**

`verify-online.js` 里那条「详情页机型清单 **≥ 9 台**」在 v10.28 后**必然红**：
本轮把首屏改成只露 5 台（`DL_DEV_SHOW`），全量搬进弹窗。

**判定：预期变更，不是退化。** 但它守的**意图**必须原样保留 ——
「看到的是并集全量，不是上游摘要那个 6 台上限」。

⇒ 不是删掉/放宽，而是**拆成三段**，让意图仍然可证伪：

| # | 新断言 | 守什么 |
|---|---|---|
| ① | 首屏机型 **== 5** 台 | 「默认展示 5 个」这条用户口径 |
| ② | 存在「查看全部 **N** 台机型」入口且 **N ≥ 9** | 数据仍是并集全量（入口还在） |
| ③ | **真点开弹窗**、`#dlBody .df-row` **≥ 9** | ★ **只有这条等价于旧断言的口径** —— 用户确实能一次看全 |

★ 可复用的判据改写法：**「语义变了」时不要把断言调松，而要把旧断言的「意图」翻译成新交互下可观测的动作**
（这里是「点开弹窗再数一次」）。放宽 = 把守卫拆掉；翻译 = 守卫跟着行为一起搬家。
