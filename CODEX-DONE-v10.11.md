# CODEX-DONE-v10.11 —— 详情页下载入口 + 机型转译配置 + 同分类推荐重做

> 用户原话：「有问题详情中无云存档等下载链接，其次我想你能够转译配置（联网搜索，机型对应的cpu等配置）
> 同分类更多中的游戏不太适配，可能是标签的原因」
>
> 三问三项一起做。**结论先说：三个问题的根因都不是「少写了个功能」，而是数据层的前提错了**——
> ① 机地 8,943 条带网盘直链的社区帖（v10.9 抓的）从没接进详情页；
> ② 机型 → GPU 的对应关系在聚合时就被拆散，旧代码只能瞎猜；
> ③ 库里每款游戏只有 1 个类型标签，而「动作冒险」独占 36.8%，按标签推荐等价于抽卡。

---

## 一、问题① 详情页缺下载入口 → 新增「📥 下载入口」区块

### 根因

详情抽屉里已有的两个相关区块**都不是下载入口**：

| 现有区块 | 数据源 | 有没有下载地址 |
|---|---|---|
| 🛠 修改器（v10.1） | Game Cheats Manager 公开清单 | ❌ 只有「有没有 / 什么版本」 |
| 💾 云存档（v10.1） | Ludusavi 开源清单 | ❌ 只有**存档文件该放哪**（路径） |
| 📥 下载入口（**本轮新增**） | 机地社区帖（v10.9 抓的 8,943 条） | ✅ 99.9% 带网盘直链（13,390 条链接） |

也就是说：**真正能拿到东西的那一套数据（mods.json）此前只存在于预览页**，从没接进用户能点到的界面。

### 做法（按用户口径：「给下载链接的详情页跳转即可（在后面）」）

- 位置：详情抽屉**最后**（`#relSlot` 之后、底部声明之前）—— 落实「在后面」。
- 形态：**只给跳转入口**，不铺正文、不复制网盘直链；每条 =「类型徽标 + 帖子标题 + 网盘类型 + 直链条数 + 日期 + 打开帖子 ↗」。
- 查询：`/api/mods/match?id=&t=` —— **必须同时传 id 与 t**。机地的话题游戏名与端游库标题是两套命名，
  只传 id 会漏、只传名称遇到「中文/English」拼接串会漏；后端做 id ∪ 名称联合兜底。
  （实测：赛博朋克2077 走名称兜底拿到 **719 条**，只按 libId 是 0 条。）
- 同屏给出 `云存档位置 / 修改器库` 的专区入口 —— 用户提到的「云存档等」那一档。
- 接口无收录时整块隐藏（不占版面）。

### 实测

| 检查项 | 结果 |
|---|---|
| 赛博朋克2077 详情 → 下载入口 | ✅ 渲染 4 条 + 「机地社区 719 条」 |
| 首条帖子 | `v2.31a 可用200+精选MOD大合集｜画质·角色美化·装备载具·功能增强`（迅雷网盘 · 1 个直链 · 2026-09-15） |
| 跳转 | ✅ `target="_blank"` → `jidiyouxi.com/post/detail/…`（源站帖子，非本站转存） |

截图：`_preview/v1011-detail-download.png`

---

## 二、问题② 机型 → CPU 配置「转译」

### 根因（最关键的一个）

旧 `device-match.js` 读的是**聚合版** `data/bannerhub.json` —— 那里的 `dv`（机型）与 `gp`（GPU）
是**两个互不相干的数组**，对应关系在聚合阶段就丢了。代码只好写 `rec.gpu = gps[0]`，
即「**把该游戏第一个 GPU 当成该机型的 GPU**」。

实测：2,631 款游戏里 **404 款** dv 与 gp 长度都不同；长度相同也不代表逐位对应 —— 纯猜。

而 `data/bannerhub-files.json` 是**逐条配置**的原始形态：`{游戏名: [[机型, GPU, 时间, 文件名], …]}`，
机型与 GPU 在这里本来就是成对的。14,467 条配置 → 1,508 个唯一机型。

### 转译链路（四跳，**全程离线**）

```
机型代码 ──配对表──▶ GPU ──gpu-soc 表──▶ SoC ──device-board──▶ CPU 核簇描述
   SM S938B          Adreno 830        Snapdragon 8 Elite (Gen 4)      （部分机型有）
   └─ 兜底 1：机型名里的 SM/MT 编号 → soc-db（1444 款芯片）
   └─ 兜底 2：营销名别名表（『小米15』→ SM8750，人工维护 data/device-alias.json）
```

### 本轮为此踩平的四类坑

| # | 坑 | 处理 |
|---|---|---|
| 1 | **MP ≡ MC**：nanoreview 写 `Mali-G925 MP12`，社区库写 `Mali-G720 MC7`，都是核心数 | 归一化 `mpN ↔ mcN`（不做的话 **Mali 系一条都匹配不上**，而 Mali 占高频记录的 46.8%） |
| 2 | GPU 列混脏值：`Q2 2022`（季度）、`turnip_v24.2.0_R22`（驱动名）、`MediaTek NPU 880`、`Spectra (200 MP…)`、`ANGLE … on Vulkan …` | `isGpuLike()` 挡掉（**宁可显示「未收录」也不能给一条假 GPU** —— 假 GPU 会带出假性能档，比空值更误导） |
| 3 | soc-db 里同一芯片多条记录，**带后缀的那条才有正经 GPU**（`MT6789` 的 GPU 字段是脏值 `Q2 2022`，而 `Helio G100 MT6789H/I/J` 才是 Mali-G68） | 同时建「精确键 + 基号键」（`mt6789h → mt6789`），查时先精确再退基号 |
| 4 | 一个 GPU 被多款 SoC 共用，nanoreview 按性能序取首个 → **Pixel 会被标成「天玑9200 Plus」** | `pickSoc()` 纠偏：先看别名表给的型号，再看机型名线索（pixel→Tensor / 麒麟→Kirin / 天玑→Dimensity） |

### 覆盖与实测

| 指标 | 改前 | 改后 |
|---|---|---|
| 机型有 GPU | 95.6%（且含瞎猜） | **98.0%**（逐条配对，抽查全对） |
| 机型有 SoC | — | **92.6%** |
| 营销名可解析 | ❌ 不支持 | ✅ `小米15 / 红米K80 Pro / iQOO 13 / …` |

| 输入 | 解析结果 |
|---|---|
| `小米15` | Adreno 830 · Snapdragon 8 Elite (Gen 4) · 830 档 |
| `SM S938B` | Adreno 830 · Snapdragon 8 Elite (Gen 4) |
| `TECNO LJ9 MT6897` | Mali-G720 · Dimensity 9400e + **CPU 核簇描述** |
| `Redmi Turbo 4` | Mali-G720 MP7 · Dimensity 8400 |
| `Google Pixel 8` | Mali-G715 · **Tensor G3**（纠偏前会显示天玑9200） |
| `华为Pura 70` | KIRIN 9010（无性能档 —— 该 GPU 未收录，如实显示而非编造） |

截图：`_preview/v1011-device-translate.png`

> 说明：本轮实现的是**全离线**链路（配对表 + 本地芯片库 + 人工别名表）。
> 用户提到的「联网搜索」是在本地三跳全落空时的兜底位；因 nanoreview 对 node 的 TLS 指纹返回 403
> （curl 同 URL 稳定 200），联网兜底留作后续独立任务，当前离线覆盖已达 92.6%。

---

## 三、问题③「同分类更多」不适配 → 多因子相似度打分

### 根因（用户猜对了：就是标签）

```
库里 15,302 条游戏，类型标签数量分布：
  1 个标签 15,237 条（99.6%）  ← 每款只有一个标签
动作冒险 5,627 条（36.8%）｜ 模拟经营 1,770 ｜ 休闲益智 1,630 ｜ 策略战棋 1,531 ｜ 角色扮演 1,379 …
运营标签（非游戏类型）：联机整合 801 ｜ 免费专区 673 ｜ 模拟器整合 38   ← 描述「怎么玩/收不收费」
```

旧实现 `browse(g=genres[0], sort=score)` → 前端再**随机洗牌**取 6：
在「动作冒险」这个 36.8% 的超大池子里取高分再打乱，**等于抽卡**；
换成「免费专区」这类运营标签就更离谱（推荐位标题会变成「同分类更多 · 免费专区」）。

### 做法：多因子打分（`data/related.js` + `/api/library/related`）

| 因子 | 说明 | 权重 |
|---|---|---|
| ① 类型交集 | **IDF 加权**：稀有标签更可信（恐怖惊悚 6.1% ≫ 动作冒险 36.8%） | 1.5 ~ 3.0 |
| ② 系列名 | 标题剥副标题/版本词后取前缀（`生化危机4 重制版` → `生化危机`） | 5.5（过闸）/ 1.8 |
| ③ 评分接近度 | `1 - |Δ评分| / 6` | 0 ~ 2.5 |
| ④ 容量接近度 | `log2` 比值 —— 1GB↔2GB 的差距 = 50GB↔100GB 的差距 | 0 ~ 1.5 |
| ⑤ 新鲜度 + 抖动 | 抖动 < 任一因子最小间隔，只打散同分 | ≤0.9 |

三个关键设计：

1. **运营标签一律剔除** —— 不进候选池，也不作为分类标题（接口返回 `genre: null` 时前端标题降级为「相似推荐」）。
2. **同系列跨标签并池** —— 同系列常被分到不同标签（生化危机4 是「动作冒险」、生化危机8 是「恐怖惊悚」），
   只按类型取池子，续作永远进不来。
3. **系列「档次一致性」闸门** —— 前缀相同 ≠ 同一部作品：库里 `赛博朋克2077`(9 分/92GB) 与
   `赛博朋克SFX`(5 分/1.5GB 音效包) 前缀相同。评分与容量**都**接近才给满权重（5.5），
   否则降为温和加成（1.8）。**没有这道闸，音效包会顶掉正经同档作品。**

### 改前 / 改后（真实数据，`_preview/v1011-before-after.html`）

| 当前游戏 | 改前（同标签 + 评分倒序 + 随机洗牌） | 改后（多因子打分） |
|---|---|---|
| 赛博朋克2077（角色扮演 9 分） | 博德之门3 / 女神异闻录5 / 上古卷轴4 / 双魂 / 缺氧 … | 人中之龙8 8.8 / 最终幻想7重制版 9.1 / 艾尔登法环 9.3 / 龙腾世纪4 9.0 |
| 生化危机4 重制版（动作冒险 10 分） | 侠盗猎车手5 / 生化危机9 / 深潜 / 堕落之主（**与上一款完全相同**） | **全部 6 条同系列**：生化危机9 安魂曲 ×3 / 生化危机4重置版 / 生化危机8 村庄 / 生化危机7 |
| 艾尔登法环（动作冒险 9.2 分） | 同上（抽卡池相同） | 艾尔登法环 黑夜君临 ×2【同系列】+ 刺客信条起源 / 消逝的光芒困兽 / 黎明行者之血 |

**同系列命中率：改前 2/24 → 改后 8/24**（且改前那 2 条为偶然）。

截图：`_preview/v1011-detail-related.png`、`_preview/v1011-before-after.png`

---

## 四、改动清单

### 新增

| 文件 | 说明 |
|---|---|
| `data/device-gpu.js` | 机型 → GPU → SoC/CPU 精确映射（配对索引 + 芯片编号索引 + 别名 + 反查 + `pickSoc` 纠偏） |
| `data/device-alias.json` | 营销名 → 芯片编号/SoC 名，人工维护（~150 条），是全离线兜底的一环 |
| `data/related.js` | 多因子相似度打分（类型 IDF + 系列 + 评分 + 容量） |
| `tools/test-related-dl.js` | 第 10 道防线：**33 项**（打分语义 17 + 前端两函数 16） |
| `tools/preview-v1011.js` | 浏览器实拍三张图（详情下载入口 / 同分类 / 机型转译） |
| `tools/preview-v1011-ba.js` | 改前改后对照页生成 |

### 修改

| 文件 | 改动 |
|---|---|
| `data/device-match.js` | 移除 `rec.gpu = gps[0]` 瞎猜；改用 `deviceGpu` 精确配对补 GPU/SoC/CPU；库外机型走离线兜底；`nogpu` 时也返回机型信息 |
| `data/gpu-soc.js` | 新增 `socByName()`（SoC 名 → GPU，供别名表用） |
| `tools/fetch-soc-map.js`（v10.10 已有） | GPU→SoC 抓取，覆盖率自检 82.5% / 92.9% |
| `server.js` | 新增 `GET /api/library/related`（多因子推荐） |
| `public/index.html` | 新增 `#dlSlot` + `loadDlBlock()`；`loadRelated()` 重写为多因子端点；fb 补 `id`；新增 `.d-dl-*` / `.rel-it .why` 样式 |
| `public/emulator.html` | 由 `tools/build-emulator-page.js` 同步（CSS / 抽屉 / 脚本三段一致） |

---

## 五、验证

| 套件 | 结果 |
|---|---|
| `test-related-dl.js`（**新**） | ✅ 33 / 33 |
| `test-mods.js` | ✅ 77 / 77 |
| `test-saves-match.js` | ✅ 44 / 44 |
| `test-date-norm.js` | ✅ 47 / 47 |
| `test-filter-layout.js` | ✅ 77 / 77 |
| `test-search-ui.js` | ✅ 44 / 44 |
| `test-emulator-page.js` | ✅ 118 / 118 |
| `test-emulator-structure.js` | ✅ 128 / 128 |
| **合计** | **✅ 568 / 568** |

浏览器实拍（Edge headless）与线上接口抽查：

| 项 | 结果 |
|---|---|
| `GET /api/library/related?t=只狼：影逝二度&g=动作冒险` | ✅ 返回打分结果（pool 5627） |
| `GET /api/mods/match?id=xd-191&t=赛博朋克2077` | ✅ count 719，条目均带 `url` 与 `links[]` |
| `GET /api/device/match?model=小米15` | ✅ Adreno 830 / Snapdragon 8 Elite (Gen 4) / 合计 2626 款可玩 |

---

## 六、遗留

1. **联网兜底未做**（用户提过「联网搜索」）：当前三跳全离线。真正需要联网的场景只剩
   「机型名既无 SM/MT 编号、又不在别名表里」的少数情况（如 `Vivo V2405A`）。
   若要做，注意 nanoreview 对 node fetch/https 返回 403（TLS 指纹差异），需用 curl 或换数据源。
2. **别名表是人工维护的**（`data/device-alias.json`，~150 条）。新增机型只需加一行；
   建议后续按「搜索无结果的关键词」定期补。
3. **同系列靠前缀匹配**，会有少量误判（`赛博朋克SFX` 这类衍生品虽已降权，仍会带「同系列」以外的弱关联）。
   若要根治需要分词/词表，成本较高，暂不做。
4. **同分类推荐仍受「单标签库」限制**：类型因子只能在 12 个大类里做文章。
   若源站补上多标签/英文标签，`related.js` 的类型因子与标签交集可直接受益（无需改接口）。
5. 详情抽屉的「下载入口」只覆盖**机地社区**；XDGAME 侧没有同类社区帖数据，暂无法补。
