# v10.20 —— 解包配置匹配（新页面）

> 日期：2026-09-18
> 触发：用户「我还需要你加入一个功能（后续我会输入一个 JSON，这个是游戏解包的，待会给你样本，
> 你提取配置后进行匹配能够适配的游戏搜索，类似现在的机型兼容）」+
> 「样本大概是 导出一个 JSON 类似兼容层最低配置等，可以先建立一个页面准备」

## 一、这一版做了什么

用户要的新功能**样本还没到**，所以本版先把「页面 + 识别引擎 + 判定引擎」立起来，
并刻意把「未知字段名」这件事设计成**可扩展的词典**——
样本到手后只改一个文件，界面和接口都不用动。

| # | 交付 | 说明 |
|---|---|---|
| 1 | **页面** `public/unpack.html`（`/unpack.html`） | 贴 JSON → 看识别结果 → 出可适配游戏清单 |
| 2 | **识别词典** `data/spec-dict.js` | 字段名未知也能认出「这是什么配置」 |
| 3 | **判定引擎** `data/spec-match.js` | 配置对撞 Steam 官方配置库（650 款） |
| 4 | **接口** `GET /api/spec/dict` · `POST /api/spec/analyze` | 首次给全站加 `express.json` |
| 5 | **防线** `test-spec.js`(82) · `test-pages-sync.js`(25) · `preview-v1020.js`(38) | 三层都补了 |

## 二、核心设计：字段名未知怎么办

样本没到，字段名不可知。所以识别走**双通道**，而不是写死键名：

| 通道 | 做法 | 置信度 |
|---|---|---|
| 键名 | key 归一（小写、去 `_ - . [ ]`）后与关键词表比对 | 高 |
| 值形态 | 值长得像 `Adreno 740` / `SM8750` / `arm64-v8a` 就推断 | 低（UI 标「推断」） |

★ 值通道**只取命中片段**，不是整串。实测踩到：值 `"OnePlus 13 (SM8750)"`
若整串当 GPU 名交给 `gpuScore()`，会落进 `fam:'other'` → **固定 400 分的假精度**；
只取 `SM8750` 才能被 `gpu-tier.js` 认出来。

## 三、判定口径（四个维度，都可解释）

不用显卡跑分比 —— `gpu-tier.js` 的 `gpuScore()` 是**移动 GPU 量纲**（Adreno/Mali 映射），
而 Steam 配置里是 PC 卡自由文本（"NVidia 6600"），落进去只会得到固定 400 分，
**比出来的是假精度**。所以只用四条能可靠解析的：

| 维度 | 判定 | 依据 |
|---|---|---|
| `arch` | ARM 跑 x86 游戏必须有转译层（box64/box86/FEX），缺了**结构上跑不起来** | 配置的 arch + 兼容层 |
| `dx` | 兼容层决定 DX 上限：DXVK→11，VKD3D→12 | 配置的兼容层 |
| `ram` | 与游戏最低内存要求逐款比较 | 双方文本解析 |
| `storage` | 与游戏所需可用空间比较 | 双方文本解析 |

`arch` + `ram` 是**关键维度**（缺了只能「待确认」），`dx` + `storage` 是次要维度
（缺了照样给结论，但如实列进 `unjudged`）。

## 四、实测踩到的坑（全部已钉成断言）

### 坑 1：`minRequirements.ram` 把设备的 `memory` 顶掉
一份 JSON 里常常同时写着「这台设备有什么」和「这游戏最低要什么」：
```json
{ "memory": "16 GB", "minRequirements": { "ram": "8 GB" } }
```
最初按「最低档优先」取值 → 拿**游戏的要求**当**设备的能力**去比，越比越离谱。
现改为 `profile.*` 只装自身配置，`p.req.*` 单独收要求，缺失时才退回并标 `src:'req'`。

### 坑 2：`minRequirements.dx = 12` 曾被读成「我支持 DX12」
要求 ≠ 能力。这会让任何 DX12 游戏都判「可跑」——**假阳性的源头**。
现在 `profile.api` 只收自身配置，要求档的 dx 进 `p.req.dx`。

### 坑 3：`"OnePlus 13 (SM8750)"` 整串当 GPU 名
见上文「双通道」。

### 坑 4：口径校准（两次，都是数字逼出来的）
| 口径 | 改前分布 | 改后 | 为什么改 |
|---|---|---|---|
| 游戏未标该项 → `unknown` | 待确认 **535**/650 | 待确认 **0** | 游戏没标 ≠ 我方不知道，不该降级 |
| 架构未标 → `unknown` | 待确认 **535**/650 | 待确认 **0** | PC 配置本来不写指令集，一个无关维度淹没了整张清单 |

改后分布：**流畅 555 / 可跑 20 / 不可跑 75**（示例配置），信息量才算出来。

### 坑 5：搜「GTA」返回 0 条
实拍抓到：库里叫 `Grand Theft Auto V 传承版`，字面包含根本匹配不到 "GTA"。
补 `abbrOf()` 首字母缩写匹配（`gtav` ← `Grand Theft Auto V`）。

### 坑 6：排序按余量 → 榜上全是小体量
最初按余量排，「能跑的最吃配置的游戏」全被几百 MB 的小品挤掉。
改为**规模优先**（内存为主、存储为辅），榜首变成 GTA V（scale 19.63）。

## 五、文件清单

| 文件 | 动作 |
|---|---|
| `data/spec-dict.js` | 新增 · 识别词典（样本变了只改这里） |
| `data/spec-match.js` | 新增 · 判定引擎 |
| `public/unpack.html` | 新增 · 派生页（由生成器产出） |
| `tools/page-assets.js` | 新增 · 从主源抽共享资产（CSS/顶栏/遮罩/脚本） |
| `tools/build-unpack-page.js` | 新增 · 生成 unpack.html |
| `tools/unpack-sections.js` | 新增 · 页面专属脚本 |
| `tools/test-spec.js` | 新增 · 82 断言 |
| `tools/test-pages-sync.js` | 新增 · 25 断言（派生页同步防线） |
| `tools/preview-v1020.js` | 新增 · 实拍 38 断言 |
| `tools/run-all.js` | 改 · 纳入两个新静态套件（15 套 → 17 套） |
| `server.js` | 改 · 加 `express.json` + 两条 `/api/spec/*` |
| `public/index.html` | 改 · 顶栏加「📦 解包匹配」入口 |
| `public/emulator.html` | 重建 （顶栏同步到三入口） |

## 六、验证

| 层 | 结果 |
|---|---|
| 静态 | `run-all.js` **17 套 1080 / 0** |
| 实拍 | `preview-v1020.js` **38/38** |
| 接口 | 合法样本 **575 款可跑** · 非法 JSON 400 · 空体 400 · 数组样本按条匹配 |

★ 实拍里的两条关键断言（都是本版最容易做错的地方）：
① 示例设备内存取 **16 GB** 而非最低要求的 8 GB、存储取 **256 GB** 而非 40 GB（分层生效）；
② 图形接口显示 **「DX ≤ 11 / 据兼容层推断」** —— 示例只写了「要求 DX11」，那是**要求不是能力**。

★ 新增的 `test-pages-sync.js` 是本版**额外收益**：本项目「改了主源忘了重建派生页」
一直没人守（派生页不报错，只是悄悄漂移）。现在用 CSS 尾部指纹 + 函数清单比对，
主源一变而派生页没重建就会立刻报出来。

## 七、遗留（等用户输入）

1. **样本未到**：字段映射待校准。已有词典已能覆盖常见命名（键名 + 值形态），
   样本到手后**只改 `data/spec-dict.js`**。
2. `arch` 未标但出现 ARM 专用层（turnip 等）时会给「待确认」——
   样本里若有明确架构字段，这条会自然消失。
3. 显卡维度暂不参与判定（原因见第三节）。若样本里的 GPU 是移动 GPU，
   `gpuScore()` 可直接启用，届时把 `cmpGpu` 加回 `judge()` 即可。

## 八、推送时踩到的网络坑（重要，值得单列）

### 现象
`git push` 连续失败，报错在三句话之间跳：
`CONNECT tunnel failed, response 502` / `OpenSSL SSL_read: unexpected eof` / `Error in the HTTP2 framing layer`。
一开始按「瞬时抖动」重试了 6 轮 ×2 组，全败 —— **不是抖动**。

### 定位（用连通性探测把「网络」和「代码」分开）
```
www.baidu.com     OK 200
api.github.com    OK 403     ← 通！（未授权时 403 是正常的）
github.com        TIMEOUT    ← 0/6 可达
```
DNS 也正常（github.com → 20.205.243.166，api.github.com → 20.205.243.168）。
**结论：不是沙箱策略（关掉沙箱重试同样失败），是 github.com 这个主机被阻断。**

### 改法：走 GitHub REST API（Git Data 通道）
`blob → tree → commit → 更新 ref`，等价于一次 fast-forward push。
落在 `tools/_push-via-api.js`（`tools/_*` 已在 .gitignore，属一次性工具）。

★ **这里踩了第二个坑，而且更隐蔽**：第一版脚本直接读**磁盘文件**建 blob，结果
```
public/index.html   磁盘 228574B（含 3264 个 CRLF）  →  入库应为 225310B（纯 LF）
```
远端 tree `b838a05` ≠ 本地 tree `02d5d57` —— **推上去了不一样的内容**，而且 API 全程 201 成功、
`git status` 也干净（`core.autocrlf=true` 让两边「看起来」一致）。
⇒ 必须用 `git cat-file blob HEAD:<path>` 取**入库字节**、`git ls-tree` 取**入库模式**。

改完后：新 tree == 本地 tree；复刻作者/时间戳后 **新 commit sha == 本地 sha（`c5fc988`）**，
远端 main 与本地 HEAD 完全同一个 sha。

### 沉淀成防线
`tools/report.js` 的「④ 是否更新到 GitHub」原先只走 `git ls-remote`，
在上述网络下会**永远显示「无法确认」**，用户就再也拿不到确定答案。
现改为：**先 ls-remote，失败自动退到 `api.github.com`**，并在输出里**标明走的是哪条路径**
（不能让「探测方式变了」变成「假装没查到」）。两条都失败才判「无法确认」。
`tools/test-report.js` 补 8 条断言守住这个回退（含「顺序不能反」）。

### 附带修掉汇报里的一个「报反」

`report.js` 的「② 分享链接」在「本地 md5 ≠ 线上 md5」时，原本用 `/.chip\.ol/` 猜版本方向：

```js
out.ver = out.same ? '与本地同版' : (/.chip\.ol/.test(txt) ? '新于本地？' : '旧版');
```

实测**报反了**：线上停在 v10.18 之前（缺 v10.19 的指南模块导航），而两边都有 `.chip.ol`，
于是显示成「**新于本地？**」—— 用户会以为线上已经是最新，**比不汇报更危险**。

改为**特征指纹逐条比对**：

```js
const FEATURES = [
  { re: /id="navUnpack"/, name: '顶栏「📦 解包匹配」入口', since: 'v10.20' },
  { re: /eg-nav/,         name: '指南模块导航',           since: 'v10.19' },
  { re: /id="navEmu"/,    name: '顶栏「手机专区」入口',    since: 'v10.18' },
];
// 本地有、线上没有 ⇒ 线上旧（取最早缺的那个当「线上停在哪一版」）；反之 ⇒ 线上新
```

现在输出直接是：
`⚠️ 旧于本地（线上尚未发布 v10.19）· 缺：指南模块导航`。
`test-report.js` 补 8 条断言（含「不许再出现『新于本地？』这种猜法」、
「方向不能写反」——断言正则别写成 `/!re\.test\(/`，源码是 `f.re.test(...)`）。


