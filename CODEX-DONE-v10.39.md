# CODEX-DONE v10.39 —— 把「结论依据」摊开 + 配置要求接上每日增量 + 修掉一份活了 3 个版本的文档事故

> 日期：2026-09-24 ｜ 状态：**本地完成**（改 `data/spec-match.js`、`tools/*`、`public/unpack.html` 与四份文档）
> 主题：**不改结论、只把依据摊开**；把预热接成每日增量；顺手挖出一份一直在仓库里但没人发现的文档事故。

---

## 一、本轮做什么（授权与排序）

用户在本轮前的盘点里批准了三件事：① P0-1 走「**降级标注**」方案 ② **授权发布** ③ 按建议顺序执行。
建议顺序 = `P0-1 口径 → P1-1 匹配率 → P1-2 增量 → 文档 → P2`。

| # | 项 | 结果 |
|---|---|---|
| **P0-1** | 「游戏没标 DirectX ⇒ 该维度 skip ⇒ 仍判流畅」口径 | ✅ **不改结论，改「把依据摊开」** |
| **P1-1** | 手游中心匹配率二轮（51.2% → ?） | ✅ **实测判定「不该改」**（负面结论，见第三节） |
| **P1-2** | 把「配置要求」接成每日增量 | ✅ `daily-sync.js` 8 步 → **9 步** |
| **P2** | 文档 | ✅ `ROADMAP.md` 重写基线 + `WORKFLOW.md` **去掉 336 行重复副本** + 新增文档结构闸 |

---

## 二、P0-1：口径不动，「依据」必须看得见

### 问题

`skip` 这一个状态**背了两种语义**：

- **「我方配置没涉及」**（如 arch —— PC 配置本来就不写指令集）⇒ 正常，不该显示；
- **「游戏数据没标」**（如某款游戏没写 DirectX 要求）⇒ **用户看不到任何提示**，
  于是看到「流畅」却不知道**这一维压根没有依据**。

前端把两种一并过滤 ⇒ 用户只看得到结论、看不到结论的**强度**。

### 改法（不动 `verdict` 逻辑一个字符）

| 层 | 改动 |
|---|---|
| `data/spec-match.js` | 每个 `skip` 标**责任侧** `side:'game'`（游戏没标）/ `side:'profile'`（我方没标）；`judge()` 新增 `unstated`（游戏侧未声明）+ `basis`（**真正参与判定**的维度）；`analyze()` 透传；stats 加 `unstated` 分布 |
| `tools/unpack-sections.js` | 卡片挂紫色 chip「**未声明：图形接口**」，title 里写明「本款结论由 … 判定」 |
| `tools/build-unpack-page.js` | `.up-ch.us{background:#F2EFFB;color:#6D28D9}` —— **与「我方配置未判定」分开配色**，否则用户分不清「该去补配置」还是「该降低对结论的信任」 |
| 「凭什么这么判」面板 | 自述新口径：游戏没标某项时该维度**不参与判定**，卡片会挂提醒，**结论不因此翻成「待确认」** |

### 实测

| 指标 | 值 |
|---|---|
| 「缺 DX ⇒ 仍判流畅」影响面 | **6,585 款**（占 17,120 的 **38.5%**）—— 不是旧记忆里的 57 条 |
| 接口 `stats.unstated.dx` | **6,619**（与上一条独立口径互证；差 34 来自当轮新入库 60 条） |
| 实拍 chip | 宽高 **96×21**、`computedStyle.color = rgb(109,40,217)`、未被裁切 |
| 实拍对照 | 星露谷物语显示「图形接口 通过」；缺 DX 的显示紫色「未声明：图形接口」 |

★ **改前先纠正一处旧认识**：原以为这是个小众边界（几十条），实测影响 **38.5%** 的条目。

---

## 三、P1-1：实测判定「不该改」（负面结论也是结论）

### 做法

抽 **40 条** C 类（「匹配器漏」）逐条看。结论与预期相反：

| 观察 | 数量 / 比例 |
|---|---|
| **垃圾查询名**（exe 残留 / 编码损坏） | 约 **40%**，如 `AssassinsCreedIIGame`、`Ragnar k`、`LEGOMARVELAvengers DX11`、`Tradu o do` |
| 数字盲区（查询有数字、命中标题一个都不含） | 仅 **15 条** |
| 其中**真错配** | **2~3 条**（`NBA2K16` → `NBA 2K27`、`WWE 2K16` → `WWE 2K26`） |
| 收紧护栏会**误杀的正确匹配** | **12 条** ⇒ **净收益为负** |

### 结论

**51.2% 已接近 BannerHub 数据源的天花板。瓶颈在「查询名质量」（源站数据本身脏），不在匹配器。**
⇒ 继续调匹配器是**负收益**；要再提只能**换/加数据源**。
把这条写进 `ROADMAP.md`，并**撤销**原「3-A 详情页封面补齐」——实测证明封面是匹配率的镜像（见第五节）。

---

## 四、P1-2：配置要求接上每日增量

### 新增第 9 步 `steamreq`（放在最后）

```
① node tools/build-steam-req.js --limit=500   ← 联网补缓存
② node tools/build-spec-req.js                ← ★ 再重建派生索引
```

★ **缺第 ② 步就是白抓** —— 索引的输入是缓存产物。这一步是本轮最容易漏的地方，已写进
`WORKFLOW.md` 的每日管道表 + 有专门断言守它（`test-daily-sync.js` 128 条里那条
「★ 同时调 build-spec-req.js（② 重建派生索引，缺了就是白抓）」）。

### 三个设计决定

| 决定 | 理由 |
|---|---|
| **上限 500 条/日** | 实测 1.08s/条 ⇒ 500 条约 9 分钟。不设上限会让「每日任务」变成小时级 |
| **顶到上限时报「仍有积压，次日续补」** | 让模型/人知道**还没补完**，而不是把 500 当成「今天全干完了」 |
| **第 9 步单独给 25 分钟超时** | 全流程唯一要联网逐条请求的步骤；v10.37 全量期间触发过 144 次限流退避 |

### 实测

| 指标 | 值 |
|---|---|
| 积压（待补条数） | **925** |
| 抽跑 60 条 | **100% 命中** · miss 0 · err 0 ⇒ 证明是真·新增，不是重扫旧数据 |
| 速率 | **1.08 s/条**（与 v10.37 的 38 条/分 一致） |
| 索引重建 | **1.3 s**（17,177 款；DX 标注 10,535 → **10,567**） |
| `--dry` 零写副作用 | 8 个写步骤全被拦 · 三个产物 **mtime 完全未变** |

---

## 五、顺带挖出并修掉的两个真问题

### 1. ★ 一份活了 3 个版本的文档事故（`WORKFLOW.md`）

**症状**：`WORKFLOW.md` 752 行，其中 **336 行是整份文档的副本**。

**根因（查 git 历史定位到提交）**：`c0a8db6`（v10.36）把**整份文档**插进了
「已完成」表那条 v10.36 长行的**中间** —— 文件 = `[前 336 行] + [一整份文档] + [那行的后半]`。
后果比"多了一段"更糟：

- 那条表行被**劈成两半**（前半在 L337、后半在 L673，中间夹着 336 行）；
- **连文档标题都被挤进了表行末尾**（`…加 \`\s*application\s*\`# GameHub 工作流程总纲`）；
- **3 个版本无人发现**：Markdown 渲染正常（重复段落只是重复显示）、所有套件都不看 `.md`、
  `git diff` 看起来只是"改了一行"。

**修法**：锚点断言 + 重建（`tools/_probe-workflow-dup.js`，**锚点失配即报错，绝不"猜一个位置"**）。
`git diff` = **1 insertion / 337 deletions** —— 与机制推演完全吻合（删 336 行副本 + 删被劈开原行、加接回后的 1 行）。
416 行、43 个标题零重复、**CRLF 逐字节保持**（`join('\n')` 天然保留：每个元素自带 `\r`）。

**为什么会有这个探针**：`_*` 已在 `.gitignore`，它不入库，但作为**手动复查工具**留着。

### 2. ★ 反证脚本自己写错了，却"看起来通过了"

首版反证的第 ⑤ 组写的是：

```js
[...lines.slice(0, pi + 1), '', sub, '', lines.slice(pi + 1)]   // ← 漏了展开运算符 `...`
```

`lines.slice(pi+1)` 没有展开 ⇒ **「剩余全部行」被当成一个数组元素** join 进去 ⇒ README.md 被整体搞乱。
结果是：闸**确实变红了**，但红的是「一级标题 0 个」，**不是我预期要打的那条判据**。

⇒ **教训：「变异把它弄坏了」≠「变异按设计弄坏了它」。**
已给每个变异加 **`selfCheck`**（变异必须先自检生效，否则该组判**作废**，不算"已抓住"）。
修好后 ⑤ 正确命中「标题路径无重复」；顺带发现第 ④ 组的**自检自己**也没做围栏感知（把 ```bash 里的
`# 注释` 数成标题，报到「6 个」），一并修掉。

---

## 六、新增防线：文档结构闸 `test-doc-structure.js`（145 条）

**为什么需要**：静态断言套件只能守住**它已知的**东西。文档从没被守过，所以它怎么坏都没人知道。

| # | 判据 | 抓什么 |
|---|---|---|
| ① | 一级标题恰好 1 个 | 整份被复制 / 标题被删。归档文档放宽到 2（走**显式例外表 + 自检陈旧**） |
| ② | **任意偏移下无大段重复** | ★ **主力判据**：不依赖标题，所以「标题被吞进行尾」也能抓 |
| ③ | 标题**路径**唯一 | 同父节下的同名子节（复制粘贴事故的典型形态） |

**两处实现坑（都踩过并修掉）**：

- ① **必须跳过围栏代码块** —— 否则 ```bash 里的 `# 或手动：kill 掉 8123` 会被当成标题，
  `WORKFLOW.md` 被误算成「24 个一级标题」、`README.md` 被算成 2 个（首版 9 条 FAIL 全是这一类）。
- ② **「标题文本唯一」太严** —— `CODEX-DONE-v10.5.md` 的 `### 修法` ×3、`README.md` 的 `### 验证` ×2
  都是**不同父节下的同名子节**，完全合法 ⇒ 改成「标题**路径**唯一」。

**标定**：健康态 47 个根目录 `.md` 最高偏移重复 **1.8%**；事故态 **77.3%** ⇒ 门槛取 **60%**，留 30 倍余量。

### 反证 6/6（`tools/_counterproof-v1039.js`）

| # | 变异 | 结果 |
|---|---|---|
| ① | **真实事故回放**：把 752 行污染版放回去 | ✅ 抓住 **FAIL×2**（偏移重复 **77.3% @ 偏移 336 行** + 标题路径重复） |
| ② | ROADMAP 前 200 行追加到自己末尾 | ✅ 抓住 **FAIL×3**（偏移 **100.0%**） |
| ③ | PITFALLS 中间 150 行插回前半部分 | ✅ 抓住（副本插在**文档中间**时也会红） |
| ④ | 删掉 ROADMAP 的一级标题（0 个） | ✅ 抓住 |
| ⑤ | README 同一父节下加同名子节 | ✅ 抓住**「标题路径无重复」** |
| ⑥ | 把偏移门槛改成 1.01（永不触发）+ 放回事故版 | ✅ **仍红，由标题路径判据兜住** ⇒ 证明两条判据有分工 |

**假断言 0 · 崩了 0 · 作废 0 · 全部文件按字节还原（5 份 md5 一致）**

---

## 七、实测数据汇总（全部现测，不抄记忆）

### 静态防线

| | 改前 | 改后 |
|---|---|---|
| 套件 | 37 套 | **38 套** |
| 断言 | 2558 条 | **2703 条**（失败 0） |
| 前置闸 | 2 / 2 | 2 / 2 |
| 整套耗时 | ≈2 分钟 | ≈2.5 分钟 |

### 数据层（2026-09-24 接口实测）

| 库 | 规模 |
|---|---|
| 端游库 | **19,074** = XDGAME 15,448 + 仅机地 3,626 ｜双料 13,448 |
| 机地话题 | 17,220 |
| PC 要求库 | **17,177**（最低 17,146 / 推荐 11,366 / 有 DX **10,567**） |
| 配置要求缓存 | cached **16,371** · withReq **16,170** · miss 200 |
| 手游中心 | 3,226 ｜匹配 **1,652 = 51.2%** |
| 修改器 | 3,635（匹配 2,812 = 77.4%） |
| Mod | 8,943（匹配 7,076 = 79.1%） |
| 云存档 | 6,606 / 15,031 路径 |
| 机型库 | 1,058 台 / 17 品牌 |
| 机型市场库 | 11,500 行 / 冲突 **3,239** |
| xref | 修改器 2,365 · 存档 6,265 · 双料 1,695 |

### ★ 线上版本实测（纠正一条记忆错误）

**线上不是 v10.28，是停在 v10.32。** 判据：

| 判据 | 线上 | 本地 |
|---|---|---|
| `index.html` md5 | `9ae5854778` | `9ae5854778`（**相同 —— 但它证明不了什么**） |
| `unpack.html` md5 | `2db227c9d1` | `2b3dcc38dd` |
| `/api/spec/dict` 的 `dict` | **`v10.20`** | `v10.34` |
| 同接口 `archRule` 指纹 | **`undefined`**（v10.35 才有该字段） | 有值 |
| `/api/pcreq/stats` | cached **713** / withReq **708** | cached **16,371** / withReq **16,170** |
| `/api/library/stats` | total **19,010** | total **19,074** |

⇒ **首页 md5 自 v10.32 起未再变动，所以它"看起来一样"**。判线上版本必须
**前端 md5 + 服务端口径指纹 + 新版独有字段**多条一起过；任何**单条**判据都会假绿
（v10.30 栽在「HTTP 200」、v10.38 栽在「只比前端 md5」、本轮再次实证）。

---

## 八、文件清单

| 文件 | 改动 |
|---|---|
| `data/spec-match.js` | `side` 责任侧分流 + `unstated` / `basis` + stats.unstated |
| `tools/unpack-sections.js` | 「未声明」chip + 「本款结论由 … 判定」+ 面板自述新口径 |
| `tools/build-unpack-page.js` | `.up-ch.us` **紫色**配色（与「我方未判定」区分） |
| `public/unpack.html` | **重建**（`test-pages-sync.js` 25/25） |
| `tools/daily-sync.js` | 第 9 步 `steamreq` + `readPrewarm()` + `fmtSteamreq()` + 超时键 25 分钟 |
| `tools/test-daily-sync.js` | 101 → **128 条** |
| `tools/test-doc-structure.js` | ★ **新建**（145 条，已登记 `SUITES`） |
| `tools/run-all.js` | `SUITES` 37 → **38** |
| `WORKFLOW.md` | 去重 **752 → 431 行**；基线 v10.22 → **v10.39**；第三节改 9 步；第四/六节换 09-24 实测；第七节补 v10.37/38/39 三条 |
| `ROADMAP.md` | 基线 v10.32 → **v10.39**；规模表全换 09-24 实测；**两个结论被改写**（封面证伪 / 匹配率天花板）；撤销 3-A |
| `tools/_probe-workflow-dup.js` · `_probe-doc-numbers.js` · `_probe-roadmap-numbers.js` · `_probe-live-fingerprint.js` · `_counterproof-v1039.js` | 新建（`_*`，不入库） |

---

## 九、如实：本轮没做的

| 项 | 说明 |
|---|---|
| **2-D 推荐配置覆盖率 66.2%** | 17,177 款里推荐配置 11,366 —— 「源站就缺」还是「没取到」**未查证**，需抽样 30 条比对源站。已列入 `ROADMAP.md` 待你决策第 5 条 |
| **第二层实拍（24 个 `preview-v*` + 9 个回归）** | 本轮未跑全量；P0-1 的 chip 用专探针实拍验证（8/8 PASS）。全量实拍需**分批**（同 CDP 连跑会 detached Frame） |
| **`基础测试数据.xlsx`** | 仍缺失（连续第 9 日），第 4 步标 `blocked / NEED_XLSX` **不误报为失败** |
| **旧 app 清理** | 工作区留了多个同名 app，只能**手工清**；★ 绝不能用 `unpublish`（会连正式入口一起下掉） |
| **发布** | 本轮用户已授权，但**被 MCP 代理故障阻塞、未上线** —— 见第十节 |

---

## 十、发布：本地已就绪，被 MCP 代理故障阻塞（**未上线**）

### 结论（先说数字）

| 项 | 实测 |
|---|---|
| 提交 | `6a249d4`（作者 `liyou <2870805343@qq.com>`，与上三次提交一致） |
| 推送 GitHub | ✅ 远端 `main` = 本地 HEAD = `6a249d4`，tree `24c63210` 逐字节一致 |
| **发布** | ❌ **未上线** —— 5 次尝试全失败（1 次 `Request timed out` + 4 次 `fetch failed`） |
| 线上现状 | 仍是旧版：`index` `9ae5854778`（自 v10.32 未改）· `unpack` `2db227c9d1` ≠ 本地 `2b3dcc38dd` · `/api/spec/dict` → `dict="v10.20"` + `archRule` **undefined** · `/api/pcreq/stats` cached **713**（本地 16,850） |

### 根因：不是载荷太重，是 `connector-proxy` 整层故障

第 1 次用**项目根**（382 MB / 2,468 文件）发布 ⇒ `Request timed out`，**符合预期**（太重）；
改用**发布载荷**（79.8 MB / 258 文件）后连续 4 次 `fetch failed`。

★ **决定性诊断**：用「**空目录 + 不存在的 appId**」再调一次 —— 仍是 `fetch failed`。
   请求根本没到达工具逻辑（否则会返回 `unknown appId` 的结构化错误）⇒ **与体积无关**。
★ **跨工具复现**：`workbuddy_marketplace_skill`（另一个走 connector-proxy 的工具）同样
   `fetch failed` ⇒ **代理层整体故障**，不是 sites 工具本身的问题。

⇒ 需**用户侧**介入（重启 WorkBuddy 客户端 / 检查连接器服务）后重跑；**无需重做任何准备工作**。

### 发布载荷已就绪（恢复后重跑一条命令即可）

沿用 `tools/_build-deploy-payload.js` 既有机制：项目目录 **972.1 MB / 14,085 文件**
⇒ 载荷 **79.8 MB / 258 文件（8.2%）**，MUST **11/11** 齐全，无 0 字节文件。

手工复核（该脚本自带 `--verify` 的 10 秒等待偏短，本次改为手工起服务验证，**载荷本身无问题**）：

| 判据 | 载荷实测 |
|---|---|
| 首页 | HTTP 200 · md5 `9ae5854778` —— **与本地同字节** |
| `/api/spec/dict` | `dict:"v10.34"` + **`archRule` PRESENT**（v10.38 的服务端判据已在载荷里） |
| `/api/library/stats` | total **19,120** |
| `/api/pcreq/stats` | cached **16,850** |

### 本轮新增一条验收判据（补掉一个真缺口）

`verify-online.js` 的 `MUST` 判的是「**某个串在不在首页**」，而 v10.33~v10.39 这一整类版本
**前端 `index.html` 一字未改**（改动全在 `data/**` 与 `tools/**`）⇒ 对它**结构性失明**；
派生页（`emulator` / `unpack`）此前**完全没有判据**（该文件注释即写明「只写首页能看到的串」）——
v10.39 恰好改的就是 `public/unpack.html`，**改了个没人看的地方**。

⇒ 新增「**派生页逐字节比对**」（不比特征串、直接比 md5）：零维护，对派生页任何改动都敏感。
   **区分度已在同一次运行内自证**（两个样本给出不同结果 ⇒ 判据既不恒真也不恒假）：

| 页面 | 线上 | 本地 | 判定 |
|---|---|---|---|
| `emulator.html` | `00a8628f62` | `00a8628f62` | ✅ 一致 |
| `unpack.html` | `2db227c9d1` | `2b3dcc38dd` | ❌ 线上是旧版（**正确的红**：该页确实未发布） |

### ★ 顺带查清：另有 6 条**既有时变红**（与本次改动无关）

按铁律 14 做对照 —— 回退到 HEAD 跑同一套件，报错逐字相同即「既有」：

| 版本 | 断言 | 通过 | 失败 |
|---|---|---|---|
| HEAD（原版，无新判据） | 29 | 23 | **6** |
| v10.39（+2 条派生页判据） | 31 | 24 | **7** = 同样 6 条（**逐字相同**）+ 新增 1 条（`unpack` 未发布） |

那 6 条全部指向**同一件事**：样本游戏机型数 **6 台**（= 上游摘要上限，未合并），
实测**连拉 3 次稳定 6 台**（不是瞬时抖动）。而 `/api/spec/dict` 仍为 `v10.20`
⇒ **沙箱数据没被换**（若沙箱换成了本地新 data，`dict` 会变成 `v10.34`）
⇒ 这是线上**运行时抓取 / 缓存**的输出退化。**成因未定论**（不能排除「发布尝试触发沙箱重启、
内存缓存清空」，也无证据支持）⇒ 如实留作待查：这几条断言依赖运行时数据、**会随机变红**，
下一版应改为不依赖运行时抓取的样本，或显式标注该口径。
