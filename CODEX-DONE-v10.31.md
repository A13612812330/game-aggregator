# v10.31 —— 详情页四条体验修正（顶图去动画 / 顶图同源 / 固定五个 / 同类卡统一）

> 用户四条口径（原文）：
> ①「详情页的顶部图不要动画了，现在还是会在一定的位置游戏名忽大忽小的闪」
> ②「我想要的置顶图是跟前端页面展示时一样的，而不是［那张糊图］，不知道哪里的图」
> ③「我需要修改器+云存档的固定显示五个的，而不是如果一边只有一个那个卡片大小就变小了」
> ④「同分类更多的卡片我需要统一大小，而不是为了完整显示图片而拉长（按比例缩放图片比较好吧），
> 且最好是显示8个」
>
> 基线 v10.30 · **已于 2026-09-21 17:25 发布上线**（覆盖同一 app，链接未变）
> 线上 md5 = 本地 md5 = `2cc4756045cce4459a6d1221662cde23` —— 见第六节

---

## 一、① 「游戏名忽大忽小地闪」= **两条原因并发**

用户描述的是「一个症状」，实测拆出来**两条同时成立**的成因。只修一条，症状照样在。

### 成因 A：阈值取早了 ⇒ **两个游戏名同屏**

旧阈值是 `hero.offsetHeight - 62 + 12` = **200**，而大图整块高 **250**。
`.d-title` 在视口里占 `186.8 ~ 236` —— 滚到 **210** 时大标题**还剩 26px 在屏上**，
小条却已经显形了。屏上同时出现「大号游戏名（残留）」+「小号游戏名（新出现）」，
观感就是**忽大忽小**。

### 成因 B：小条带位移 + 过渡 ⇒ **一边下滑一边扫过大标题**

旧实现是 `transform:translateY(-102%)` + `transition 260ms`。
小条要从大标题**正上方 63px 处**滑下来，中途**正好扫过大标题那条残留** —— 这就是「闪」。

### 改法：阈值改成立即（同帧切换）+ 把动画整条删掉

```
旧：const on = Math.max(60, hero.offsetHeight - 62 + 12);   // 200，早于大图离开
新：const on = Math.max(60, hero.offsetHeight);             // 250，大图整块离开
```

**为什么改成 `heroH` 就是无缝的**（不是随便挑的数）：
小条的静态位在内容偏移 **250**（紧随 250px 高的大图之后），所以
「大图整块滚出视口」与「小条正好贴顶」是**同一个瞬间** —— `visibility` 一切即无缝，
**不需要任何过渡**。

CSS 同时删干净：

```css
/* 旧 */ transform:translateY(-102%); transition:transform .26s …,opacity .2s ease,visibility 0s .26s;
/* 新 */ opacity:0;visibility:hidden;pointer-events:none      ← 激活时只切 visibility
```

### ★ `>` 与 `>=` 的**一像素级**差异（踩过）

阈值恰好等于大图高，所以边界那一帧很关键。实测：

| 写法 | 首次激活位置 |
|---|---|
| `y > on` | **y=260**（晚 10px，多出 1~10px 空档） |
| `y >= on` | **y=250** ✅ |

⇒ 必须 `y >= on`。这条已固化成断言（`test-v1031.js` 里正反各一条）。

---

## 二、② 置顶图「不知道哪里的图」—— 实测是 **128×128 的方图**

先用 `naturalWidth` 亲自量，不靠看：

| 字段 | 实测值 | 尺寸 |
|---|---|---|
| `fb.cover`（**列表在用的那张**） | `shared.cdn.queniuqe.com/store_item_assets/steam/apps/1369630/header.jpg` | **460×215** |
| `d.cover`（详情页原来取的） | `www.xdgame.com/uploads/allimg/….png` | **128×128** |

128×128 的方图被 `object-fit:cover` 铺进 **720×250** 的顶图 ⇒ 放大 **5.6 倍** + 裁掉大半，
糊成一片色块、标题被裁掉 —— 用户截图里那块「不知道哪里的图」就是它。

### 改法：优先级翻过来

```js
/* 旧 */ const cover = d.cover || (fb && fb.cover) || '';
/* 新 */ const cover = fb.cover || d.cover || '';     // 库内封面优先 = 用户在列表看到的那张
```

- `d.cover` 仍作**兜底**：从源站链接直开详情（没有 fb）时只有它可用；
- 顺带修掉一处「图会变」：搜索结果的**预览抽屉**本来就用 `fb.cover`，详情加载完却换成
  `d.cover` —— 同一张封面在两三秒内自己换一次。现在两边同源，不再跳。

---

## 三、③ 修改器 + 云存档「固定显示五个」

用户截图样本精确锁定为 **xd-692 史莱姆牧场**（接口实测：修改器 1 个来源 / 云存档 6 条记录，
与用户截图完全一致）。改造前**两卡差 176px**：

| | v10.30（改造前） | v10.31（改造后） |
|---|---|---|
| 修改器卡 | 332×**161** | 332×**330.4** |
| 云存档卡 | 332×**337** | 332×**330.4** |
| 差值 | **176px** | **0**（≤1px） |

### 改法：**两个机制必须同时上**，缺一个都退化

```css
.d-pair{…;align-items:stretch}                          /* ① 两卡等高 */
.d-rows{min-height:calc(var(--d-row-h) * var(--d-rows))} /* ② 单行内容也预留 5 行 */
```

| 只做 stretch | 只做 min-height | 两者都做 |
|---|---|---|
| 卡片等高，但内容仍 1 行 ⇒ **一大片空白** | 卡片**不等高** | 一样大、都占五行 ✅ |

配套：

- `--d-row-h:36.7px`（实测单行节距 = 32.7px 行高 + 4px 间距）、`--d-rows:5` ⇒ 底高 **183.5px**；
- `.d-pair .d-hint2{margin-top:auto}` —— 预留出来的空档若不处理，提示会被顶到**半空**，
  看着像两块内容；
- ⚠️ v10.29 选的 `align-items:start`（「两块各自按内容高度收」）**就此作废**，
  并在 CSS 注释里写明理由，避免下次又改回去。

---

## 四、④ 同分类更多：卡片统一大小 + 按比例缩放 + 8 个

### 4.1 `object-fit:contain` 是**唯一**能同时满足两条要求的写法

本族封面是**两种原生比例混排**：

| 来源 | 原生尺寸 | 比例 |
|---|---|---|
| 机地封面 | 140×140 | 1.00（方图） |
| Steam header | 460×215 | 2.14 |

| 取值 | 方图（140×140） | 宽图（460×215） |
|---|---|---|
| `cover` | **裁掉上下**（实测「黄金之心」标题整条不见了） | 正常 |
| `fill` | **被横向拉扁** | 被纵向拉扁 |
| `contain` ✅ | 完整 + 两侧留白 | 完整 + 两侧留白 |

⇒ `cover → contain`。实测图片盒 `88 x 49.5`、比例 **1.778**（16:9）、fit=**contain**。
机地 140×140 方图不再被裁（「黄金之心」标题回来了）。

### 4.2 「显示 8 个」——`limit` 必须**比展示数大**

`limit` 是**后端打分返回的条数**，前端还要 ① 滤掉没有详情页 url 的、② 滤掉与本条同 url 的自身回环。
只取 8 会让过滤后的实际张数在 **5~7** 之间跳 —— 先前那个「6」就是这么来的。

```js
const qs = 'limit=12'                                  // 取数 12（> 展示数）
const picks = (j.items || []).filter(…).slice(0, REL_SHOW);   // 展示 REL_SHOW = 8
const REL_SHOW = 8;                                    // ★ 只写一处
```

### 4.3 一处**自我发现**的设计瑕疵：`--cd-cols:8` 是假接口

初版把 8 写成了 CSS 变量 `--cd-cols:8`。但它**只定义、无人引用** —— 正是 v10.30 的
`:root` 断言在防的「只定义不用 = 规范没生效」。而且它决定的是**取几条数据、渲染几个 DOM**，
本质是 JS 的职责。⇒ 删掉该变量，改用 JS 常量 `REL_SHOW`。

---

## 五、验证（全部实测，不是形容词）

### 第一层 · 静态防线（`node tools/run-all.js`）

| | 结果 |
|---|---|
| 套件数 | **31 套**（新增 `test-v1031.js`，已登记 `SUITES`） |
| 断言数 | **2120 / 2120** |
| 前置闸 | **2 / 2**（`check-inline-syntax` + `check-card-rules`） |

受影响套件同步（**改判据 + 补反向断言**，不是删旧断言凑绿）：

| 套件 | 变化 |
|---|---|
| `test-v1028-detail.js` | 127 → **130/130** |
| `test-v1029-detail.js` | 117 → **121/121** |
| `test-v1030-cards.js` | 85 → **90/90** |
| `test-v1031.js` | 新增 **65/65** |

### 第二层 · 浏览器实拍（`node tools/preview-v1031.js`）

| 量 | xd-692 史莱姆牧场 | xd-38 终结者莉莉 |
|---|---|---|
| 阈值首次激活 | **y=250** | **y=250** |
| 「大标题与小条同屏」次数 | **0 / 41 点** | **0 / 41 点** |
| 临界点来回翻转 | **1 次**（必需的单次激活，非抖动） | **1 次** |
| 阈值下方(~200)来回 | **0 次** | **0 次** |
| `mini` 的 transition / transform | **0s** / **none** | **0s** / **none** |
| 顶图 src | `…/steam/apps/433340/header.jpg` | `…/steam/apps/1369630/header.jpg` |
| 顶图 natural | **460×215** | **460×215** |
| 大图与小条缩略图同图 | **true** | **true** |
| 两卡尺寸 | 332×**330.4** / 332×**330.4** | 332×**319.4** / 332×**319.4** |
| 行盒 `min-height` | **183.5px** / 183.5px | **183.5px** / 183.5px |
| 同分类张数 | **8** | **8** |
| 卡宽 / 卡高唯一值 | **[106]** / **[121.7]** | **[106]** / **[121.7]** |
| 图盒 / 比例 / fit | `88x49.5` / **1.778** / **contain** | `88x49.5` / **1.778** / **contain** |
| 页面级 JS 异常 | 0 | 0 |

**合计 51/51 通过。**

### 反证（「打坏必须变红」）—— 两层各做一遍

**静态层** `tools/_counterproof-v1031.js`：搬一份 `public/` + 套件到 `_cf131/` 打副本，
单点变异后跑，判据 = 退出码非零 **且** 指定断言名出现在 FAIL 行。**19 / 19 合规**。

**实拍层** `tools/_counterproof-v1031-live.js`：临时改写 `public/index.html`，
`try/finally` 还原 + md5 核对。**7 / 7 合规**。

实拍层反证**当场抓出两个真问题**（这就是它存在的意义）：

1. **`expect` 关键词写错**：约定「cover 退回 d.cover 优先」时，能抓住它的是
   **像素尺寸**那条（`natural=128x128`），而**不是**「大图与小条同图」那条 ——
   因为两者**共用同一个 `cover` 变量**，优先级翻回去它们**依然相等**（一起变糊）。
2. **`④ slice(0,8)` 打坏了却照样全绿** —— 真相是：本样本下 `limit=12` 过滤后仍 ≥8，
   所以 `slice(0,8)` 与 `slice(0,REL_SHOW)` **结果完全一样（都是 8 张）**。
   真正决定张数的是 **`limit` 比展示数大**，而 `REL_SHOW` 只是「8 只写一处」的可维护性。
   ⇒ 实拍层改用「写死 6」「limit=6」两个变异验**可观测张数**；`REL_SHOW` 本身归静态层守。

---

## 六、发布状态（判新版**必须比 md5**）

拉线上 `index.html` 与本地逐条比对 **CSS 规则体**（不是看 HTTP 200 —— 本项目三次踩到
「三个域名全 200、两个旧一个新」）：

| 判据 | 线上 | 本地 v10.31 |
|---|---|---|
| `.d-mini` 规则体 | 含 `transform:translateY(-102%)` + `transition:…` | **无** transform / transition |
| `.d-pair` 规则体 | `align-items:start` | **`align-items:stretch`** |
| `.d-rows` 规则体 | **不存在** | `min-height:calc(var(--d-row-h) * var(--d-rows))` |
| `rel-it img` | `object-fit:cover` | **`object-fit:contain`** |
| hero 阈值 | `Math.max(60, hero.offsetHeight - 62 + 12)` | **`Math.max(60, hero.offsetHeight)`** |
| `cover` 优先级 | `d.cover` 优先 | **`fb.cover` 优先** |
| `REL_SHOW` | **不存在** | `const REL_SHOW = 8;` |
| `related` 取数 | `limit=8` | **`limit=12`** |
| md5 | `5b24a89b8e95a59c7e47a0aa0a1fad31` | `2cc4756045cce4459a6d1221662cde23` |

⇒ **发布前**线上 = v10.30（8 条判据全部指向旧实现），v10.31 本地未发布。

### 发布结果（2026-09-21，用户授权后覆盖同一 app）

| 项 | 值 |
|---|---|
| 操作 | `workbuddy_sites_deploy` · action=deploy · **复用既有 appId**（链接不变） |
| appId / sandbox | `wbapp_047aLTlMY7YdDmtVpp3BYa` / `445143a7b3004d749eab6be0fe8836e5` |
| 分享链接 | https://gamehub-agg-v4.app.workbuddy.host/ |
| ★ 线上 md5 | **`2cc4756045cce4459a6d1221662cde23`** = 本地 md5（连拉 3 次一致，376,814 字节） |
| 线上验收 | `tools/verify-online.js` **27 / 27 通过**（含「线上首页含全部特征串（v10.14~v10.31）」26 项齐） |

**`MUST` 加串（实测区分度，基准 = `git show HEAD~1:public/index.html` = v10.30，其 md5 `5b24a89b8e…`）**：

| 候选串 | v10.30 | v10.31 | 采信 |
|---|---|---|---|
| `--d-row-h` | 0 | 4 | **✅ 已加** |
| `REL_SHOW` | 0 | 4 | **✅ 已加** |
| `Math.max(60, hero.offsetHeight)` | 0 | 1 | **✅ 已加** |
| `object-fit:contain` | **1** | 3 | ❌ 旧版已有（本文件判定是 `includes`，只看有无） |
| `align-items:stretch` | **3** | 4 | ❌ 旧版已有 |
| `limit=12` | 1 | 2 | ❌ 旧版已有 |

★ 教训延续 v10.30 那条：**基准要取「上一版」**。v10.30 当时误取 `HEAD~1`（= 自己）；
本次 HEAD 已是 v10.31 提交 ⇒ `HEAD~1` = v10.30 才是正确基准，且导出后先核 md5 再采信计数。

---

## 七、新增 / 改动的文件

| 文件 | 动作 |
|---|---|
| `public/index.html` | 四条改动的全部实现（`:root` / `.d-mini` / `.d-pair` / `.d-rows` / `.rel-row .rel-it img` / `cover` / `REL_SHOW` / `dHeroSpy` / 模板） |
| `public/emulator.html` · `public/unpack.html` | 派生页重建（改主源必须重建，否则**悄悄漂移且不报错**） |
| `tools/test-v1031.js` | **新增**（65 条，含 9 条反向断言） |
| `tools/preview-v1031.js` | **新增**（实拍 51 条，把 `_verify-v1031.js` 的实测数字固化） |
| `tools/_counterproof-v1031.js` | **新增**（静态层反证 19 条变异） |
| `tools/_counterproof-v1031-live.js` | **新增**（实拍层反证 7 条变异 + 主源还原核验） |
| `tools/test-v1028-detail.js` · `test-v1029-detail.js` · `test-v1030-cards.js` | 判据同步 + 反向断言 |
| `tools/run-all.js` | `SUITES` 登记 `test-v1031.js`（30 → 31） |
| `tools/_probe-v1031*.js` · `_verify-v1031.js` · `_shot-*.js` · `_live-ver.js` | 侦察 / 实拍 / 版本判定探针（`_*` 不入库） |
