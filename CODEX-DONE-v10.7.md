# CODEX-DONE v10.7 — 搜索弹窗优化（分组按相关性排序 + 行密度）+ 详情抽屉加宽

> 日期：2026-09-14 ｜ 前置：v10.6（云存档跨源名称匹配）
> 用户原始反馈：「优化下搜索弹窗，以及可以拓宽点详情页吗」
> 决策记录：用户选定 **详情抽屉 680px 基准 / 宽屏 50vw**；搜索弹窗**只做两项**
> ——「收紧行高 + 去掉隐形占位」「分组按相关性排序」。**未选**「空分组不再占位」「弹窗同步加宽」，
> 故这两项**未动**（见第六节「按你的选择没做的事」）。

---

## 一、改了什么（5 处）

| # | 位置 | 改动 | 一句话原因 |
|---|---|---|---|
| 1 | `.drawer` | `width:min(520px,100vw)` → `min(clamp(680px,50vw,1040px),100vw)` | 520px 在 1440 视口只占 36%，正文被拉到 2465px 高 |
| 2 | `.kv` / `.shots` | 2 列 → 3 列（移动端分别回退 1 / 2 列） | 抽屉变宽后用上多出来的横向空间，纵向省 1/3 |
| 3 | 详情模板 | `#relSlot`（同分类更多）从**第 2 位**挪到**最后** | 推荐位不该抢在游戏本体信息前面 |
| 4 | `.sm-row` 系列 | 行距 1.6 → 1.3/1.35；缩略图 86×50 → 76×44；内边距 9→7；`.go2` 改绝对定位 | 行高 84→68，并消掉每行 85px 的隐形占位 |
| 5 | `paintSearchResult` | 分组由**写死顺序**改为**按贴合度动态排序** + 端游库本体提权 | 搜游戏本名时端游库被压在第 4 组，要滚动才看得到 |

---

## 二、① 详情抽屉加宽：为什么是 `clamp(680px,50vw,1040px)`

```
1440 视口：50vw = 720  → clamp(680,720,1040) = 720  （50%，正是你要的）
1100 视口：50vw = 550  → clamp 取下限 680           （比原来 520 仍宽 31%）
1920 视口：50vw = 960  → 960                        （50%）
2560 超宽：50vw = 1280 → 夹到上限 1040              （防止变成整屏）
≤760px  ：由 `.drawer{width:100vw}` 移动端分支接管（未动）
```

最外层再套一层 `min(..., 100vw)`：**761–1360px 这段视口**下 680px 不会横向溢出（实测 820px 视口 `scrollWidth 820 ≤ 820`）。

配套：

```css
.kv{grid-template-columns:repeat(3,1fr)}      /* 单列 235px，数值不被压窄 */
.shots{grid-template-columns:repeat(3,1fr)}   /* 预览 3 列 */
@media(max-width:760px){
  .kv{grid-template-columns:1fr}              /* 移动端仍单列 */
  .shots{grid-template-columns:repeat(2,1fr)} /* 移动端回退两列（保留可读性） */
}
```

**实测**：抽屉 520 → **720px**；正文 `scrollHeight` 2465 → **2327px（-6%）**；KV 2 → **3 列**；预览 2 → **3 列**。

---

## 三、② 「同分类更多」挪到末尾

详情模板原来的 DOM 顺序：

```
d-hint → bhSlot → trBlock → svBlock → crossSrc → ★relSlot → 评分 → KV → 介绍 → 版本 → 配置 → 预览
```

`#relSlot` 由 `loadRelated()` 异步填充，位置在**第 6 位** —— 打开抽屉**第一眼看到的是别人的推荐**，
自己的评分 / 参数 / 介绍反而要往下翻。现改为：

```
d-hint → bhSlot → trBlock → svBlock → crossSrc → 评分 → KV → 介绍 → 版本 → 配置 → 预览 → ★relSlot
```

> ⚠️ **这一项你没明确点选**，是我在诊断里标为「顺序有问题」后顺手修的。
> 纯 DOM 换位、`loadRelated()` 逻辑一行没改，**想要原样一句话退回**。

---

## 四、③ 搜索弹窗：行密度 + 隐形占位

### 根因：撑高行的不是缩略图，是 `line-height:1.6`

最初以为行高 70px 是「86×50 缩略图 + 9px 内边距」造成的，实测后推翻：

```
body{line-height:1.6}  ← 全球通用行距，在 11–14px 的小字号上留白过大
  标题行 14px × 1.6 = 22.4px
  英文名 11px × 1.6 = 17.6px
  标签行 11px × 1.6 = 19.6px（.pill 未设行距，继承 body）
  ─────────────────────────────
  三行合计 64px + 间距 4px + 内边距 18px + 边框 2px = ★88px
```

**先把行距收紧，再把缩略图和内边距改小**，两件事都做才降得下来：

```css
.sm-row{padding:7px 10px}          /* 原 9px 11px */
.sm-row img,.sm-row .ph2{width:76px;height:44px}   /* 原 86×50 */
.sm-row .bd{gap:2px}               /* 原 3px */
.sm-row .t{line-height:1.3}        /* 22.4 → 18.2 */
.sm-row .en{line-height:1.3}       /* 17.6 → 14.3 */
.sm-row .m{line-height:1.35}       /* .pill 随之 19.6 → 17 */
.sm-row .pth{line-height:1.3}
```

### 隐形占位：`opacity:0` 不等于「不占地方」

`<span class="go2">查看详情 →</span>` 原先是**流内元素**，虽然 `opacity:0` 看不见，
却实打实占着 ~85px —— 每行右侧的来源徽标都被从边缘推开 85px，**每行都白空一块**。

```css
.sm-row{position:relative}
.sm-row .go2{position:absolute;right:10px;top:50%;transform:translateY(-50%);
  opacity:0;background:var(--c-primary-soft);border-radius:8px;padding:3px 9px;
  pointer-events:none}
.sm-row:hover .go2,.sm-row.on .go2{opacity:1}
```

三个设计点：

1. **底色与行 hover 底色一致** → 淡入时正好盖住来源徽标（那一瞬间用户要看的是「点了能去哪」，不是「数据打哪来」）
2. **绝对定位** → 行宽逐像素不变，**不会像 v10.5 那样诱发回流**（实测 hover 前后行宽 740.88 → 740.88，差 0.00px）
3. **`pointer-events:none`** → 浮出的标签永远不吞掉整行的点击

### 实测（A/B，同一份数据）

| 指标 | 改前 | 改后 |
|---|---|---|
| 行高分布（搜「赛博朋克」18 行） | `{69:2, 84:13, 103:3}` | `{59:2, 68:13, 83:3}` |
| 常见行高 | 84px | **68px（-19%）** |
| 来源徽标距行右边缘 | **79px** | **11px** |
| 首屏完整可见行数 | 6 行 | **9 行（+50%）** |
| 结果区滚动总高 | 1742px | 1449px（-17%） |

---

## 五、④ 分组按相关性排序

### 原问题

分组顺序**代码里写死**为 `手游中心 → 修改器 → 云存档 → 端游库`，注释解释是「手机专区语境下手游放最前，
端游库作兜底」。但四个库搜的是同一批游戏，搜「剑星」时**用户真正要找的那款游戏在最后一组**。

### ★ 第一版实现直接失败（值得记）

第一版只按「标题 ↔ 关键词」贴合度打分：

```
3 = 归一化后完全同名   2 = 以关键词开头   1 = 包含关键词   0 = 标题里没有
```

真机一跑就发现**几乎总是四组全平局** —— 因为四个库收录的是**同一款游戏**，标题当然一样：

| 关键词 | 四组得分 | 结果 |
|---|---|---|
| 艾尔登法环 | `{mobile:3, trainer:3, save:3, pc:3}` | 全平局 → 退回原顺序，端游库照样垫底 |
| 只狼 | `{mobile:2, trainer:2, save:2, pc:2}` | 同上 |
| GTA | `{mobile:2, trainer:2, save:2, pc:2}` | 同上 |
| 赛博朋克 | `{mobile:2, trainer:2, save:2, pc:2}` | 同上 |

**「只按贴合度排」对游戏搜索这个场景是无效的。**

### 定稿规则：贴合度 + 端游库本体提权

```
端游库得分 = 贴合度 + (贴合度 ≥ 2 ? 1 : 0)
```

理由：端游库代表的是**游戏本体**这一实体，也是四组里**唯一能点进完整详情页**的；
其余三组都是「关于这款游戏」的附加信息（能不能手机玩 / 有没有修改器 / 存档放哪）。
提权只在端游库**确有本名命中（精确或近似）**时生效，其余情况**一律不动默认顺序**。

实现要点（`paintSearchResult`）：

```js
const rn = (s) => String(s == null ? '' : s).toLowerCase().replace(/[\s\-_:：·・/\\|'"“”‘’()（）\[\]]/g, '');
const qEff = (j.aliasNote && j.aliasNote.to) || q;   // ★ 别名命中时用别名目标名打分
const qn = rn(qEff);
const fit = (...nameLists) => { /* 逐名取最高分 3/2/1/0 */ };
const pcFit = fit(pcHit.map((x) => x.title));
const groups = [ {i:0,...,score:fit(...)}, ..., {i:3,...,score:pcFit + (pcFit >= 2 ? 1 : 0)} ];
groups.sort((a, b) => b.score - a.score || a.i - b.i);   // Array#sort 稳定 → 同分保持原顺序
```

- **`qEff` 用别名解析后的词**：用户搜别名时，标题里出现的是别名目标名，拿原词打分会四组全 0 分，
  排序静默退化成旧顺序 —— 这是个不写测试根本发现不了的坑。
- **`a.i - b.i` 兜底**：全平局时行为与旧版**完全一致**（只做动态提权，不改默认顺序）。

### 实测（正例 / 负例都覆盖）

| 关键词 | 改前顺序 | 改后顺序 |
|---|---|---|
| 剑星 | 📱 → 🛠 → 💾 → 🖥️ | **🖥️** → 🛠 → 📱 → 💾 |
| 艾尔登法环 | 📱 → 🛠 → 💾 → 🖥️ | **🖥️** → 📱 → 🛠 → 💾 |
| 只狼 / GTA / 赛博朋克 / 星露谷 / 怪物猎人 | 📱 → 🛠 → 💾 → 🖥️ | **🖥️** → 📱 → 🛠 → 💾 |
| **以撒的结合 重生**（库内零命中） | 📱 → 🛠 → 💾 → 🖥️ | 📱 → 🛠 → 💾 → 🖥️（**保持不变** ✅） |
| **蔚蓝Celeste**（库内零命中） | 📱 → 🛠 → 💾 → 🖥️ | 📱 → 🛠 → 💾 → 🖥️（**保持不变** ✅） |

负例词的找法：从 `/api/mobilehub/list` 取手游名逐个查 `/api/search/all`，挑出 `pc.count === 0`
且其它库有命中的（`以撒的结合 重生` = mob 1 / tr 1 / sv 2 / pc 0）。

---

## 六、按你的选择**没做**的事

| 没做 | 原因 | 想要的话代价 |
|---|---|---|
| 空分组不再占位 | 你没点选 | 一次性小改：`groups` 里 `rows` 为空时整组不 push |
| 搜索弹窗加宽 | 你没点选 | 780 → 880px |
| 行高再往 58px 压 | **做不到**，见下 | 需要动信息层级 |
| 详情抽屉 KV 4 列 | 680px 下只会把单列压到 ~155px | 不建议 |

### 关于「首屏 12 行」的如实说明

我在选项描述里写的是「首屏可见结果从 ~9 条提到 ~12 条」，**实测只到 9 行**（原 6 行）。
原因是行里最多有 4 行文字（标题 / 英文名 / 标签 / 云存档路径），收紧行距后 3 行文字行的物理下限是 ~68px，
4 行的云存档行 ~83px。要真到 12 行只能**砍掉一行信息**，两条备选路径：

1. 把英文名并到标题行（`剑星 · Stellar Blade`）→ 典型行降到 ~53px → 约 12 行；
   代价是英文名从「独立弱化行」变成标题的附属，视觉层级变化明显。
2. 弹窗纵向预算调大（`top:9vh;max-height:78vh` → `top:8vh;max-height:84vh`）→ 多约 60px，约 +1 行。
   纯占位调整、零风险（1440×1000 下弹窗底部现在离屏幕底还有 ~130px 空着）。

二选一（或都做）说一声即可。

---

## 七、新增第七道防线 `tools/test-search-ui.js`（44 条）

| 组 | 条数 | 覆盖 |
|---|---|---|
| 分组排序·正例 | 18 | 6 个词 ×（端游库第一 / 四组齐全 / 无重复分组） |
| 分组排序·负例 | 4 | 2 个端游库零命中的词 ×（手游中心第一 / 完整顺序 = 默认） |
| 行密度 | 6 | 常见行高 ≤70、最高 ≤85、首屏可见 ≥8 行、标题/英文名行距、标签胶囊高 |
| 隐形占位 | 6 | `position:absolute`、`pointer-events:none`、徽标留白 ≤14px、hover 行宽/文字区不变、浮出完整盖住徽标 |
| 抽屉宽度 | 4 | 1100 / 1440 / 1920 / 375 四档视口实测 |
| 抽屉栅格 | 5 | 桌面 KV 3 列 + 单列 ≥190px + 预览 3 列；移动端 KV 1 列 + 预览 2 列 |
| 溢出 | 1 | 820px 视口无横向滚动 |

**为什么必须单独存在**：`分组排序`是**动态**逻辑，静态体检只能证明源码里有排序语句，
证明不了「搜游戏本名时端游库真的排第一」，更证明不了「零命中时没乱提权」；
`行高/留白`是布局量，jsdom 算不出来（同 `test-filter-layout.js` 踩过的坑）。

`tools/test-emulator-structure.js` 同步 +24 条静态断言（104 → **128**），
含**派生页一致性**：`emulator.html` 必须同步到同样的 `clamp` 宽度 / `.go2` 绝对定位 / `pcFit` 提权 / `relSlot` 位置 / 行距。

---

## 八、验收基线（全跑通过）

| 套件 | 结果 |
|---|---|
| `test-emulator-structure` | **128 / 128**（104 → 128） |
| `test-emulator-page`（jsdom 行为） | 118 / 118 |
| `test-emuhub` | 118 / 118 |
| `test-filter-layout`（浏览器布局） | 77 / 77 |
| `test-alias-guard` | 7 / 7 |
| `test-saves-match` | 44 / 44 |
| **`test-search-ui`（新增）** | **44 / 44** |

页面指纹（v10.7）：

```
public/index.html     32d8b43223e0069c0eeb7085ca0a8c88   162,312 B
public/emulator.html  821db2b467360fc290a43bc73c1ce87c   234,668 B
```

验收图：`_preview/v107-search-before.png` / `v107-search-after.png` / `v107-hover-after.png`
/ `v107-detail-before.png` / `v107-detail-after.png` / `v107-detail-after-scrolled.png`

回滚：`_bak/index.html.bak-20260914-180422`、`_bak/emulator.html.bak-20260914-180422`

---

## 九、给 Codex 的建议（未实施）

1. **`build-emulator-page.js` 加「派生页一致性体检」**：现在靠测试断言比对，属于事后补救。
   更彻底的做法是生成时顺带打印共享资产的指纹，测试只比对指纹。
2. **`.sm-row` 的四种行模板（`smRow` / `smMobileRow` / `smTrainerRow` / `smSaveRow`）抽成同一个 `smRowShell()`**：
   本轮的 `go2` 位置、行距、缩略图尺寸都要在 4 个模板里同步改，漏一个就长短不一。
   结构体检目前没有断言「四个模板的 go2 文案规则一致」。
3. **贴合度打分抽成 `tools/lib-name-fit.js`**：与 `cross-source-name-matching` skill 里的
   归一化/匹配口径已经开始漂移（skill 用「词边界包含」，这里是「连续子串 + 贴合度」），
   建议统一到一处并让 skill 引用它。
4. **`paintSearchResult` 里 `groups` 数组可读性**：四组对象的字段已经 9 个，
   再加筛选维度（如按库类型折叠）建议改成工厂函数 `mkGroup({...})`。
