# CODEX-DONE v10.38 —— 每日维护自动化「降本改造」：10 步 → 1 条命令

> 日期：2026-09-23 ｜ 状态：**本地完成，未发布**（本轮不动 `server.js` 与 `public/*.html`）
> 主题：把「模型亲自跑 10 条命令、逐步读输出」的每日自动化，改成「跑一条命令、只读一份摘要」。

---

## 一、为什么要做（问题不是「慢」，是「每一步的原始输出都要进上下文」）

| # | 症状 | 实际代价 |
|---|---|---|
| 1 | prompt 让模型**亲自跑 10 条命令**，每步的 stdout（含大段 JSON）都进上下文 | 单次开销大；且模型有多轮工具往返 |
| 2 | prompt 里**写死了不存在的 node 路径** `…/22.22.2-2/node.exe` | **每天先试错一轮再自行纠正**，纯浪费（执行记录里连着记了 5 天） |
| 3 | 10 个步骤各写各的成败判断，无统一语义 | 容易漏判；失败时模型要重新调接口复核，又烧一轮 |

★ 这三条都不是「功能坏了」，而是**成本结构坏了** —— 功能一直正常，只是每天多烧一遍。

---

## 二、做了什么（四件事）

### 1. 新建 `tools/daily-sync.js` —— 8 步收进一次进程

原来 10 步（体检 / XD 增量 / BannerHub / 实测配置库 / 手游中心 / 修改器 / 云存档 / 机地同步 / 轮询 / 汇总）
全部内置，**只回吐一份 ≤8KB 的摘要**（实测约 1.1KB）。

关键设计：
- **node 路径不写死**：跑子进程用 `process.execPath`（谁启动我，就用谁跑）。
- **python 路径不写死用户名**：按 `os.homedir()` 推导 + PATH 兜底。
- **摘要读产物 json 的 `stats`**，不解析 stdout 文本（四个产物都带结构化 stats，稳得多）。
- **统一状态码**：末行 `DAILY_OK` / `DAILY_PARTIAL` / `DAILY_FAIL`；退出码 0 / 2 / 3。
- **阻塞 ≠ 失败**：`E:\新建文件夹\基础测试数据.xlsx` 缺失是**外部原因**，
  单独标 `NEED_XLSX`（`blocked`），不会把「今天挂了」误报出来。
- 每步独立 try/catch + 超时（xd 10 分 / bh 12 分 / jidi 5 分 / 脚本 3 分 / 联网 7 分）。

附加开关：`--dry`（只体检不写）· `--json`（给程序读）· `--skip=a,b` · `--timeout-xx=NNN`。

### 2. 新建 `tools/test-daily-sync.js`（**101 条**）并登记 `SUITES`

守六件事：选择器纯函数 / **`--dry` 的行为级保证** / 摘要体积与行数 / 数据口径 / 实现约束 / 参数解析。

### 3. 新建 `tools/_counterproof-daily-sync.js`（**16 条变异，16/16 合规**）

★ 反证**当场抓出了我自己套件的一个假断言**（见第四节）。

### 4. 改造 automation prompt（**3.2KB → 1.5KB**）

`GameHub 内容库每日自动同步`（`f2faa379-…`，每日 09:30）：
- 10 步详细指令 → **「只跑 `node tools/daily-sync.js`」**
- 去掉写死的 node 版本路径（实测 `node` 已在 PATH 里）
- 明确「不要读回文件、不要再调接口复核、不要重跑」
- 记录格式精简，并**显式删掉**那条已解决的待办（见第三节 3）

---

## 三、过程中发现并修掉的四个真问题

### 1. ★ `--dry` 违背承诺：嘴上说不写，实际把三个任务都真跑了

第一版把 `--dry` 判断写在**每个步骤内部**，结果 **XD 增量 / BannerHub 刷新 / 机地同步** 三步漏了。
第一次 `--dry` 跑完，`lastIncrAt` 显示成「刚刚」——才发现它们真的执行了，**而且没有任何报错**。

**修法**：把决策抽成纯函数 `planStep(def, opts)`，并在 `STEP_DEFS` 上声明 `write: true`，
由主流程**单点拦截**。内层那四处重复判断全部删除（同一件事只留一个真源）。

**验证**：`--dry` 跑前后比对 `lastIncrAt` / `lastSyncAt` / `bh.startedAt` —— **完全未变**。

### 2. 字段口径错：摘要显示 `xdgamer ? + jidi ?`

分源条数在 `bySource` 下（`bySource.xdgamer` / `bySource.jidi`），**顶层没有这两个键**。
第一版写 `lib.xdgamer` ⇒ 静默变成问号（不报错、不崩，就是数字没了）。

**修法**：抽 `sourceCounts(lib)` 纯函数作为唯一取值口。

### 3. `idxState.jidi.total` 是**全库总量**，不是 jidi 源条数

机地上报的 `total` 来自 `upsert()` 的返回值（= 全库总量）⇒ 直接展示会得到
「**jidi 19051**」这种误导数字（真实是 3625）。**修法**：改读 `bySource.jidi`。

### 4. ★ 反证抓出的假断言：只查「源码里出现过 `bySource`」

第一轮反证 **15/16**，⑩ 未被抓住。根因：我的断言写成
`ok(/bySource/.test(SRC))`，但 `stepJidi` 里也有 `bySource`，
⇒ **把汇总段改成读错层级，断言照样全绿**。

**修法**（三管齐下）：
- 抽出 `sourceCounts()` 纯函数；
- 用**真实形状**的输入做行为断言 —— 传 `{total, xdgamer, jidi}`（只有顶层键）必须返回 `null`；
- 静态检查**收窄**到 `lib.bySource`（带 `lib.` 前缀）。

修后 ⑩ 被 **4 条断言**同时抓住，反证 **16/16**。

★ **这条不做反证就永远发现不了** —— 它不会报错、不会崩，只是「守了个寂寞」。

---

## 四、实测对照

### 4.1 改造前后

| 判据 | 改造前 | 改造后 |
|---|---|---|
| 模型需执行的命令数 | **10** | **1** |
| 进上下文的原始输出 | 每步 stdout 全部 | 一份摘要（实测 **1.1 KB**） |
| prompt 里的 node 路径 | 写死 `22.22.2-2`（**实测不存在**） | 不写死（`node` 已在 PATH） |
| prompt 体积 | 约 3.2 KB | **约 1.5 KB** |
| 单轮耗时（实测） | 约 3 分钟（模型多轮往返） | **28 秒**（一次进程） |
| 失败语义 | 10 步各自判断 | 统一 `DAILY_OK / PARTIAL / FAIL` |

### 4.2 实跑结果（`node tools/daily-sync.js`，28 秒）

与**同一天早上 09:35 的人工流程记录逐项吻合**：

| 步骤 | 结果 |
|---|---|
| 服务体检 | ✅ 已在运行 |
| XD 增量 | ✅ 库 19051 → 19051 |
| BannerHub 刷新 | ✅ 3s · games 2676 / configs 14748 / phones 1526 / gpus 91 / 匹配 929 |
| 实测配置库 | ⛔ `NEED_XLSX`（xlsx 不存在，phonecfg 仍 09-15 版） |
| 手游中心重建 | ✅ 3220 条（社区独有 2198 / 实测独有 931 / 双料 91）· 匹配端游 1649 (51.2%) |
| 修改器 | ✅ 3619 条（CE 1850 / 风灵 1154 / 社区 699 / 小幸 59 / GCM 22）· 命中 2924 (80.8%) |
| 云存档 | ✅ 6604 款 / 15027 路径 / 1023 注册表 / 1132 手机能玩 · 在端游库 6323 |
| 机地同步 | ✅ got 34 / added 0 · jidi 源 3625 |
| 汇总 | 库总量 **19051 = xdgamer 15426 + jidi 3625** · 双料 13448 ｜ 退出码 **0** |

### 4.3 验证链

| 项 | 结果 |
|---|---|
| 静态防线 | **37 套 / 2,531 条 / 0 失败 / 异常退出无**（新增 1 套 +101 条；补丁再 +14 条） |
| `test-daily-sync.js` | **101 / 101** |
| 反证 | **16 / 16 合规**（含前置「未变异基线必须先全绿」检查） |
| `--dry` 零写副作用 | ✅ 跑前后 `lastIncrAt` / `lastSyncAt` / `bh.startedAt` **完全未变** |
| `test-report.js`（补丁后） | **111 / 111**（97 → +14） |
| 反证（补丁） | **8 / 8 抓住**（`tools/_counterproof-v1038b.js`，含字节级还原校验） |

---

## 五、遗留与未做（如实）

| # | 项 | 状态 |
|---|---|---|
| 1 | **`E:\新建文件夹\基础测试数据.xlsx` 缺失** | ⛔ 仍是**唯一实质阻塞**（实测不存在，已连续 8 日）。补回文件即自动恢复 |
| 2 | 线上未发布 | 本轮只改 `tools/**`（+ `data/**` 由每日任务改写），**线上数据层仍是旧的 `cached 713`** |
| 3 | 本次实跑产生的数据变动 | `data/mobilehub.json`、`trainers.json`、`saves.json`、`bannerhub*` 等 —— 属每日任务正常产物 |
| 4 | 「缺 DX ⇒ skip ⇒ 仍可判流畅」口径 | 仍未改（属口径决策，等授权） |
| 5 | `WORKFLOW.md` 整段重复 | 349 行起是前 348 行副本，仍未清（未获授权不擅自重构文档） |
| 6 | v10.36 遗留 C 类 | 手游中心 145 条 / 实测库 36 条 |

---

## 六、经验（已回写技能）

1. **`--dry` / 只读模式这类「承诺」必须用声明式单点拦截**，不要在每步内层手写 `if` ——
   手写必漏，且漏了不报错（本次漏了 3 步）。
2. **断言不能只查「符号出现过」**：同一符号常出现在多个函数里，
   ⇒ 一个函数改错、另一个函数把检查撑成假绿。**收窄到 `对象.字段` 全路径，或抽纯函数做行为断言**。
3. **变异必须可加载**：把 `if (x)` 改成 `if (x && false)`，而不是插入新声明（会 SyntaxError ⇒ 只能报「崩了」）。
4. **反证要先跑未变异基线**：基线不绿则反证无意义（「红了」可能只是副本环境问题）。

---

## 补（2026-09-23，同日）：第②项「✅ 已是最新」是**假绿** —— 加服务端判据

**发现方式**：v10.38 推送后按惯例出五项汇报，`report.js` 第②项打印
「✅ 与本地逐字节一致 = 已是最新」。与记忆里「线上仍是旧版」对不上 ⇒ 实测复核 ——
结论是**记忆对、判据错**。

**实测（同一时刻，两条判据并列）**

| 判据 | 本地 | 线上 LIVE | 结论 |
|---|---|---|---|
| `public/index.html` md5 | 9ae5854778 | 9ae5854778 | 一致 |
| `/api/spec/dict` md5 | a4d58fa917 | 7b1de02de2 | **不一致** |
| `dictInfo().archRule` | `{noTranslatorInfo:'unknown',explicitDisabled:'fail'}` | **`undefined`** | ⇒ 线上仍停在 v10.35 之前 |
| `/api/library` md5 | 4de44784d2 | 4de44784d2 | 一致（本版未动该接口） |

**根因**：`report.js` 只比 `index.html` 的 md5。而 v10.33~v10.38 六轮改动**全在 `data/**` 与
`tools/**`**，前端一字未动 ⇒ 单判据对这一整类改动**结构性失明**，且失明时不报错、不崩，
长得跟真绿一模一样。

**改法**（`tools/report.js`）

- 新增 `SERVER_FEATURES`：每条含 `srcRe`（对本地源码）+ `apiTest`（对线上接口 JSON）。
- 新增纯函数 `serverVersionLabel(probe, localSrc)` / `serverFpRow(probe)`；
  `probeServer(base)` 只读拉 `/api/spec/dict`。
- 判定改为**两条判据都过**才算「已是最新」；前端同版但服务端旧 ⇒
  「⚠️ 前端同版，但 服务端旧于本地（线上尚未发布 v10.35） ⇒ **不能判为已是最新**」。
- 两种「取不到」都**降级为 ⚠️**：接口拉不到、以及**本地源码里找不到该指纹**（判据失效）。
  ★ 后者是关键：指纹串一旦被改名，判据会**永远为真** —— 必须显式报出来，不许静默放行。
- 匹配 `srcRe` 前先 `stripComments`（`archRule` 在 `spec-match.js` 注释里也出现过；
  不清注释就是第二轮假绿）。
- 顺手加 `require.main === module` 守卫 + `module.exports` ⇒ 判据可**行为级**单测，
  不再只能「grep 符号出现过」。

**验证**：`test-report.js` 97 → **111/111**（+14，全部行为级：喂真形状输入、看返回值）；
反证 `_counterproof-v1038b.js` **8/8 抓住**（含 ①「线上缺指纹也算一致」、⑤「缺指纹仍渲染 ✓」、
⑦「硬编码 stub 冒充探测」、⑧「弱化文案」），基线未变异必须全绿，末位比对 md5 证明确实**字节级还原**。

**教训**（已回写 `PITFALLS.md` 第十四节）：判「线上是不是最新」这类结论时，
**一条判据只覆盖它自己看到的那一层**。凡「改了 A 层、却用 B 层判据验收」的结构，
都必然出现单判据假绿 —— 而它不会报错。
