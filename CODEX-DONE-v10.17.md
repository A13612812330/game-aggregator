# CODEX-DONE v10.17 —— 机型清单「列不全」根因修复 + 译名显示质量

> **用户原话**：
> 「还有什么任务没有解决
>   目前 `[截图：手机模拟器配置块]` 还是没有完全展示所有机型，
>   其次没有根据我提供的内容进行映射具体的手机」
>
> **一句话结论**：两条抱怨是两个**不同的**根因，都已定位并修掉：
> ① **「没有映射成具体手机」= 线上站点还停在 v10.14** ——
>    实测线上 `index.html` **没有** `bhHwSlot` / `toggleDevHardware` / `data-hw` / `hasDetailUrl`
>    （v10.15、v10.16 全都不在），而你截图里那句「…上游汇总了 6 款机型，共 25 套配置」
>    与 v10.14 的代码逐字一致。本地代码早就译好了，**只是没发布**。
> ② **「没有完全展示所有机型」= 真·代码缺陷** ——
>    机型清单读的是社区库**聚合摘要** `bannerhub.json` 的 `dv`，而那是**上游的 6 格摘要**
>    （全库 2,648 条游戏，`dv` 最大长度就是 **6**，`255` 款正好卡在 6 格）
>    → 详情页**结构性最多只能显示 6 台**。全量在逐条配置里。**已修：6 台 → 9 台。**

---

## 一、① 线上 vs 本地：实测对照

把线上页面拉下来逐特征比对（`tools/_cmp-online.js`，用完已删）：

| 特征 | 本地 | 线上 index | 线上 emu |
|---|---|---|---|
| 机型清单两列（v10.15 改版） | 有 | 有 | 有 |
| 跨源按钮闸门 `hasDetailUrl`（v10.15） | 有 | **无** | **无** |
| 本站实测记录槽位 `bhRecSlot`（v10.15） | 有 | **无** | **无** |
| 搜索弹窗按键条 `sm-foot`（v10.15） | 有 | **无** | **无** |
| 硬件参数槽位 `bhHwSlot`（v10.16） | 有 | **无** | **无** |
| 点击展开硬件 `toggleDevHardware`（v10.16） | 有 | **无** | **无** |
| 译名查询键 `data-hw`（v10.16） | 有 | **无** | **无** |

线上还留着 v10.14 的样式与文案（`d-devlist{display:flex;flex-wrap:wrap}` +
逐字一致的「…上游汇总了 6 款机型，共 25 套配置」）——与你截图的观感完全对应。
**结论：线上需要重新发布**（发布复用同一 sandbox，链接不变、内容被覆盖）。

---

## 二、② 机型清单为什么最多只能 6 台（根因链）

同一款游戏的机型，在两条链路里**条数与写法都不同**：

| 链路 | 文件 / 字段 | 终极漫画英雄vs卡普空3 | 说明 |
|---|---|---|---|
| 社区库**聚合摘要** | `bannerhub.json` → `games[].dv` | **6 台**：`MTN NX3`、`BRP NX3`… | ⚠️ **上游只给 6 格**；写法残缺（无品牌前缀、无连字符） |
| **逐条配置** | `bhparams.json` → `items[].device` | **7 台**：`HONOR MTN-NX3`、`samsung SM-A057M`… | 全量、写法规范 |
| 详情页机型清单（改前） | 只读 `hit.devices` ← `dv` | **6 台** | 结构性上限 |

**证据（可复现）**：全库 2,648 条游戏，`dv` 长度分布 `1:1811 / 2:311 / 6:255 / 3:144 / 4:80 / 5:47`，
**最大就是 6**；卡在 6 格（即被截断）的有 **255 款**。

### 修法

新增 `data/deviceset.js` 做「归一 + 合并」，`/api/mobilehub/match` 改为返回合并结果：

```
剥品牌前缀 + 去掉所有分隔符 → 归一键
  `HONOR MTN-NX3` → MTNNX3   ┐
  `MTN NX3`       → MTNNX3   ┘ 同一台 → 取「信息更全」的写法（保留 HONOR 与连字符）
```

- 品牌前缀表**只有一份**（复用 `data/devicemarket.js` 的 `BRAND_PREFIX`，不另写一套）
- 合并保序、去重；`mergeReport()` 如实报出「摘要几台 / 合并几台 / 新增哪几台」
- 前端 `more` 行改口径：**只在真被截断时**出现，且说「还有 N 台机型未展开」，
  不再说「上游共汇总 N 款机型」（那句话在 N 恰好等于已列出的台数时会被读成「还有更多没显示」——正是你的读法）

**效果（同一款游戏）**：

| | 改前 | 改后 |
|---|---|---|
| 机型台数 | 6 | **9** |
| 抬头计数 | 6 款机型 · 5 种 GPU | **9 款机型 · 5 种 GPU** |
| 主行 | `MTN NX3`（残缺代号） | **`Honor Magic8 Lite`** |
| 新增 | — | `samsung SM-A057M` → Samsung Galaxy A05s<br>`ITEL itel S666LN`<br>`motorola moto g(20)` |
| 底部提示 | 「…上游汇总了 6 款机型，共 25 套配置」 | **不出现**（没被截断就不该说） |

> ⚠️ **已知边界（刻意不合并）**：`devKey` 只剥**一层**品牌前缀，而 `moto` 既是品牌词又是型号词的一部分，
> 所以 `motorola moto g24`（键 `MOTOG24`）与 `moto g24`（键 `G24`）**不会**合并。
> 强行合并要引入「键集合求交集」，短键（`G24`）跨品牌误合并的风险更大；
> 且本项目两条链路对这台机器写法一致，**无实际影响**。已在 `deviceset.js` 与断言里写明。

---

## 三、顺手修掉的译名显示瑕疵

实拍里抓到两处（都是「显示对了但不好看」）：

| 现象 | 根因 | 修法 |
|---|---|---|
| `Honor HONOR Magic8 Lite` | MobileModels 的 market 自带品牌词（`HONOR Magic8 Lite`），再拼 brand 就重了 | `tidyMarket()` 剥掉开头的品牌词 |
| `Xiaomi Redmi Note 11 Pro+ (\`pissarro\`)` | market 尾部带内部代号（还包着反引号） | 先按已知 codename 精确剥，再兜底「**小写字母开头**的短括号串」 |

★ **护栏**：`(2023)`、`(2a)`（`Nothing Phone (2a)`）这类**以数字开头**的括号是型号的一部分，**不剥**
（断言里各钉了一条，避免以后一刀切误伤）。

---

## 四、回归

| 套件 | 结果 |
|---|---|
| `tools/test-v1017.js`（**新增**，第 15 道防线） | **54 / 54** |
| `tools/preview-v1017.js`（**新增**，浏览器实拍） | **21 / 21** |
| `test-v1016` / `preview-v1016` | 126/126 · 46/46 |
| `test-v1015` / `preview-v1015` | 62/62 · 42/42 |
| 其余 11 套静态 + 3 套浏览器防线 | 全绿（`test-emulator-page` 118/118、`test-search-ui` 44/44、`test-filter-layout` 77/77） |

**实拍关键断言（终极漫画英雄vs卡普空3，`xd-2044`）**：
`机型清单 9 台` · `HONOR MTN-NX3 主行 = Honor Magic8 Lite` · `品牌词不重复` ·
`25053PC47G 主行 = Xiaomi POCO F7` · `无「上游共汇总」误导文案` · `more 行未出现` ·
`点一下仍能展开硬件面板 844px（含 Snapdragon 8s Gen 4 / Adreno 825）` · `无横向溢出` · `无 JS 报错` ·
`派生页同步 9 台`

### ⚠️ 本轮抓到的「假绿」（值得记住）

`preview-v1017.js` 第一版把断言函数签名写成 `chk(name, ok, extra)`，而调用按 `(条件, 名称)` 传 ——
`ok` 收到非空字符串**恒为真**，于是 **20 条断言全部假通过**（输出全是 `✅ true`）。
是「输出里没有一句人话」这个异常暴露了它。已把签名对齐为 `chk(条件, 名称, extra)` 并加了一句
「断言名称缺失 → 多半是参数顺序又写反了」的自检提示。
**教训：断言库的参数顺序必须有自检，假绿比红更危险。**

---

## 五、文件改动清单

| 文件 | 改动 |
|---|---|
| `data/deviceset.js` | **新增** —— `devKey` / `mergeDevices` / `mergeReport`（机型串归一与合并） |
| `data/devicemarket.js` | 新增并导出 `stripBrand()` / `tidyMarket()`；`resolve()` 用它产出 display |
| `data/bhparams.js` | 新增 `cachedDevices()`（同步、只读缓存、绝不打网络） |
| `server.js` | `/api/mobilehub/match` 返回合并后的 `devices` + `devicesSummaryCnt` / `devicesAdded` |
| `public/index.html` | 机型清单用全量清单算截断；`more` 行改为「还有 N 台未展开」 |
| `public/emulator.html` | 重建同步 |
| `tools/test-v1017.js` | **新增** —— 54 断言 |
| `tools/preview-v1017.js` | **新增** —— 21 断言实拍 |
| `tools/_cmp-online.js`、`_probe-devices.js`、`_probe-merge.js`、`_probe-fix.js`、`_restart.js` | 临时诊断脚本，用完**已清理** |

---

## 六、发布与线上验收（2026-09-18 补）

### 6.1 旧链接为什么会「还是老样子」

| | 事实 |
|---|---|
| 旧线上链接 | `https://36aa37e911e6447eb86eb187240daff2.app.workbuddy.host` |
| 最后一次成功发布 | **2026-09-16 03:38**（登记见 `~/.workbuddy/cloudstudio-deploy-history/9e6bd6a044ecf4c9_1789529936068.json`） |
| 之后发生了什么 | v10.15 / v10.16 / v10.17 三轮改动**一次都没发** |
| 重新发布的两次尝试 | ① `updateExistingApp` → 「本工作区没有可更新的应用」② 指定 appId `9e6bd6a044ecf4c9` → 工具不认（该 app 不在本会话的登记表里） |
| 直接指定旧域名段 | 「应用预留域名 `36aa37e911e6447eb86eb187240daff2-54822.app.workbuddy.host` **未绑定到本次发布环境**」 |

**结论**：旧 sandbox（`36aa37e9…`）已失效，域名无法重新绑定 ⇒ **只能新建 app，链接必然改变**。
这是「改完没发布」这一复发问题的第二次代价（第一次是用户看到 v10.14 界面以为代码没改）。

### 6.2 新链接

```
https://gamehub-agg-join.app.workbuddy.host/
```

- sandbox `207a9f3016a248f5b7e1182f3e53c06e`，`npm install` + `node server.js`，端口 8123
- 线上首页 185KB，`bhHwSlot` / `toggleDevHardware` / `data-hw` / `hasDetailUrl` / `d-hw .kvs2` **五特征齐**

### 6.3 新增 `tools/verify-online.js`（把「发没发」变成可执行断言）

`curl | grep 特征串` 只能证明**代码里有**，证不了**打开页面对**。本工具用真浏览器打开线上链接、
点一次详情页、量一次面板高度，产出 `_preview/live-*.png`。对同一款游戏（`xd-2044`）的验收结果：

| 断言 | 结果 |
|---|---|
| [接口] 机型库已上线 | ✅ 8,261 个内部编号 |
| [接口] 机型清单合并到 ≥ 9 台 | ✅ 9 台 |
| [接口] kalvo 从沙箱可达 | ✅ 81 行 / 12 组 |
| [页面] 首页含本版全部特征串 | ✅ 5 项齐 |
| [线上] 详情页机型 ≥ 9 台 | ✅ 9 台（宽 323px，无横溢） |
| [线上] `MTN NX3` → `Honor Magic8 Lite` | ✅ |
| [线上] `Xiaomi POCO F7`（用户举的那台） | ✅ |
| [线上] 「上游共汇总 N 款机型」误导文案 | ✅ 已消失 |
| [线上] **点一下真能展开硬件面板** | ✅ 844px 高 / 650px 宽，内容含 Snapdragon 8s Gen 4 + Adreno 825 |
| [线上] 无 JS 报错 | ✅ |

**线上验收：13 / 13 通过。**

## 七、遗留 / 未做

1. ~~**线上未重新发布**~~ —— **已发布（2026-09-18）**，见第六节；新链接
   `https://gamehub-agg-join.app.workbuddy.host/`。⚠️ **旧链接已失效**，对外一律用新链接。
2. **GitHub 未提交** —— v10.16 + v10.17 两轮改动都还没 `git commit`（v10.15 已提交 `6c1892e`）。
3. **可译率仍是 67.4%** —— 剩余多为**本来就是营销名**的机型（`motorola moto g24` 等，`resolved=false` 但显示正确），
   真正的硬缺口是 MobileModels 未收录的小众代工厂代号，**没有编造**。
4. **`bannerhub.json` 的 `dv` 上限是上游行为**，我们只能「并上逐条配置」补；
   若某游戏逐条配置也没预热（`bhparams` 缓存未命中），机型数仍会回到摘要的 6 台以内 ——
   这类游戏占比很小，但**未做统计**。
5. **门槛行与清单首行重复**（v10.15 设计）：`门槛 Motorola moto g24 … 304` 下面紧接着又是同一台。
   与 ⑤「评分重复」同类问题，**待你确认是否合并**（清单首行已有橙色高亮，摘要行可去掉）。
6. v10.14 就记下的两条：`samsung SM-S926U → Exynos 8895` 配对疑似有误（有 `≈` 兜底）；
   6 个平台爬虫（好游快爆、九游、游民星空、机核、游侠网、Steam）仍是 TapTap 之外待逐个优化的队列。
