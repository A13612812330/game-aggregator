# v10.33 —— 配置要求预热「可跑全量」+ 内存实测（回答用户提问）

> 基线 **v10.32** · 本轮改动：`data/pcreq.js`（缓存层，改完**必须重启服务**）·
> `server.js`（接口，改完**必须重启服务**）· `tools/build-steam-req.js`（预热脚本）·
> `.gitignore` · 新增 `tools/test-v1033.js` / `tools/_counterproof-v1033.js`
>
> 用户口径（原文）：
> 「可以，但是有个问题，**内存占用会不会很大**」
> 「JSON其实差不多够了（解包那个样本其实就差不多）」
> 「**先做主线**」
>
> ⇒ 本轮 = ①回答内存问题（实测，不估）②解包那条不再阻塞 ③**开工主线 P0：预热**。
> 而一开工就发现：**这脚本根本跑不了全量**，且跑起来会**前功尽弃**。本版修的就是这个。

---

## 一、内存会不会很大？—— 实测结论：**不会，13.3 MB**

用户问的是「把 18,131 条全预热进来，内存会不会爆」。这个必须量、不能估：
新增 `tools/_probe-mem.js`，拿现有 712 条真实缓存当样本合成了 18,131 条来量。

| 判据 | 实测 |
|---|---|
| 缓存现状（712 条） | 271,561 B = **265.2 KB**，单条均值 **370 B** |
| 合成 18,131 条落盘体积 | **6.9 MB**（7,058 KB），单条均值 **399 B** |
| **常驻内存增量（heapUsed，两次 gc 后）** | 下界 **0.8 MB**（值共享同一对象，真实数据不会这样）<br>**上界 13.3 MB**（每条独立 + 文本各异 ⇒ **真实值接近这个**） |
| 服务进程当前实测（WS） | **257.5 MB** |
| ⇒ 预热后的服务 | ≈ **278 ~ 285 MB**，**+8 ~ 10%** |
| 32 GB 机器 | **完全无压力** |

> ★ 为什么要给「上下界」而不是一个数：如果只测「值共享同一引用」的下界，
> V8 会把相同字符串内部化复用，量出来只有 0.8 MB —— **严重低估**。
> 真实数据每款游戏的 CPU / 备注文本本来就不同，V8 无法复用 ⇒ 真值接近上界。

### 但真正的成本**不在内存，在写盘**

`flush()` 是「**全量序列化 + 全量写盘**」，18,131 条规模下单次实测 **65 ms**
（`JSON.stringify` 47 ms + 写文件 11 ms）。预热 17,424 条时的三种口径：

| 落盘策略 | 次数 | 累计写入 | 纯写盘耗时 |
|---|---|---|---|
| 每条都 flush | 17,424 | **120 GB** | **1,133 秒**（不可接受） |
| 现有 5s debounce | ≈ 3,800 | ≈ **26 GB** | ≈ 248 秒（能跑，纯浪费） |
| **批模式（本版采用，每 500 条）** | **35** | **241 MB** | **2.3 秒** |

⇒ 新增 `pcreq.setAutoFlush(false)` + `flushNow()`，预热进程按批落盘；服务进程保持自动落盘。

---

## 二、四个「跑不了全量 / 会前功尽弃」的修复

### ① 只走封面一路 appid —— 整批漏掉 3,124 条**机地独有**游戏

原 `loadLib()` 是 `appid: pcreq.appidOf(it.cover)`。实测 19,010 条端游库：

| 来源 | 条数 |
|---|---|
| 仅封面解析得出 | 14,997 |
| 仅库内**顶层 `appid` 字段**得出 | **3,124** ← 原脚本整批跳过 |
| 两路都有 | 10（实测 **10/10 一致**） |
| **可预热总量** | **18,131**（原脚本只覆盖 15,007 = **82.8%**） |
| 两路都没有 | 879（只能走名称搜索） |

★ 被跳过那批恰恰是**机地独有**的游戏 —— 封面是 `img2.52jidi.com` 而非 Steam CDN：
**剑星 / 渔力全开 / 极限竞速：地平线 6 / 黎明行者之血**。它们本来就搜不到 Steam，
配置要求只能靠这条通路 ⇒ **漏掉这批 = 漏掉最缺数据的那批**。

### ② 服务端也读不到那 3,124 条（缓存里有、前端看不见）

`/api/pcreq` 原来只从 `cover` 解析 appid。实测「剑星」：

| | 修复前 | 修复后 |
|---|---|---|
| `appid` | **（空）** | **3489700** |
| 走的通路 | 名称搜索命中的另一个条目 | 库里顶层字段直查 |
| `rec`（推荐配置） | **无** | **有（7 字段）**，推荐 GPU = RTX 2060 SUPER / RX 5700 XT |

⇒ 也就是说：**预热把剑星抓回来了，前端却读不到**，拿到的还是更差的条目。已在 `server.js`
补「顶层字段兜底」，优先级仍以封面解析为先（既有行为不变）。三页（index/emulator/unpack）
的调用**都带 `id`**（实测 2/2 处），所以修一处即可三页受益。

### ③ 双重限流 —— 全量要跑 11 小时而不是 5.3 小时

`pcreq.throttled()` 内部已是**模块级串行链 + `MIN_GAP` 1100 ms**，
而旧脚本外层又固定 `await wait(1100)` ⇒ **每条实际 2.2 s**。

**实测证据**：`--limit=500` 跑 **2 m 46 s 连 100 条都没到**（日志里一行进度都没打出来）；
若真按 1.1 s/条，166 秒应该已到 150 条、进度早该输出。

同一个坑还有第二种形态：退避写 `gap = Math.min(gap * 2, 12000)`，
而 `--gap` 默认 0 ⇒ `0 * 2` 恒为 0 ⇒ **看起来在退避，实际一点没等**。

修法：`--gap` 默认改 **0**（只由 `throttled` 管节奏，`--gap` 语义改为「额外再等」）；
退避改成 `Math.min(Math.max(gap, THROTTLE_GAP) * 2, 12000)`。
另加**跨文件一致性断言**：`pcreq.MIN_GAP` 必须 == 脚本里的 `THROTTLE_GAP`（改一处忘另一处就漂移）。

### ④ ★ 跨进程互覆盖 —— 这条会让 5 小时白跑（本轮最危险）

写文档时才发现，属**灾难级**：
预热进程每 500 条落一次盘，而**服务进程落盘是全量覆盖**
（`writeFileSync` 写自己内存里的整份 map，只有 700 多条）。
⇒ 只要有一个用户请求触发一次 5 s debounce 写盘，**预热成果整批消失**，且**不报错**。

修法（两边都要，只修一边没用 —— 后写的总会赢）：

| 位置 | 机制 | 作用 |
|---|---|---|
| `doWrite()` | 落盘前 `mergeDisk()` | 服务端不再用自己那份覆盖磁盘 |
| `peek()` 未命中 | `refreshFromDisk()`（带 30 s TTL） | ★ 预热**边跑线上边生效**，跑完**不必重启** |

**实测验证**（不是推断）：用预热脚本单独写一条服务内存里没有的（针影裁梦 / appid 4019220），
再请求服务：

```
响应 7 ms        ⇒ 走的是缓存（不是实时抓 —— 实时抓要过 1.1s 限流）
cached 713 → 714 ⇒ 服务**没重启**就看到了预热新写的条目
```

TTL 是必须的：不带的话，每查一款库里没有的游戏都要同步读 6.9 MB 盘 + parse 47 ms。

### ⑤ 顺手：进度粒度与落盘解耦

初版把 `writeProgress` / 日志挂在「每 BATCH 条落盘」那一个分支里 ⇒ `batch=500` 时
**9 分钟才有一次进度更新**，5 小时的长跑等于看不见进度（实测：启动 5 分钟时进度文件
还停在上一轮的值）。现在日志/进度每 **50 条**刷一次，落盘仍按 `BATCH`（写盘放大不变）。

---

## 三、验证（全部实测）

| 层 | 结果 |
|---|---|
| 静态防线 | **33 套 / 2236 条 / 0 失败**（新增 `test-v1033.js` **53 条**，已登记 `SUITES`） |
| 前置闸 | **2 / 2** |
| 反证（静态） | `tools/_counterproof-v1033.js` **15 / 15 合规** |
| 五项汇报防线 | `test-report.js` **97 / 97** |
| 接口实测 | 剑星 `appid=3489700` + min/rec 各 7 字段 · 针影裁梦 **7 ms** 命中 |
| 预热实测 | 待抓 **17,329** 条 · 跑到 800 条时速率 **38 条/分** ⇒ 全量约 **7.2 小时** |

### ★ 反证当场抓出我 **4 处**断言缺陷（这就是它存在的意义）

1. **⑦ 断言只判「存在一处」** —— 每页其实有 **2 个** pcreq 调用点（两条分支），
   去掉其中一个的 `&id=` 之后套件**照样全绿**。
2. **⑦ 改成计数判据后又栽一次** —— 「数所有 `(id ? '&id=' …)` ≥ 数所有 pcreq 调用」，
   而页面里**别的接口**也用同样写法拼 id（实测 3 处 vs 调用 2 处）⇒ 少一处仍是 2 ≥ 2。
   正解：把「qs 定义 + 紧接着的 pcreq 调用」作为**一个整体**匹配，逐个调用点检查。
3. **⑧e 是假绿** —— 初版「先 seed 含 999 再 require 再 peek」，而 `ensure()` 是**惰性**的
   （require 不读盘、首次 peek 才读），所以它测的其实是「首次读盘」，
   把 `refreshFromDisk` 整个删掉也照样绿。改成「先 `peek('111')` 触发 ensure、**之后**再写盘」。
4. **⑧e 期望值写反** —— 我一度断言 `peek('999') === null`，而正确行为就是
   「未命中时自动刷新并命中」。把正确行为当期望值，是最隐蔽的一类错。

另有两处**反向断言被自己的注释判红**（③e / ④f）：文件头为了解释「旧实现错在哪」写了旧写法，
全篇扫描就把它当代码判了。修法：反向断言一律**先剥注释**再判
（`stripComments()`）—— 否则等于逼着以后不敢在注释里解释历史。

---

## 四、预热：进行中

```
node tools/build-steam-req.js --all --batch=500
```

* 待抓 **17,329** 条（18,131 已有缓存 802 条）· 节流 **1100 ms**
* ⚠️ **耗时实测修正**：按节流下限估是 5.3 小时，但跑到 800 条时实测
  **速率 38 条/分**（≈1.6 s/条）、**剩余 ≈435 分钟 ⇒ 全量约 7.2 小时**。
  慢的 45% 来自：`throttled` 的 `lastAt` 打在**请求发起前**，
  所以真实周期 = `MIN_GAP(1100ms)` **+ 请求本身的网络往返**。
  ⇒ 启动时那行「下限」只是下限，**以日志里的「速率 / 剩余」为准**（按实测速率算）。
* **断点续跑**：已有缓存直接跳过，中断后重跑接着抓（失败条目不写负缓存，重跑会补）
* **限流自适应**：连续 429/503 就翻倍拉大额外等待，连续 20 条恢复后回落
* **进度**：`_prewarm-progress.json`（每 50 条刷新）+ `_preview/prewarm-all.log`
* ★ 跑的过程中**线上就在生效**（见 §二④），跑完**不需要重启服务**
* 剩余 879 条无 appid 的走双语名称搜索，可用 `--mode=search` 单独补（每条 2~4 次请求，更慢）

---

## 五、文件清单

| 文件 | 改动 |
|---|---|
| `data/pcreq.js` | 原子写（`.tmp` + rename + 回退直写）· `setAutoFlush` / `flushNow` / `mergeDisk` / `refreshFromDisk` · `doWrite` 前 merge · `peek` 未命中时刷新 |
| `server.js` | `/api/pcreq` 补「库里顶层 `appid` 字段」兜底，`resolve()` 收到 `appid` |
| `tools/build-steam-req.js` | 两路 appid 合并 · 批模式 · 限流退避修正 · 进度解耦 · `--dry` / `--mode` / `--batch` |
| `tools/test-v1033.js` | **新增**静态套件（53 条，含 3 条真行为断言） |
| `tools/_counterproof-v1033.js` | **新增**反证（15 条变异，含 2 条专打「部分退化」） |
| `tools/_probe-mem.js` | **新增**探针（内存 / 落盘 / 写盘成本实测，`_*` 不入库） |
| `.gitignore` | 加 `/_prewarm-progress.json` |

> `tools/_*` 为临时探针，`.gitignore` 已覆盖（**不入库**）。
