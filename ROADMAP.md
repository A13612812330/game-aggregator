# GameHub 功能全景与路线图

> 基线 **v10.32**（2026-09-22）· 本文是**产品视角的唯一入口**：第一节「现在有什么」、
> 第二节「接下来做什么」。
> **流程**（怎么改、按什么顺序、哪里会炸）看 `WORKFLOW.md`；
> **踩坑**看 `PITFALLS.md`；**逐版细节**看 `CODEX-DONE-vX.Y.md`。
>
> ★ **本文所有数字都是接口 / 文件实测**（2026-09-22 13:5x，服务 8123），复现命令见文末附录。
> 不与任何旧文档对抄 —— 上一版规模表停在 v10.24，已经过期。

---

## 〇、一页速览

这个站在做一件具体的事：**把「PC 游戏」和「手机能不能玩」这两件事连起来**。

| 用户想问的问题 | 本站的答案来自 | 成熟度 |
|---|---|---|
| 这游戏现在火不火？ | 双源热榜（XD 官方热度 + 机地社区） | ✅ 稳定 |
| 这游戏要什么配置？ | PC 要求库 17,120 款 + Steam 官方接口 | ✅ **命中率已补齐（16,108 / 94.1% 官方原文，v10.37）** |
| 我手机 / 模拟器能不能跑？ | 机型实测库 1,037 条 + 机型库 1,055 台 + 芯片层级表 | ✅ 稳定 |
| 我手上这个解包能跑吗？ | 解包匹配（arch / dx / ram / storage 四维判定） | 🟡 字段校准待用户样本 |
| 有修改器 / 存档吗？ | 修改器 3,618 + Mod 8,943 + 云存档 6,604 | ✅ 稳定 |
| 去哪下载？ | 双源直链（XD 6/6 + 机地 5/5） | ✅ 稳定 |

**一句话结论**：数据层已经足够宽（19,010 款端游、15 个库），
**下一步的收益不在「再抓更多」，而在「把已有的用起来」** ——
最典型的就是配置要求：**正确率 95%，而预热覆盖率 v10.37 已从 3.9% 补齐到 94.1%**。

---

## 一、现在有什么

### 1.1 用户入口：三个页面 + 一个抽屉

| 页面 | 路径 | 在做什么 | 实测规模 |
|---|---|---|---|
| **首页** | `/` | 全站热榜（周 / 月 / 全站 / 社区四榜，双源合并）+ 最新收录 + 分类浏览 + **搜索弹窗**（`Ctrl K`，跨站实时） | 294KB（JS 169 / CSS 117） |
| **手机专区** | `/emulator.html` | 手游中心 / 模拟器指南 / 机型兼容查询 / 修改器 / 云存档（**7 个区块**） | 364KB（JS 223 / CSS 124） |
| **解包匹配** | `/unpack.html` | 上传解包 JSON → 判定可跑档位；下挂字典区 | 324KB（JS 188 / CSS 127） |
| **游戏详情** | 三页共用的抽屉 | **8 个分区**（见 1.2） | — |

★ 三页是**单一真源派生**：共享资产（CSS / 顶栏 / 遮罩 / 搜索 / 通用脚本）只有一处真源
（`index.html`），由 `tools/page-assets.js` 抽出，`build-*-page.js` 生成派生页。
⇒ **改共享资产只改主源，改完必须重建两页**（`test-pages-sync.js` 25 条守）。

### 1.2 详情抽屉的八个分区

| # | 分区 | 内容 | 数据来源 |
|---|---|---|---|
| 1 | **评分参数** | 双源评分 / 热度 / 标签 | XD + 机地 |
| 2 | **游戏介绍** | 版本介绍 + 简介（带来源标记） | 双源 |
| 3 | **游戏预览** | 截图墙 + 灯箱翻页 | 双源截图 |
| 4 | **配置要求** | 最低 / 推荐（含 DX 版本） | 机地 15,864 + Steam 官方 |
| 5 | **手机配置** | 社区配置 vs 实测库 | BannerHub + 本地实测 xlsx |
| 6 | **修改器 / 云存档** | 两块并排（v10.32 合并为条上**一项**） | GCM + Ludusavi |
| 7 | **同分类** | 同类卡 8 个 | 本地分类索引 |
| 8 | **下载** | 本体 / Mod / 修改器（真 tab） | 双源直链 |

★ 定位条按**屏幕位置**切分区（v10.32 才明确这条原则：两块并排 ⇒ 合并成一项）。

### 1.3 数据层：十五个库（实测规模）

| 库 | 规模 | 接口 |
|---|---|---|
| **端游库** | **19,010** = XD 15,388 + 机地 17,070 ｜双料 13,448 | `/api/library/stats` |
| **机地话题** | **17,220** | `/api/jiditopics/stats` |
| **PC 要求库** | **16,523** 款 | `/api/spec/dict` |
| **配置要求缓存** | **712**（命中 707 / miss 4 / `q:` 搜索键 1） | `/api/pcreq/stats` |
| **手游中心** | **3,203** ｜匹配端游 **1,634 = 51%** ｜配置 **15,644** ｜纯社区 2,181 | `/api/mobilehub/stats` |
| **BannerHub 社区库** | **2,665** 款 / **14,655** 配置 / **1,524** 机型 / 91 GPU | `/api/bh/stats` |
| **机型实测库** | **1,037** 条 / **1,025** 款 ｜可玩 **543** · 不可玩 **474** · 未标 20 | `/api/pc/stats` |
| **修改器** | **3,618**（匹配端游 **2,799 = 77.4%**） | `/api/trainers/stats` |
| **Mod** | **8,943**（匹配 **7,076 = 79.1%**）｜链接 13,390 | `/api/mods/stats` |
| **云存档** | **6,604** 款 / **15,027** 条路径 ｜手机能玩 **1,127** | `/api/saves/stats` |
| **机型库** | **1,055** 台 / **17** 品牌 | `/api/device/stats` |
| **机型市场库** | **11,500** 行 / **8,261** 代号 / **3,938** 市场 / 24 品牌 | `/api/device/market-stats` |
| **机型硬件库** | **34**（命中 28 / 缺 6） | `/api/device/hardware-stats` |
| **设备补齐** | **16**（14 条有芯片、全部来自在线） | `/api/device/fill-stats` |
| **模拟器指南** | 7 块：技术栈 / 芯片 / 兼容层 / 调优 / 避坑 / 跑分 / 版本 | `/api/emuguide` |
| **跨源对照（xref）** | 修改器 2,359 · 存档 6,263 · **双料 1,693** | `/api/xref/stats` |

### 1.4 数据来源：九个外部源

| 源 | 供给什么 | 实测出处（接口返回的 `source` 字段） | 风险 |
|---|---|---|---|
| **XDGAME** | 端游 15,388 款 + 热度榜 + 下载直链 | `xdgamer` | 部分游戏返回 200 + 「你没有权限下载」页 |
| **机地** `jidiyouxi.com` | 端游 17,070 款 + 17,220 话题 + 下载直链 | 签名接口 `get_topics` | 曾连续 6 日静默失效（v10.29 修） |
| **BannerHub** | 社区手机配置 14,655 份 | `the412banner.github.io/bannerhub-game-configs` | GitHub 源，较稳 |
| **MobileModels** | 机型市场库 11,500 行 | `khwang9883/MobileModels (MIT)` | GitHub 源 |
| **kalvo.com** | GPU / 机型对照 | — | ★ 必须 curl（node fetch 403） |
| **Steam** | 配置要求（appid 直查 / 双语搜索） | `steam-appdetails` | 直连可用；**唯一稳定通路** |
| **GCM** | 修改器清单 3,618 | Game Cheats Manager 公开数据 | 公开数据 |
| **Ludusavi** | 云存档 6,604 款 / 15,027 路径 | `Ludusavi manifest (MIT)` | 17MB manifest，需本地缓存 |
| **本地人工实测** | 机型实测库 1,037 条 | `基础测试数据.xlsx`（**只读**） | 人工维护，不自动增长 |

⚠️ **PCGamingWiki 已实测不可用**（Cargo API 被 Cloudflare 拦死，HTTP 403）——
它本是补非 Steam 游戏配置的最佳备选。⇒ **Steam 官方接口是当前最优通路**（v10.32 结论）。

### 1.5 工程底座

| 项 | 实测 |
|---|---|
| `server.js` | **1,659** 行 / **74** 条路由（**72** 条 `/api/*`） |
| 前端三页 | 1.24MB（`index` 380KB · `emulator` 468KB · `unpack` 416KB，**全部内联、零构建**） |
| 静态防线 | **35 套 / 2347 条**（`run-all.js`，含前置闸 2 个） |
| 实拍防线 | `preview-v*.js` **24** 个 + 回归 `test-search-ui` / `test-v1025-*` |
| 反证 | 两层：静态 19/19 · 实拍 3/3（**打坏必须变红**） |
| 工具脚本 | **155** 个（`tools/*.js`） |
| 每日自动化 | 「GameHub 内容库每日自动同步」**ACTIVE**，每天 **09:30**，10 步固定顺序 |
| 线上 | `https://gamehub-agg-v4.app.workbuddy.host/` · **v10.32** · 验收 29/29 |
| 推送 | `_push-via-api.js`（github.com 直连被阻断） |

### 1.6 覆盖率现状 —— 四个真实短板

按「影响用户感知」排序，**这是规划的依据**：

| # | 短板 | 实测 | 用户感知 |
|---|---|---|---|
| **1** | ~~**配置要求没预热**~~ | ✅ **v10.37 已收官**：命中 **16,200 / 17,329（93.5%）** ⇒ 缓存 `cached 16,312 / withReq 16,111`（1.04 MB → 5.92 MB）；未收录 211 · 失败 918 | 已解决；残留 918 条（5.3%）可 `--force` 增量补抓 |
| **2** | 匹配器假阳性未收 | 未匹配里 473 条假阳性 —— ⚠️ **该数字 09-21 重测已过期**，真实症状见 v10.36：三份归一化（都漏剥 `™ ® ©`）+ 代际零校验。✅ **v10.36 已收口**（手游中心硬冲突 3 → 1，收假阳性 8 条） | 「已匹配」这个数字不可信 |
| **3** | 手游中心匹配率 51% | 3,203 中匹配 **1,634**，未匹配 1,569 | 一半手游查不到对应的 PC 端游 |
| **4** | 详情页封面缺 | `libCover` 部分为空（**未量化**） | 卡片空白 |

★ 上一版规模表（v10.24）写「带 appid 15,007 条」——**这个口径偏窄**：
15,007 只是「**封面 URL 能解析出** appid」的（78.9%），另有 **3,134** 条只有顶层 `appid` 字段，
**真实可预热总量是 18,131（95.4%）**。⇒ 缺口与工时全部上修，详见 2.2。

---

## 二、接下来做什么

### 2.1 阶段总览

| 阶段 | 主题 | 优先级 | 阻塞 | 一句话 |
|---|---|---|---|---|
| **一** | 把「正确率」变成「命中率」 | **P0** | ✅ **已授权，v10.33 进行中** | 实测约 **7.2h**，换 95% 的即时可用 |
| **二** | 匹配可信度 + 数据质量 | **P0/P1** | ✅ **2-A/2-B/2-C 均已收口**（v10.34 / v10.35 / v10.36） | 让「已匹配」可信、解包字段对齐（✅ 2-A+2-C v10.36 · ✅ 2-B v10.34） |
| **三** | 体验收口 + 结构收敛 | P1/P2 | 无 | 三页一致、封面补齐、拆大文件 |
| **v11** | 配置匹配能力统一 | 结构性 | 阶段二之后 | 三套相似实现 → 一套 |

### 2.2 阶段一（P0）· 把「正确率」变成「命中率」

> **为什么排第一**：这是全项目**唯一一个「数据已经有了、只是没用」**的缺口。
> 实测带 appid 的条目里 **95.0% 能查到配置**（v10.32，n=40），
> 而缓存只覆盖 **3.9%** ⇒ 用户看到的差距全是**等待**，不是缺失。

| 动作 | 内容 | 判据 |
|---|---|---|
| **1-A 预热管道** | ✅ **已收官（v10.33 开工 → v10.37 完成）**：命中 **16,200 / 17,329（93.5%）** · 限流 144 次自动退避 · 后台 9h35m42s。**★ 派生索引必须重建才算落地** ⇒ v10.37 重建 `data/spec-req.json`（Steam 官方原文 **3.9% → 94.1%**、DX 标注 +2,223、六字段零退化） | 换数据后**必须重启服务**（v10.37 实测服务内存陈旧快照 `4423` vs 磁盘 `16312`） |
| **1-B 做成增量而非一次性** | ⏳ **未做**（挂载点已具备：v10.38 已把每日维护收进 `tools/daily-sync.js`，新增一步即可接上；`build-steam-req.js` 本身支持断点续跑） | 挂到每日 09:30 **之后**，只补当日新增 ⇒ 次日 `withReq` 自动跟上库增长，**不需要人工再跑** |
| **1-C 断点续跑** | ✅ **已做**：中断后从上次位置继续（缓存本身就是进度） | 重跑不重复请求已有 appid（实测二次启动「跳过 802 条」） |
| **1-D 限流保护** | ✅ **已做**：`throttled` 串行 + 1100ms；429/503 自动**翻倍退避**、连续 20 条恢复后回落 | 网络异常与「搜不到」**分开统计**；失败不写负缓存（重跑会补） |

**验收判据**（可复跑）：
```bash
node -e "const s=require('./data/pcreq.js').stats();console.log(s)"
# 目标：withReq / 18131 ≥ 90%，且 miss 比例 < 10%
curl 侧：/api/pcreq/stats 的 withReq 与库增长同步
```

⚠️ **风险与对策**：Steam 有速率限制 ⇒ 不并发裸奔；分夜跑；**先小批量（200 条）验证无封禁再全量**。

### 2.3 阶段二（P0/P1）· 匹配可信度 + 数据质量

| # | 项 | 现状 | 目标 | 阻塞 |
|---|---|---|---|---|
| **2-A** | **收朴素子串假阳性（#33-B）** | ✅ **v10.36 已收口**。开工前重测：**473 条已过期**；真实病根是 **3 份归一化（都漏剥 `™ ® ©`）+ 代际零校验**。收 8 条假阳性（`Crysis 2 - Maximum Edition` 原挂**孤岛危机重制版**、`The Sims 1 - Legacy Collection` 原挂 **The Sims™ 4 资料片**…），净效果「从错配变未匹配」。★ 只做**零和收窄**方向（查询有数字、命中标题一个都不含）—— 反方向会误杀 `Tomb Raider`→9终极版 / `SkullGirls`→2nd Encore 等 4 条。剩余 C 类 **36** 条 | ✅ 已达成 | 无 |
| **2-B** | **解包字段校准** | ✅ **已完成（v10.34 字段校准 + v10.35 口径收口）**：四处缺陷全部「先量再改」（`tools/_probe-spec.js`） | 四维判定（arch / dx / ram / storage）与实际对齐 | ✅ 无 —— **口径决策已落（v10.35）**：ARM 无转译层信息 ⇒ 保留「待确认」 |
| **2-C** | 手游中心匹配率 51% → ? | ✅ **v10.36 已开工并收口第一轮**。先分类成因（`_probe-match-audit.js` 三桶：**Z 残渣 / A 库中确无 / B 护栏该拦 / C 匹配器漏**，**只有 C 可修**）：1,565 未匹配 = 残渣 264 + 库中确无 251 + 护栏拦截 905 + **可修 C 145**。实测 **1,635 → 1,646**（51.0% → **51.3%**；剔残渣 55.6% → **55.9%**）· C 151 → **145** · 硬冲突 **3 → 1**。天花板 +4.9pp，下轮入口 = 探针 C 类清单 | ✅ 第一轮达成 | 无 |
| **2-D** | 配置要求**推荐配置**覆盖 | 命中 38 条里推荐配置 **29（76.3%）** | 明确「没有推荐配置」是源站就没有，还是没取到 | 依赖 1-A 的全量数据 |

★ 2-A 与 2-C 是**同一个匹配器的问题**，建议一起做（共用同一套判据改造）。

### 2.4 阶段三（P1/P2）· 体验收口 + 结构收敛

| # | 项 | 内容 | 备注 |
|---|---|---|---|
| **3-A** | 详情页封面补齐（P1-4） | **先做成因分类**（a 没匹配上 / b 库里没封面 / c 渲染问题） | 禁止为了「看起来有封面」硬挂 |
| **3-B** | 三页一致性收口 | 导航与空态统一（共享资产已同源） | 改共享资产只改 `index.html` |
| **3-C** | 拆 `emulator-sections.js`（**59KB** 单文件） | ★ 约束：**不能加 `require`/`export`** —— 它被生成器**当字符串读进来内联**，只能按顺序拼接。拆完 `emulator.html` md5 必须**逐字节不变**（纯重构） | 纯重构，可反证 |
| **3-D** | 补修改器 / 云存档 / 手游中心专属断言（P3-1） | 这三个分区覆盖较浅 | 防线短板 |
| **3-E** | 数据层清洗统一 | 同一字段多条链路 ⇒ 清洗函数必须共用 | 曾出现同一 GPU 两处写法不同 |

### 2.5 v11 主轴 · 配置匹配能力统一

> 项目里有**三套相似但独立**的实现，都在做「文本 → 结构化配置 → 判定」：

| 已有 | 输入 | 判定依据 |
|---|---|---|
| 机型兼容 | 机型名 / 内部编号 | GPU → SoC → CPU 核簇（层级表） |
| 手游中心 | 社区配置 vs 实测库 | 跨源名称匹配 |
| 解包匹配 | 解包 JSON | `arch` / `dx` / `ram` / `storage` |

三套共用的东西：**文本归一化 · 型号→芯片转译 · 跨源名称匹配 · 脏值清洗**。
这些已经沉淀成 skill（`device-model-to-chip-translation` / `cross-source-name-matching` /
`gpu-tier-cross-vendor`），但**代码层没收敛**。⇒ 统一后可少维护两套重复逻辑，
**且阶段二的匹配器改造正好是统一的契机**（先改一套、再抽公共层）。

### 2.6 长期债（如实列，不美化）

| 债 | 现状 | 影响 |
|---|---|---|
| **外部源依赖 9 个** | 源站改版会**静默失效** | 已发生：机地搜索连续 6 日不可用无人发现 ⇒ 需要**主动巡检**（不只靠实拍断言） |
| 线上是**覆盖式发布** | 链接不变、内容被覆盖；旧链接照样 200 | 已**三次**栽在「200 区分不出新旧」⇒ 判版本只能比 `index.html` md5 |
| 前端零构建 | `index.html` 380KB 单文件 / 内联 JS 169KB | 无类型检查、无模块化，靠防线而不是编译器兜底 |
| 抓取必须 curl（部分源） | nanoreview / kalvo 用 node `fetch` 一律 403（TLS/JA3） | 例外：Steam `store.steampowered.com` 用 fetch 正常 |
| 人工实测库不增长 | 机型实测库 1,037 条来自 xlsx，**只读** | 这部分覆盖率天花板由人工投入决定 |

### 2.7 需要你决策的三件事

| # | 事项 | 我的建议 | 代价 |
|---|---|---|---|
| **1** | ~~配置要求预热是否开工~~（阶段一） | ✅ **已完成（v10.33 → v10.37）** | 已跑完；★ **校正一条旧认识**：`spec-req.json` 换数据后**必须重启服务**（原记「跑完不必重启」只对 `steam-req.json` 成立） |
| **2** | ~~解包 JSON 样本~~（2-B） | ✅ **已开工并完成（v10.34）**：用户口径「JSON其实差不多够了（解包那个样本其实就差不多）」⇒ 实测识别率 **89.6% → 91.0%**、消除 **7,806 款假阴性**、机型可认 | ✅ **口径已决策（v10.35）**：ARM 缺转译层信息 ⇒ **降回「待确认」**（原先「不可跑」）；只有配置**显式声明**不可用才判不可跑 |
| **3** | **要不要加产品向功能** | 现有主线（数据 + 匹配）还没做完，**不建议**现在铺新功能 | — |

★ 关于第 3 条：此前讨论过的产品向候补（搜索历史 / 收藏 / 分享导出 / 移动端强化）**都还没有明确需求**，
需要你先说要哪个，我不会自行开工。

---

## 附：本页数字的复现命令

```bash
cd "E:/新建文件夹/WorkBuddy/2026-09-03-16-11-58/game-aggregator"

# 数据层规模（一次拉全部 stats）
node tools/_probe-inventory.js

# 预热缺口 / 附属库覆盖率
node tools/_probe-gap.js

# 真实口径（封面 URL + 顶层字段 + 缓存）
node -e "const p=require('./data/pcreq.js'),a=require('./data/games.json');
let c=0,f=0,e=0;for(const x of a){const u=p.appidOf(x.cover);if(u)c++;if(x.appid)f++;if(u||x.appid)e++;}
console.log('封面解析',c,'顶层字段',f,'任一',e);
console.log(p.stats())"

# 路由数 / 防线套件数 / 工具数
grep -oE "app\.(get|post)\(\s*'[^']+'" server.js | sed "s/.*('//" | sort -u | wc -l   # 74
sed -n '/const SUITES/,/^\];/p' tools/run-all.js | grep -cE "^\s*'"                    # 32
ls tools/*.js | wc -l                                                                  # 155

# 防线 / 汇报
node tools/run-all.js
node tools/report.js
```

> 探针 `tools/_probe-*.js` 是**临时**脚本（`.gitignore` 覆盖，不入库），只读、不改任何文件。
> 数字会随抓取过期 —— **判据是接口实测，不是这份文档**；对不上时先信接口。
