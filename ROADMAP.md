# GameHub 功能全景与路线图

> 基线 **v10.39**（2026-09-24）· 本文是**产品视角的唯一入口**：第一节「现在有什么」、
> 第二节「接下来做什么」。
> **流程**（怎么改、按什么顺序、哪里会炸）看 `WORKFLOW.md`；
> **踩坑**看 `PITFALLS.md`；**逐版细节**看 `CODEX-DONE-vX.Y.md`。
>
> ★ **本文所有数字都是接口 / 文件实测**（2026-09-24 16:0x，服务 8123），复现命令见文末附录。
> 不与任何旧文档对抄 —— 上一版规模表停在 **v10.32**，已经过期；其中两处结论本轮被实测**推翻**（见 1.6）。

---

## 〇、一页速览

这个站在做一件具体的事：**把「PC 游戏」和「手机能不能玩」这两件事连起来**。

| 用户想问的问题 | 本站的答案来自 | 成熟度 |
|---|---|---|
| 这游戏现在火不火？ | 双源热榜（XD 官方热度 + 机地社区） | ✅ 稳定 |
| 这游戏要什么配置？ | PC 要求库 **17,177** 款，Steam 官方原文覆盖 **94.1%** | ✅ **命中率已补齐（v10.37）** |
| 我手机 / 模拟器能不能跑？ | 机型实测库 **1,037** 条 + 机型库 **1,058** 台 + 芯片层级表 | ✅ 稳定 |
| 我手上这个解包能跑吗？ | 解包匹配（arch / dx / ram / storage 四维，**v10.39 起把结论依据摊开**） | ✅ 字段校准已收口 |
| 有修改器 / 存档吗？ | 修改器 **3,635** + Mod **8,943** + 云存档 **6,606** | ✅ 稳定 |
| 去哪下载？ | 双源直链（XD 6/6 + 机地 5/5） | ✅ 稳定 |

**一句话结论**：数据层已经足够宽（**19,074** 款端游、**15 个库**），
**下一步的收益不在「再抓更多」，而在「把已有的用起来」**。

★ 但 v10.39 给这句话加了一条**反向限制**：**「用起来」之前要先分清哪条已经到天花板**。
手游中心匹配率 51.2% 经实测判定为**数据源天花板**（瓶颈是查询名质量，不是匹配器），
继续调匹配器是**负收益** —— 详见 1.6 短板 3。

---

## 一、现在有什么

### 1.1 用户入口：三个页面 + 一个抽屉

| 页面 | 路径 | 在做什么 | 实测规模 |
|---|---|---|---|
| **首页** | `/` | 全站热榜（周 / 月 / 全站 / 社区四榜，双源合并）+ 最新收录 + 分类浏览 + **搜索弹窗**（`Ctrl K`，跨站实时） | **371** KB（JS 226 / CSS 136） |
| **手机专区** | `/emulator.html` | 手游中心 / 模拟器指南 / 机型兼容查询 / 修改器 / 云存档（**7 个区块**） | **457** KB（JS 290 / CSS 144） |
| **解包匹配** | `/unpack.html` | 上传解包 JSON → 判定可跑档位；**新增「未声明」提醒与依据来源** | **408** KB（JS 250 / CSS 147） |
| **游戏详情** | 三页共用的抽屉 | **8 个分区**（见 1.2） | — |

> 规模口径：**KB = 磁盘字节 / 1024**。★ 此前两版文档**混用过两种口径**（v10.32 用「字节 / 1000」、
> v10.38 用「字符数 / 1024」），同一个文件因此出现过三个不同的 KB 数 —— 本版统一为字节 / 1024。
> 三页合计 **1,237 KB ≈ 1.21 MB**。

★ 三页是**单一真源派生**：共享资产（CSS / 顶栏 / 遮罩 / 搜索 / 通用脚本）只有一处真源
（`index.html`），由 `tools/page-assets.js` 抽出，`build-*-page.js` 生成派生页。
⇒ **改共享资产只改主源，改完必须重建两页**（`test-pages-sync.js` 25 条守）。

### 1.2 详情抽屉的八个分区

| # | 分区 | 内容 | 数据来源 |
|---|---|---|---|
| 1 | **评分参数** | 双源评分 / 热度 / 标签 | XD + 机地 |
| 2 | **游戏介绍** | 版本介绍 + 简介（带来源标记） | 双源 |
| 3 | **游戏预览** | 截图墙 + 灯箱翻页 | 双源截图 |
| 4 | **配置要求** | 最低 / 推荐（含 DX 版本） | PC 要求库 **17,177**（Steam 官方 15,751 / 机地 983 / 双源 416 / 无 27） |
| 5 | **手机配置** | 社区配置 vs 实测库 | BannerHub **14,802** + 本地实测 **1,037** |
| 6 | **修改器 / 云存档** | 两块并排（v10.32 合并为条上**一项**） | GCM + Ludusavi |
| 7 | **同分类** | 同类卡 8 个 | 本地分类索引 |
| 8 | **下载** | 本体 / Mod / 修改器（真 tab） | 双源直链 |

★ 定位条按**屏幕位置**切分区（v10.32 才明确这条原则：两块并排 ⇒ 合并成一项）。

### 1.3 数据层：十五个库（实测规模）

| 库 | 规模 | 接口 |
|---|---|---|
| **端游库** | **19,074** = XDGAME 15,448 + 仅机地 3,626 ｜双料 **13,448** | `/api/library/stats` |
| **机地话题** | **17,220** | `/api/jiditopics/stats` |
| **PC 要求库** | **17,177**（最低 17,146 / 推荐 11,366 / 有 DX 10,567） | `/api/spec/dict` |
| **配置要求缓存** | cached **16,371** · withReq **16,170** · miss **200** | `/api/pcreq/stats` |
| **手游中心** | **3,226** ｜匹配端游 **1,652 = 51.2%** ｜配置 **15,791** ｜纯社区 2,204 | `/api/mobilehub/stats` |
| **BannerHub 社区库** | **2,682** 款 / **14,802** 配置 / **1,529** 机型 / 91 GPU | `/api/bh/stats` |
| **机型实测库** | **1,037** 条 / **1,025** 款 ｜可玩 **543** · 不可玩 **474** · 未标 20 | `/api/pc/stats` |
| **修改器** | **3,635**（匹配端游 **2,812 = 77.4%**） | `/api/trainers/stats` |
| **Mod** | **8,943**（匹配 **7,076 = 79.1%**）｜链接 13,390 | `/api/mods/stats` |
| **云存档** | **6,606** 款 / **15,031** 条路径 ｜手机能玩 **1,132** | `/api/saves/stats` |
| **机型库** | **1,058** 台 / **17** 品牌 | `/api/device/stats` |
| **机型市场库** | **11,500** 行 / **8,261** 代号 / **3,938** 市场 / 24 品牌 ｜**内部冲突 3,239** | `/api/device/market-stats` |
| **机型硬件库** | **34**（命中 28 / 缺 6） | `/api/device/hardware-stats` |
| **设备补齐** | **16**（14 条有芯片、全部来自在线） | `/api/device/fill-stats` |
| **跨源对照（xref）** | 修改器 2,365 · 存档 6,265 · **双料 1,695** | `/api/xref/stats` |

另有一个**静态指南**（非库）：模拟器指南 7 块 —— 技术栈 / 芯片 / 兼容层 / 调优 / 避坑 / 跑分 / 版本（`/api/emuguide`）。

### 1.4 数据来源：九个外部源

| 源 | 供给什么 | 实测出处（接口返回的 `source` 字段） | 风险 |
|---|---|---|---|
| **XDGAME** | 端游 15,448 款 + 热度榜 + 下载直链 | `xdgamer` | 部分游戏返回 200 + 「你没有权限下载」页 |
| **机地** `jidiyouxi.com` | 端游源 17,074 款 + 17,220 话题 + 下载直链 | 签名接口 `get_topics` | 曾连续 6 日静默失效（v10.29 修） |
| **BannerHub** | 社区手机配置 14,802 份 | `the412banner.github.io/bannerhub-game-configs` | GitHub 源，较稳 |
| **MobileModels** | 机型市场库 11,500 行 | `khwang9883/MobileModels (MIT)` | GitHub 源 |
| **kalvo.com** | GPU / 机型对照 | — | ★ 必须 curl（node fetch 403） |
| **Steam** | 配置要求（appid 直查 / 双语搜索） | `steam-appdetails` | 直连可用；**唯一稳定通路** |
| **GCM** | 修改器清单 3,635 | Game Cheats Manager 公开数据 | 公开数据 |
| **Ludusavi** | 云存档 6,606 款 / 15,031 路径 | `Ludusavi manifest (MIT)` | 17MB manifest，需本地缓存 |
| **本地人工实测** | 机型实测库 1,037 条 | `基础测试数据.xlsx`（**只读**） | 人工维护，不自动增长 |

⚠️ **PCGamingWiki 已实测不可用**（Cargo API 被 Cloudflare 拦死，HTTP 403）——
它本是补非 Steam 游戏配置的最佳备选。⇒ **Steam 官方接口是当前最优通路**（v10.32 结论，v10.37 已验证到 94.1%）。

### 1.5 工程底座

| 项 | 实测 |
|---|---|
| `server.js` | **1,672** 行 / **74** 条路由（**72** 条 `/api/*`） |
| 前端三页 | **1,237 KB ≈ 1.21MB**（`index` 371 · `emulator` 457 · `unpack` 408，**全部内联、零构建**；口径 = 字节 / 1024） |
| 静态防线 | **38 套 / 2703 条**（`run-all.js`，另含**前置闸 2 个**；整套实测约 2.5 分钟）。★ 其中 `test-doc-structure.js`（145 条）是**唯一读文档而非读代码**的套件 —— v10.39 为「整份文档被复制进行内」那次事故补的闸 |
| 第二层防线 | `preview-v*.js` **24** 个 + 回归 **9** 个（`test-search-ui` + 8 × `test-v1025-*`，需 CDP、**须分批跑**） |
| 工具脚本 | **192** 个（`tools/*.js`，含 **82** 个 `_*` 临时探针与反证脚本，后者 `.gitignore` 覆盖） |
| 项目目录 | **963 MB** |
| 每日自动化 | 「GameHub 内容库每日自动同步」**ACTIVE**，每天 **09:30**，**9 步**（v10.38 起收进一条命令 `node tools/daily-sync.js`；v10.39 加第 9 步「配置要求预热」） |
| 线上 | `https://gamehub-agg-v4.app.workbuddy.host/` · **实测停在 v10.32** |
| 推送 | `_push-via-api.js`（github.com 直连被阻断） |

★ **线上判版的正确口径（v10.38 教训 + 本轮实证）**：
`index.html` md5 线上 `9ae5854778` **与本地完全相同**，但这**证明不了**线上是最新版 ——
首页自 v10.32 起未再改动，字节自然一致。实测三处决定性证据说明它停在 v10.32：

| 判据 | 线上 | 本地（v10.39） |
|---|---|---|
| `unpack.html` md5 | `2db227c9d1` | `2b3dcc38dd`（不同） |
| `/api/spec/dict` 的 `dict` | **`v10.20`** | `v10.34` |
| 同接口 `archRule` 指纹 | **`undefined`**（该字段 v10.35 才有） | 有值 |
| `/api/pcreq/stats` | cached **713** / withReq **708** | cached **16,371** / withReq **16,170** |
| `/api/library/stats` | total **19,010** | total **19,074** |

⇒ **判「线上是不是最新」= 前端 md5 + 服务端口径指纹（+ 新版独有字段）多条一起过**，
任何**单条**判据都会假绿（v10.30 栽在「HTTP 200」、v10.38 栽在「只比前端 md5」）。

### 1.6 覆盖率现状 —— 三个真实短板（原四个，其中一个已被证伪）

按「影响用户感知」排序，**这是规划的依据**：

| # | 短板 | 实测 | 用户感知 |
|---|---|---|---|
| **1** | ~~**配置要求没预热**~~ | ✅ **v10.37 已收官**：命中 **16,200 / 17,329（93.5%）**，缓存 1.04 → 5.92 MB；v10.39 已接成**每日增量**（上限 500 条/日，积压 925 条次日续补） | 已解决 |
| **2** | ~~匹配器假阳性未收~~ | ✅ **v10.36 收口**（硬冲突 3 → 1）+ **v10.39 二轮实测判定「不该改」**（见下「天花板」） | 已解决 |
| **3** | **手游中心匹配率 51.2%** | **3,226** 中匹配 **1,652**，未匹配 1,574。★ **v10.39 判定：这是数据源天花板，不是算法问题** | 一半手游查不到对应的 PC 端游（**但其中多数查不到是对的**） |
| **4** | ~~详情页封面缺~~ | ❌ **证伪：它不是独立短板，是「匹配率」的镜像** | 卡片空白（**跟随匹配率，单独做没用**） |

**★ 短板 4 的证伪依据（实测，同源对照组）**：

| 库 | 有 `libId`（匹配命中） | 有 `libCover`（封面） | 差 |
|---|---|---|---|
| 手游中心 | **51.2%** | **51.2%** | **0** |
| 修改器 | **77.4%** | **77.4%** | **0** |
| 云存档 | 95.7% | 96.7% | +63 |
| Mod | 79.1% | 0.0% | — （Mod 数据结构里**没有**该字段） |

⇒ `libCover` 与 `libId` **同源**：封面是从「匹配到的库内条目」上取的。
**提高封面覆盖率 = 提高匹配率**，两者是同一个数。⇒ 把它单列为一条 P1 待办是**重复计数**，
旧版 ROADMAP 的「3-A 详情页封面补齐」据此**撤销**。

**★ 短板 3 的「天花板」结论（v10.39 实测，与旧版记忆相反）**：
旧版记「未匹配里 473 条假阳性」⇒ 隐含「还有 473 条可捞」。v10.39 抽 40 条 C 类逐条看：

- **约 40% 是垃圾查询名**：exe 残留 / 编码损坏，如 `AssassinsCreedIIGame`、`Ragnar k`、
  `LEGOMARVELAvengers DX11`、`Tradu o do`。**这些匹配上反而错**。
- **数字盲区仅 15 条**，逐条看**真错配只有 2~3 条**（`NBA2K16` → `NBA 2K27`、`WWE 2K16` → `WWE 2K26`）。
- 而**收紧护栏会误杀 12 条正确的**匹配 ⇒ **净收益为负**。

⇒ 结论：**51.2% 已接近 BannerHub 数据源的天花板**，瓶颈在**查询名质量**（源站数据本身脏），
不在匹配器。继续调匹配器是负收益；要再提就是**换/加数据源**，不是改算法。

★ **口径纠错（保留，仍具教益）**：v10.32 写「带 appid **15,007** 条」——那只是
「**封面 URL 能解析出** appid」的（78.9%），另有 3,134 条只有顶层 `appid` 字段
⇒ 真实可预热总量是 **18,131（95.4%）**。**同一个词在两种写法下差 5 倍 —— 数字对了不等于口径对了。**

---

## 二、接下来做什么

### 2.1 阶段总览

| 阶段 | 主题 | 优先级 | 阻塞 | 一句话 |
|---|---|---|---|---|
| **一** | 把「正确率」变成「命中率」 | **P0** | ✅ **已收官**（v10.33 → v10.37） | 命中 93.5%、官方原文 94.1%；v10.39 已接成每日增量 |
| **二** | 匹配可信度 + 数据质量 | **P0/P1** | ✅ **全部收口**（v10.34 / v10.35 / v10.36 + v10.39 二轮） | 让「已匹配」可信；手游中心**判定已达天花板** |
| **三** | 体验收口 + 结构收敛 | P1/P2 | 无 | 统一口径、拆大文件、补断言 |
| **v11** | 配置匹配能力统一 | 结构性 | 阶段二之后 | 三套相似实现 → 一套 |

### 2.2 阶段一（P0）· 把「正确率」变成「命中率」—— ✅ 全部完成

| 动作 | 内容 | 状态 |
|---|---|---|
| **1-A 预热管道** | ✅ **收官（v10.33 开工 → v10.37 完成）**：命中 **16,200 / 17,329（93.5%）** · 限流 144 次自动退避 · 后台 9h35m42s。★ **派生索引必须重建才算落地** ⇒ v10.37 重建 `data/spec-req.json`（Steam 官方原文 **3.9% → 94.1%**、DX 标注 +2,223、六字段零退化） | ✅ |
| **1-B 做成增量而非一次性** | ✅ **v10.39 完成**：接进 `tools/daily-sync.js` 第 9 步 —— ① `build-steam-req.js --limit=500` 补缓存 ② **再** `build-spec-req.js` 重建索引（**缺第 ② 步就是白抓**）。上限 500 条/日（实测 1.08s/条 ≈ 9 分钟），顶到上限时摘要报「仍有积压，次日续补」 | ✅ |
| **1-C 断点续跑** | ✅ 中断后从上次位置继续（缓存本身就是进度）；重跑不重复请求已有 appid | ✅ |
| **1-D 限流保护** | ✅ `throttled` 串行 + 1100ms；429/503 自动**翻倍退避**、连续 20 条恢复后回落；**网络异常与「搜不到」分开统计**；失败不写负缓存 | ✅ |

★ **换数据后必须重启服务**（v10.37 实测：服务内存陈旧快照 `cached 4423` vs 磁盘 `16312`）。
原记「跑完不必重启」**只对 `steam-req.json` 成立**，对 `spec-req.json` **不成立**。

**复现判据**：
```bash
node -e "const p=require('./data/pcreq.js');console.log(p.stats())"
# 目标：withReq / 库总量 ≥ 90%，且 miss 比例 < 10%
```

### 2.3 阶段二（P0/P1）· 匹配可信度 + 数据质量 —— ✅ 全部完成

| # | 项 | 现状 | 结论 |
|---|---|---|---|
| **2-A** | 收朴素子串假阳性 | ✅ **v10.36 收口**。开工前重测发现**旧数字（473 条）已过期**；真实病根是 **3 份归一化（都漏剥 `™ ® ©`）+ 代际零校验**。收 8 条假阳性（`Crysis 2 - Maximum Edition` 原挂**孤岛危机重制版**…） | ✅ 已达成 |
| **2-B** | 解包字段校准 | ✅ **v10.34 字段校准 + v10.35 口径收口**：四处缺陷全部「先量再改」，识别率 **89.6% → 91.0%**、消除 **7,806 款假阴性** | ✅ **口径已落**：ARM 无转译层信息 ⇒ 保留「待确认」；只有配置**显式声明**不可用才判不可跑 |
| **2-C** | 手游中心匹配率 | ✅ **v10.36 第一轮**（1,635 → 1,646）+ **v10.39 第二轮判定「不该改」** | ✅ **已达数据源天花板（51.2%）**，见 1.6 |
| **2-D** | 配置要求**推荐配置**覆盖 | 17,177 款里推荐配置 **11,366（66.2%）** | ⏳ **未查证**：源站本身就缺推荐配置，还是没取到 —— 需抽样比对（**下一件可做的事**） |

★ 2-A 与 2-C 是**同一个匹配器的问题**，已一并做完（共用同一套判据改造）。

### 2.4 阶段三（P1/P2）· 体验收口 + 结构收敛

| # | 项 | 内容 | 备注 |
|---|---|---|---|
| ~~**3-A**~~ | ~~详情页封面补齐~~ | ❌ **已撤销** —— 实测证明它是匹配率的镜像（1.6 短板 4），单列即重复计数 | 跟着 2-C 走；2-C 已到天花板 ⇒ **这条自动结案** |
| **3-B** | 三页一致性收口 | 导航与空态统一（共享资产已同源） | 改共享资产只改 `index.html` |
| **3-C** | 拆 `emulator-sections.js`（**59KB** 单文件） | ★ 约束：**不能加 `require`/`export`** —— 它被生成器**当字符串读进来内联**，只能按顺序拼接。拆完 `emulator.html` md5 必须**逐字节不变**（纯重构） | 纯重构，可反证 |
| **3-D** | 补修改器 / 云存档 / 手游中心专属断言 | 这三个分区覆盖较浅 | 防线短板 |
| **3-E** | 数据层清洗统一 | 同一字段多条链路 ⇒ 清洗函数必须共用 | 曾出现同一 GPU 两处写法不同 |
| **3-F** | ★ **解包匹配的「依据展示」延伸** | v10.39 已把「游戏侧未声明」摊开（紫色 chip + 「本款结论由 … 判定」）；可继续把**每一维的原始值**也铺开 | 小改，判据清晰 |

### 2.5 v11 主轴 · 配置匹配能力统一

> 项目里有**三套相似但独立**的实现，都在做「文本 → 结构化配置 → 判定」：

| 已有 | 输入 | 判定依据 |
|---|---|---|
| 机型兼容 | 机型名 / 内部编号 | GPU → SoC → CPU 核簇（层级表） |
| 手游中心 | 社区配置 vs 实测库 | 跨源名称匹配 |
| 解包匹配 | 解包 JSON | `arch` / `dx` / `ram` / `storage` |

三套共用的东西：**文本归一化 · 型号→芯片转译 · 跨源名称匹配 · 脏值清洗**。
这些已经沉淀成 skill（`device-model-to-chip-translation` / `cross-source-name-matching` /
`gpu-tier-cross-vendor`），但**代码层没收敛**。⇒ 统一后可少维护两套重复逻辑。

★ **v10.39 已局部先行**：归一化已收敛成唯一真源 `data/name-normalize.js`（v10.36），
「结论依据」的表达方式也统一成 `unstated` + `basis` 两字段。**v11 可以照这个模式往下抽。**

### 2.6 长期债（如实列，不美化）

| 债 | 现状 | 影响 |
|---|---|---|
| **外部源依赖 9 个** | 源站改版会**静默失效** | 已发生：机地搜索连续 6 日不可用无人发现 ⇒ 需要**主动巡检**（不只靠实拍断言） |
| 线上是**覆盖式发布** | 链接不变、内容被覆盖；旧链接照样 200 | 已**四次**栽在「判据单一」⇒ 判版本必须**多条判据一起过**（见 1.5） |
| **文档会被脚本事故污染** | v10.36 提交把整份 `WORKFLOW.md` 插进了一行长行中间（752 行里 336 行是副本），**三个版本无人发现** | ✅ **v10.39 已补闸**：`test-doc-structure.js`（145 条，登记在 38 套内）守「一级标题唯一 + 任意偏移无大段重复 + 标题路径唯一」，反证 **6/6**（含**真实事故回放**）⇒ 同类事故下次提交就会红 |
| 前端零构建 | `index.html` 371KB 单文件 / 内联 JS 226KB | 无类型检查、无模块化，靠防线而不是编译器兜底 |
| 抓取必须 curl（部分源） | nanoreview / kalvo 用 node `fetch` 一律 403（TLS/JA3） | 例外：Steam `store.steampowered.com` 用 fetch 正常 |
| 人工实测库不增长 | 机型实测库 1,037 条来自 xlsx，**只读** | 这部分覆盖率天花板由人工投入决定 |
| **工作区留了旧 app** | 同一 sandbox 每发一版多一条同名 app + 一个域名，**旧域名全返回 200** | 只能**手工清**；★ 绝不能 `unpublish`（取的是「同目录最新一次发布」⇒ 会**把正式入口一起下掉**） |

### 2.7 需要你决策的事

| # | 事项 | 我的建议 | 状态 |
|---|---|---|---|
| **1** | ~~配置要求预热是否开工~~ | ✅ **已完成（v10.33 → v10.37）**，并已接成每日增量（v10.39） | 结案 |
| **2** | ~~解包 JSON 样本~~ | ✅ **已完成（v10.34 / v10.35）**：识别率 89.6% → 91.0%，消除 7,806 款假阴性；ARM 口径已定 | 结案 |
| **3** | **是否发布 v10.39** | ✅ **已授权** —— 线上实测停在 **v10.32**（落后 7 个版本），且 `data/**` 与前端都已就绪 | 本轮执行 |
| **4** | **要不要加产品向功能** | 现有主线（数据 + 匹配 + 体验）还没做完，**不建议**现在铺新功能 | 待你定 |
| **5** | **2-D 推荐配置覆盖率（66.2%）是否要查证** | 建议做：抽样 30 条比对源站，判定「源站就缺」还是「我们没取」 —— 成本低、能关掉一个未知 | 待你定 |

★ 关于第 4 条：此前讨论过的产品向候补（搜索历史 / 收藏 / 分享导出 / 移动端强化）**都还没有明确需求**，
需要你先说要哪个，我不会自行开工。

---

## 附：本页数字的复现命令

```bash
cd "E:/新建文件夹/WorkBuddy/2026-09-03-16-11-58/game-aggregator"

# 数据层规模（一次拉全部 stats）
node tools/_probe-doc-numbers.js        # 本项目文档口径的全套数字（接口 + 文件）

# 线上真实版本（★ 不能只比 index.html md5）
node tools/_probe-live-fingerprint.js   # 三页 md5 + 服务端口径指纹 + 新版独有字段

# 文档结构闸（抓「整份被复制」这类事故）
node tools/_probe-workflow-dup.js       # 偏移比对 + 锚点断言；--write 才写回

# 路由数 / 防线套件数 / 工具数
node -e "const fs=require('fs');const s=fs.readFileSync('server.js','utf8');\
console.log('路由',[...s.matchAll(/app\.(get|post)\(\s*'([^']+)'/g)].length,\
'行数',s.split('\n').length)"
# 防线 / 汇报
node tools/run-all.js
node tools/report.js
```

> 探针 `tools/_probe-*.js` 是**临时**脚本（`.gitignore` 覆盖，不入库），只读、不改任何文件。
> 数字会随抓取过期 —— **判据是接口实测，不是这份文档**；对不上时先信接口。
