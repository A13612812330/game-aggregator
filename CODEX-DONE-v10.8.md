# CODEX-DONE v10.8 —— 最近更新时间排序 / 全站榜缺号 / 日期数据根治

> 用户原话：**「优化，其次首页的最近更新好像有点小问题（时间排序的问题）首页的全站榜少了2」**
>
> 两个问题都查到了**具体且可复现**的根因，另有第三项（日期数据治理）是顺着根因一起做的。
> 用户确认的取舍：排序 = **源站更新日期优先 + 收录时间打平**；榜单 = **过滤后重编号 1..N**。

---

## 一、问题 2：全站榜「少了2」

### 现象

浏览器实测（修复前）：冠军卡是**赛博朋克2077**，但普通卡序号是 **3,4,5,…,10** ——
**榜单从「3」开始，缺 1 和 2**。这就是用户说的"少了2"。

### 根因

源站「全站最热（year）」TOP10 的**第 1 名是 `XDGAME游戏运行库检测工具206.04.13`**（不是游戏）：

```
1  8547  XDGAME游戏运行库检测工具206.04.13    ← 非游戏
2  191   赛博朋克2077/Cyberpunk 2077
3  2713  极限竞速：地平线5顶级版
...
10 3967  钢铁雄心4/Hearts of Iron IV
```

`fetchers/xdrank.js` 旧写法：

```js
out[KEYS[idx]] = rows.filter((x) => !/(运行库|检测工具)/.test(x.name)).slice(0, 10);
```

过滤本身没问题，但**沿用了源站 `<em>` 序号**。于是过滤后剩 9 条、序号是 2..10；
前端 `paintRank()` 又把 `list[0]`（rank=2）当冠军卡展示（冠军卡不显示序号），
普通卡从 rank=3 开始 → 用户看到「从 3 开始」。

**附带影响**：前三名红标高亮（`.no.r1/.r2/.r3`）也整体错位一格。

### 修法

抽出纯函数 `cleanPlate(rows)`（便于离线回归），**过滤后重排为连续 1..N**，
原序号保留在 `srcRank` 便于对源站追溯；剔除条数汇总到 `out._dropped` → API `meta.dropped`
→ 前端 meta 显示「已滤除 1 条工具条目」（免得用户以为数据丢了）。

```js
function cleanPlate(rows) {
  const clean = rows.filter((x) => !/(运行库|检测工具)/.test(x.name || ''));
  return {
    dropped: rows.length - clean.length,
    rows: clean.slice(0, 10).map((x, i) => ({ ...x, rank: i + 1, srcRank: x.rank })),
  };
}
```

### 物理上限说明

源站该档只给 10 行，剔掉 1 条工具后**只能有 9 条**，无法凑满 10。已按用户选择只做重编号。

---

## 二、问题 3：最近更新时间排序乱

### 现象

浏览器实测（修复前）的分隔条序列：

```
昨天更新 2026-09-14 → 8/12 更新 2026/8/12 → 9/9 更新 2026/9/9 → 09-13 更新 2026-09-13 → 09-12 更新 2026-09-12
```

日期明显跳序，且**同一列里混着 `09-14` 与 `2026/8/12` 两种格式**。

### 四个叠加根因

| # | 根因 | 证据 |
|---|---|---|
| ① | **排序键用了 `updatedTs`（本站抓取时间）而非源站更新日期** | 昨天抓到的 8 月老游戏（`生存日志` 2026/8/12）排到了 9-13 前面 |
| ② | **两个源站日期格式不同** | XD 是 ISO（15210 条），机地是斜杠不补零（52 条） |
| ③ | **机地日期被入库正则贪婪吃位** | 源站真值 `2026/9/8` → 入库成 **`2026/9/89`**（多吞一个 9） |
| ④ | **斜杠/畸形日期让 `new Date()` 解析失败** | 兜底 `updatedTs` 变 **NaN** → 排序退化成 0 → 排到最后 |

### ③ 的证据（抓源站原文逐条核对，非推测）

```
入库值        源站 release_date 真值
2026/9/89  →  2026/9/8      吞吞堡垒
2026/9/49  →  2026/9/4      鬼武者 Way of the Sword
2026/9/39  →  2026/9/3      黎明行者之血
2016/6/69  →  2016/6/6      钢铁雄心IV
2025/3/48  →  2025/3/4      侠盗猎车手5
2024/3/88  →  2024/3/8      红色警戒2
```

来源正则（`fetchers/jidi.js:42` 与 `jidiHeadless.js:161` 两处同款）：

```js
/发行日期[:：]\s*(\d{4}\/\d{1,2}\/\d{1,2})/
                                  ^^^^^^^^^ 贪婪，把日期后紧跟的数字一并吞掉
```

### 修法（4 层，逐层收口）

**① 新增统一日期工具 `shared.normDate / dateTs`** —— 全站唯一出口。
解析两种分隔符 → 校验月/日范围 → **还原被吞的「日」（`89`→`8`）** → 输出 ISO。
`日 > 31` 时取首位数字（已用 8 条源站真值逐条核对吻合）。

**② 抓取层**：`jidi.js` / `jidiHeadless.js` 的正则放宽到 `\d{1,3}` 取完整可疑串，
再交给 `normDate` 做范围校验与还原（正则本身救不回来 —— `89` 后跟 `:` 时贪婪仍成立）。

**③ 数据层**：
- 新增 `data/date-norm.js`（`normalizeOne` / `normalizeDates`），与既有 `cover-url.js` 同一模式；
- `gamesDb.load()` 就地归一化（防每日增量再带进脏值），`upsert()` 入口再拦一道；
- `updatedTs` 兜底加 `Number.isFinite` 判断（**NaN 在 JSON 里会变 null，`|| 0` 救不回来**）。

**④ 排序层**：`gamesDb.browse()` 排序键改为
**源站更新日期优先 → 同日按收录时间打平**（用户选定）。
排序键用 `Object.defineProperty` 以 **non-enumerable** 挂载（`_sortTs`），
`persist()` 的 `JSON.stringify` **不会把它写进数据文件**（已验证）。

**⑤ 前端**：新增 `normDateKey()`，`normTime()` 改为统一输出 `MM-DD`
（旧版只认含 `-` 的写法，机地那批会**原样输出 `2026/8/12`**）；分组键也改用归一化值。

### 一个副作用（如实记录）

我用"发布日期"兜底了缺失的 `updatedTs`，于是**预发布游戏**（如 `扎德箭术` 2026-09-19）
会带着未来日期排在最前。按"日期优先"的语义这是**正确**的（它确实是目前最新的游戏信息），
但如果希望未来日期不置顶，需要单独定规则 —— **未擅自加，等你定**。

---

## 三、存量数据迁移结果

`tools/migrate-dates.js`（`--dry` 干跑 / `--fetch` 联网补齐）：

| 项目 | 数量 |
|---|---|
| 扫描总条数 | 15,302 |
| 发生改动 | 60 |
| ├ 还原被吞位的日 | 3 |
| ├ 补零 / 格式统一 | 57 |
| └ 补上 `updatedTs`（原为 null） | 43 |
| 联网补齐无日期条目 | **6 / 6 全部成功** |

补齐的 6 条（发布日期可交叉验证）：

```
生化危机4重置版      → 2023-03-24   （真实发售日，对得上）
光与影：33号远征队   → 2025-04-24   （真实发售日，对得上）
《致命躯壳 II》      → 2026-08-20
刺客信条：黑旗 记忆重置 → 2026-07-09
龙之剑:觉醒          → 2026-07-23
杀戮尖塔2            → 2026-03-06
```

迁移后校验：**非 ISO 日期 0 条 · 无日期 0 条 · 非法 updatedTs 0 条 · `_sortTs` 落盘 0 条**。

---

## 四、顺带体检（用户点选的第 4 项）

`tools/_probe-dataaudit.js` 全量扫描 15,302 条，结果：

| 发现 | 数量 | 判定 |
|---|---|---|
| 标题近似但 URL 不同 | 673 | **非 bug** —— 跨源同款（XD 与机地各一份）/ 站内不同复刻包 |
| `genres` 为空 | 43 | 可改进（机地入库条目没带类型，详情页有数据） |
| 机地封面无扩展名 | 23 | **探针误报** —— 实测 `Content-Type: image/jpeg`，是有效图片 |
| 无封面 | 7 | 真实缺失 |
| `score` 越界 | **1** | **真 bug，已修**（`xd-3160` 蜗牛克利德 score=54，见下） |
| `updatedTs` 比日期早 6 年 | 1 | **探针规则写反了** —— 实为"老游戏新收录"（钢铁雄心IV），正常 |
| `updatedTs` 在未来 | 1 | 预发布游戏（扎德箭术 2026-09-19），非 bug |

### 修掉的 `score` 越界

`xd-3160`「蜗牛克利德/Clid The Snail」的 `score = 54`。
`fetchers/indexer.js` 从列表页 `.rank` 元素取评分，**早期没有范围校验** ——
`.rank` 里偶尔不是评分而是名次/其它数字。

修法：`indexer.js` 加 `sn > 0 && sn <= 10` 范围校验（与 `xdgamer.detail` 的既有判定一致）
＋ `tools/fix-bad-score.js` 清理存量（1 条置 null）。`browse` 的排序侧原本就有 `<=10` 防护，
但**详情页会把它当评分展示**，这才是必须修的原因。

---

## 五、修复过程自身抓出的两个坑（都写进注释与测试了）

1. **漏一行 `require` → 库静默变空**。
   `gamesDb.load()` 的 `catch (e) { /* 首次运行无文件 */ }` 把 `ReferenceError` 也吞了，
   外部表现是「列表全空」却零报错。已改为：非 `ENOENT` 一律 `console.error`。

2. **并行编辑同一文件会互相覆盖**。
   本轮有 3 处改动（`shared.js` 的 `normDate` 定义、`gamesDb.js` 的 `require`、
   `index.html` 的 `normDateKey` 定义 + `paintRank` 文案）被后续编辑覆盖掉，
   表现为「函数被调用但未定义」→ 前端 JS 中断 → 列表不渲染。
   **已确认是流程问题（同文件多次编辑必须串行），不是代码问题。**

---

## 六、数值对照（修复前 → 修复后）

| 指标 | 前 | 后 |
|---|---|---|
| 全站榜名次 | 冠军卡(rank2) + 普通卡 **3..10** | 冠军卡(rank1) + 普通卡 **2..9**，连续 |
| 全站榜 meta | 无说明 | 「已滤除 1 条工具条目」 |
| 「最新更新」日期序列 | `9-14 → 8/12 → 9/9 → 9-13 → 9-12`（跳序） | `09-19 → 昨天09-14 → 09-13 → …`（严格倒序） |
| 日期分隔条重复 | 有（`2026-09-12` 出现两段） | **无** |
| 卡片日期标签格式 | `09-14` 与 `2026/8/12` 混排 | **统一 `MM-DD`** |
| 非 ISO 日期 | 52 条 | **0** |
| 无日期条目 | 6 条 | **0** |
| NaN/非法 `updatedTs` | 43 条 | **0** |
| `score` 越界 | 1 条 | **0** |
| 前 120 条排序逆序数 | 2 处 | **0** |

---

## 七、交付物

**新增**
- `data/date-norm.js` —— 日期归一化单一真源（`normalizeOne` / `normalizeDates`）
- `tools/migrate-dates.js` —— 一次性迁移（`--dry` / `--fetch`）
- `tools/fix-bad-score.js` —— 越界评分清理
- **`tools/test-date-norm.js`（第 8 道防线，47 条）**
- `tools/preview-v108.js` + `_preview/v108-rank-year.png`、`_preview/v108-recent-list.png`
- `tools/_probe-dataaudit.js`（体检探针，保留复用）

**修改**
- `shared.js` —— 新增 `normDate` / `dateTs`
- `data/gamesDb.js` —— load/upsert 归一化、`_sortTs` 排序键、browse 排序改日期优先、catch 不再静默
- `fetchers/jidi.js`、`fetchers/jidiHeadless.js` —— 日期正则去贪婪 + 归一化
- `fetchers/indexer.js` —— score 范围校验
- `fetchers/xdrank.js` —— 抽出 `cleanPlate()`，过滤后重编号
- `server.js` —— `/api/rank` 回传 `meta.dropped`
- `public/index.html` —— `normDateKey` / `normTime`、分组键、榜单 meta 文案
- `public/emulator.html` —— 生成器同步
- `data/games.json` —— 迁移写回（备份 `_bak/games.json.bak-20260915013338`、`…bak-score-…`）

**未改**：`tools/emulator-sections.js`、`data/xref.js` 等本轮无涉及的模块。

---

## 八、测试基线（八道防线，全绿）

| 防线 | 条数 | 说明 |
|---|---|---|
| test-emulator-structure | 128 / 128 | 结构体检（含派生页一致性） |
| test-emulator-page | 118 / 118 | jsdom 行为 |
| test-emuhub | 118 / 118 | 手游中心 |
| test-alias-guard | 7 / 7 | 别名护栏 |
| test-saves-match | 44 / 44 | 云存档跨源匹配 |
| **test-date-norm** | **47 / 47** | **本轮新增：日期归一化 / 排序 / 榜单重编号** |
| test-filter-layout | 77 / 77 | 浏览器布局回归 |
| test-search-ui | 44 / 44 | 搜索弹窗 + 详情抽屉 |

新防线刻意做了**双向覆盖**：11 条"该还原的"（源站真值对照）+ 10 条"该拒绝的"
（`2026/2/30`、`2026/13/1` 等非法值不能造出更错的显示）。
只测还原会退化成"什么都接受"，只测拒绝会退化成"什么都拒绝"。

---

## 九、待办 / 给你的建议

1. **预发布游戏的未来日期**是否置顶 —— 现在是置顶（按日期语义正确），要改需你定规则。
2. **43 条 `genres` 为空的机地条目** —— 详情页有类型数据，可加一步回填（未做，等你点头）。
3. **7 条无封面** —— 机地侧本身没有，源站也没有，属于正常缺失。
4. **673 条跨源同款** —— 若希望搜索/列表里"同一款只出一条"，需要做跨源去重合并
   （涉及 UI 决策，不是纯数据问题，未动）。
5. **`catch` 静默吞错**在本轮已修 `gamesDb`，但项目里可能还有同类写法
   （如 `data/*.js` 的 `try{}catch{}`），建议后续统一排查一遍。
