# v10.35 —— arch 口径收口：ARM 缺转译层信息降回「待确认」

> 承接 `ROADMAP.md` 阶段二 **2-B** 遗留的**唯一一处口径决策**。
> 用户 2026-09-22 决策：**降回「待确认」**。
>
> 本轮**只改一个实现文件**：`data/spec-match.js`（`cmpArch` 一处分支）。
> 前端一律不动 ⇒ `public/*.html` 字节未变。改前/改后都用同一条命令
> （`node tools/_probe-spec.js`）量，避免用形容词代替数字。

---

## 一、决策依据：这不是「体验问题」，是「无法区分」

v10.34 让 `cpu_abi` 能读出「这是 ARM」之后，**兼容层里没写 `box64` / `box86` / `FEX`** 的配置
一律判 arch **fail** ⇒ 整款「不可跑」（原先落「待确认」）。

问题在于：**「导出物里没写 box64」与「设备没有 box64」在导出物里无法区分。**
而判错的代价**不对称**：

| 判错方向 | 用户看到什么 | 影响面 |
|---|---|---|
| 误判 `fail` | 整库显示「不可跑」——一个**不可能的结论** | 实测 16,519 款 |
| 误判 `unknown` | 显示「待确认」——用户自己看一眼就能判断 | 14,792 款 |

且这与本文件**既有口径自相矛盾**：`cmpNum` 早就写着
「`req == null` → `skip`（**游戏没标这项 ⇒ 跳过，不降级**）」——
「游戏没标要求」与「配置没标转译层」属于**同一类「没信息」**，不该只对后者降级。

---

## 二、实测（`tools/_probe-spec.js`，全库逐款，**不看被 `limit` 截断的 items**）

同一台 ARM 设备画像，只差**一个键**（`box64` 有没有出现在导出里）：

| 画像 | `box64` | arch 判定 | 全库结论（16,519 款） |
|---|---|---|---|
| **G**（`cpu_abi:arm64-v8a` + `compatibility:{dxvk,turnip}`）改前 v10.33 | 无 | unknown 100% | 待确认 **14,552** / 信息不足 **240** / 不可跑 **1,727** |
| **G** 改后 v10.34 | 无 | **fail 100%** | **不可跑 16,519（100%）** ← 一个键翻掉整库 |
| **G** 本轮 v10.35 | 无 | **unknown 100%** | 待确认 **14,552** / 信息不足 **240** / 不可跑 **1,727**（**回到 v10.33**） |
| **H** 参考组（同设备 + `box64`） | 有 | ok 100% | 流畅 **10,081** / 可跑 **5,739** / 不可跑 **699** |

⇒ 改后与 v10.33 的分布**逐档完全一致**（14,552 / 240 / 1,727）——
这正是预期：本轮只把「缺信息」从 fail 挪回 unknown，**没有动任何其它口径**。

### 唯一保留 fail 的情形

配置**显式声明**转译层不可用 —— 那是有信息量的「明确不支持」：

| 配置 | v10.34 | v10.35 |
|---|---|---|
| `box64: 'off'` / `'disabled'` / `'false'` / `'no'` / `'none'` / `'0'` / `'无'` / `'禁用'` | ok（**漏判**） | **fail**（修掉了这个反向漏洞） |
| `box64: ''`（键在值空） | ok | ok ——**空值 ≠ 禁用**，导出物里「键在值空」是常态 |
| `box64: '0.3.4'` / `fex` / `box86` | ok | ok |

---

## 三、改了什么（`data/spec-match.js` · `cmpArch` 一处）

```js
const tr = Object.keys(profile.layer || {}).find((k) => X86_TRANSLATORS.some((w) => k.includes(w)));
if (tr) {
  const raw = String((profile.layer[tr] || {}).raw || '').trim();
  if (TR_OFF.test(raw)) {            /* ★ 新增：显式声明不可用 ⇒ fail（唯一保留 fail 的情形） */
    return { dim: 'arch', state: 'fail', note: '…配置**显式声明** ' + tr + ' 不可用…' };
  }
  return { dim: 'arch', state: 'ok', note: 'ARM 架构，但有 ' + tr + ' 转译 x86 指令' };
}
/* ★ 改：原 `state: 'fail'` ⇒ 现 `state: 'unknown'` */
return { dim: 'arch', state: 'unknown', note: 'ARM 架构，配置未提及 x86 转译层…——缺信息，不能据此判不可跑' };
```

`TR_OFF = /^(off|disabled|disable|false|no|none|null|nil|0|无|禁用|关闭|不支持|不可用)$/i`（大小写不敏感）。
决策理由与实测数字**写成注释留在函数上方**，避免下次有人又「顺手收紧」。

---

## 四、验证

| 项 | 结果 |
|---|---|
| 静态防线 | **35 套 / 2,344 条 / 0 失败**（前置闸 2/2；新增 `test-v1035.js` **41 条**，已登记 `SUITES`） |
| 既有套件 | `test-v1034.js` **63/63**、`test-spec.js` **86/86**（三条断言随口径更新，见下） |
| 反证 | `_counterproof-v1035.js` **9/9 合规**；`_counterproof-v1034.js` **12/12 合规**（变异已改成「把口径退回 fail」） |
| 探针 | 画像 G 回到 待确认 14,552 / 信息不足 240 / 不可跑 1,727（与 v10.33 逐档一致） |
| 接口实测（**重启服务后**） | `/api/spec/analyze` 画像 G：`dist = {maybe:14552, unknown:240, no:1727}`；`arch` 维度 `state=unknown`、note 为新文案；识别率 100% |
| 前端 | **未动**（`public/*.html` 字节未变 ⇒ 线上 md5 不变） |

### 随口径一并更新的三条旧断言（**不是**为了让测试变绿，是它们记录的旧口径已作废）

| 文件 | 原断言 | 现断言 |
|---|---|---|
| `test-spec.js` | `ARM 且无转译层 → arch 判 fail` / `arch fail 直接导致整款不可跑` | `…→ arch 判「待确认」` / `缺转译层信息 → 整款「待确认」，**不许**判不可跑`；**新增** `显式声明 box64 不可用 → arch 判 fail` |
| `test-spec.js` | `无 x86 转译层 → 全部判不可跑`（只看被 `limit` 截断的 `items`） | `全库不许出现「可跑 / 流畅」` + `可玩数 0`（改用 `stats.dist` / `stats.playable`，全库口径） |
| `test-v1034.js` | 同上前两条 | 同上前两条（并在断言名里写明「v10.35 口径」） |

> ⚠️ 写这两条时踩到一个坑：`stats.dist` **只含出现过的档位** ——
> 一条 `ok` 都没有时 `dist.ok` 读出来是 **`undefined` 而不是 `0`**
> ⇒ 直接写 `dist.ok === 0` 会**假红**。已改成 `(dist[k] || 0)` 归一。

### 反证这次学到并已写进技能的两条（`counterproof-assertions`）

1. **变异要整体回退旧写法**：单点替换常被**第二层守卫**兜住 ⇒ 打不红
   （v10.34：`cpuabi` 被更窄的 `abi` 兜住、`v` 被后面的 `if (kind0)` 兜住）。
2. **套件崩了 ≠ 断言没抓住**：`find(...).v` 抛 TypeError ⇒ **一行 FAIL 都没打**
   ⇒ 反证只看「有没有 FAIL 行」会误判成假绿。两个反证脚本都加了**汇总行判据**：
   没跑完 ⇒ 报 **`UNVERIFIED`**（既不算合规、也不算假红），必须修。

---

## 五、没做的事（**明确列出，避免误读**）

- **未发布**：本轮只改 `data/**`，`public/*.html` 字节未变 ⇒ 线上 md5 不变。
  LIVE 仍是 **v10.28**（v10.29 / v10.30 / v10.33 / v10.34 / v10.35 均未发布，**发布需逐轮授权**）。
- **未动 `arch` 之外的任何维度**：`dx` / `ram` / `storage` 的口径与判定一行未改，
  实测分布也证明没被牵连。
- **未加版本标记到接口**：`/api/spec/dict` 仍只报词典版本（`v10.34`，本轮没改词典）。
  若要让「服务端是不是最新」也能一眼判，需要加一个 rules 版本字段 —— 属于另一件事。
- **未开工**：2-A（收朴素子串假阳性 473 条）与 2-C（手游中心匹配率 51%）已在下一轮排期。

---

## 六、下一轮

| # | 事项 | 依据 |
|---|---|---|
| **2-A** | 收朴素子串假阳性（#33-B，473 条，如 `Pro Evolution Soccer 2013` 命中「万物皆可蟹：动物进化」的 `Evolution`） | 先重测量化再收 |
| **2-C** | 手游中心匹配率 51% → ?（1,569 未匹配）：先分类成因（真没有 / 名字不同 / 匹配器漏） | 依赖 2-A 的方法论，**共用同一套判据** |
