# CODEX-DONE v10.19 —— 「模拟器指南」模块化：三个根因 + 吸顶模块导航

> 用户原话：**「模拟器指南 这个页面太乱了，我需要你模块化显示」**
> 日期：2026-09-18 ｜ 承接 v10.18（机型清单三要素 / 未收录联网补全）

---

## 一、结论先行

用户说的「乱」**不是设计问题，是样式根本没接上**。三个根因叠加：

| # | 根因 | 性质 | 后果 |
|---|---|---|---|
| ① | `var(--c-line)` 在 `public/index.html` 里**从未定义** | CSS 变量缺失 | 12 处 `border:1px solid var(--c-line)` **整条声明失效** → 所有卡片边框消失 |
| ② | 渲染层类名与 CSS **完全脱节** | 实现与设计对不上 | ④优化 / ⑥版本 / ⑦帧率 退化成浏览器默认圆点列表 = 一坨文字 |
| ③ | 7 个模块平铺、无导航、无卡片边界 | 信息架构 | 页面被拉成一整条，用户找不到「我要看哪一节」 |

根因 ② 的具体脱节（渲染层造的类，CSS 里**一个都没有**）：

| 渲染层在用（CSS 里不存在） | CSS 里已写好（但没人用） |
|---|---|
| `.eg-n` / `.eg-tx` / `.eg-sub` / `.eg-key` | `.eg-ly .no` / `.bd` / `.nm` / `.sb` / `.ds` / `.flag` |
| `.eg-ch-h` / `.eg-tier` / `.eg-ch-b` / `.eg-ch-note` | `.eg-chip .hd` / `.gen` / `.tag` / `.soc` / `.drv` / `.nt` |
| `.eg-list`（**CSS 里完全不存在**） | `.eg-tbl`（三类列宽）/ `.eg-tune`（带 CSS counter）/ `.eg-av` |

⇒ 也就是说：**CSS 早就按「模块化」设计好了，只是渲染层一直在用另一套名字，全部落空。**

---

## 二、改法

### 1. 修根因①（1 处替换）
`public/index.html`：`var(--c-line)` → `var(--c-border)`（12 处，全在指南区）。
> 不新增 `--c-line` 同义变量 —— 同一语义只留一种写法。

### 2. 修根因②（重写渲染层）
`tools/emulator-sections.js` 的 `initEg()` 全部改用 CSS **已定义**的类名：
- 五层栈 → `.eg-ly[.key] > .no + .bd(.nm/.sb/.ds) + .flag`
- 芯片卡 → `.eg-chip > .hd(.gen/.tag) + .soc + .drv(.ln/.ln.alt) + .nt`
- ③⑥⑦ → `.eg-tbl.w2c` 两列表格（带表头，可扫读；新增 `.eg-tbl.w2c` 列宽 180px + 1fr）
- ④ → `<ol class="eg-tune">`（复用 CSS counter 自动编号）
- ⑤ → `.eg-avoid` 网格 + `.eg-av` 卡片

### 3. 修根因③（模块化）
- **吸顶模块导航** `<nav class="eg-nav" id="egNav">`：7 个 chip，点击跳转 + 滚动联动高亮
- **每个模块一张卡片**：`.eg-sec` 由「无边界平铺」升级为卡片（背景 + 1px 边框 + 14px 圆角 + 序号徽章 `.eg-no`）
- 机型兼容区也共用 `.eg-sec`，**一并对齐**（3 个 h3 换成序号徽章），整页视觉统一

### 4. 新增 CSS
`.eg-nav`（吸顶导航，桌面 `top: calc(var(--topbar-h) + 1px)`）· `.eg-sec` 卡片化 ·
`.eg-sec h3 .eg-no` 序号徽章 · `.eg-tbl.w2c` 两列表 ·
新增 `--topbar-h` 变量（`:root` 60px，`≤760px` 覆盖 54px）

---

## 三、真实数据对照（全部实拍实测，非估计）

| 指标 | 改前 | 改后 |
|---|---|---|
| `.eg-sec` 卡片边框 | **0px**（声明失效） | **1px** `rgb(230,233,240)` |
| 卡片圆角 | 0 | **14px** |
| 失效孤儿类 `.eg-list` | **23 个**（5+4+9+5 …） | **0 个** |
| ④ 优化清单 | 浏览器默认圆点列表 | `.eg-tune` + `::before` `counter(eg)` 自动编号 |
| ⑤ 避坑 | 裸 `<li>` 塞进 grid | `.eg-av` 卡片 × 5（红渐变） |
| ③⑥⑦ | 裸列表 | 两列表格，**4 / 6 / 8 行 + 1 行表头** |
| 顶部模块导航 | **无** | **7 个 chip**，吸顶（`top=61px`，顶栏 `bottom=61px`，不被遮挡） |
| 模块内条目 | — | egStack 5 · egChips 5 · egWrap 4 · egTune 9 · egAvoid 5 · egVer 6 · egBench 8 |

---

## 四、本轮踩到的坑（4 个，都是「看起来对、实际错」）

1. **`var(--c-line)` 从未定义** —— CSS 变量缺失不会报错，`border:1px solid var(--x)` 只是**静默失效**。
   排查方式：`grep -c 'var(--c-line)'` 有 12 处，而 `grep '--c-line\s*:'` **0 处** ⇒ 立刻定位。
2. **类名脱节**：数据全渲染出来了、页面却"空/乱" —— 与 v10.13 修过的「数据在、页面空」是**同一类 bug**。
   断言必须读 `getComputedStyle`，只断言「元素存在」会 100% 假绿。
3. **导航条被顶栏盖住**（实拍抓到）：`.eg-nav` 原来写 `top:0`，而 `.topbar` 是
   `position:sticky; top:0; z-index:60` —— 导航条 `z-index:7` **被完全遮住**。
   元素在、`position` 确实是 `sticky`、前面所有断言全绿，**但用户根本看不见它**。
   ⇒ 新增断言「导航条上沿不高于顶栏下沿」。**「存在且属性正确」≠「用户看得见」。**
4. **scroll spy 两个边界 bug**（连续两轮实拍才收敛）：
   - 用 `IntersectionObserver` + rootMargin 时，点击后高亮**不跟随**（回调只给变化元素，判据与直觉不符）→ 改用 rAF 节流 scroll spy，判据收敛成一句话：「最后一个 top 已越过导航条下沿的模块」。
   - 落点（`scroll-margin-top` = 122px）与判定线（nav 下沿 114 + 8 = 122）**恰好相等**，浮点误差让高亮抖回上一节 → 容差留到 **16px**。
   - 另外 `scrollIntoView({behavior:'smooth'})` 在 headless 下出现**静默失效**（高亮切了、页面 `scrollY` 0→0）→ 改为自算位置 `window.scrollTo`，并加滚动轨迹采样定位。

---

## 五、回归

| 层 | 结果 |
|---|---|
| 静态防线（`node tools/run-all.js`） | **15 套 972 / 0**，异常退出无 |
| 浏览器 `test-emulator-page.js` | **118 / 118** |
| 实拍 `preview-v1019.js`（本轮新增） | **66 / 66** |

`preview-v1019.js` 断言分 7 组：A 导航（7 chip/sticky/占版面）· B 卡片（**边框真的在**）·
C 类名对齐 + **样式落地**（读 computedStyle）· D 七模块条数 · E 导航交互（含**遮挡**与滚动轨迹）·
F 版面健康度 · G 窄屏 375。

### 顺带转正一个工具
`tools/run-all.js`（原为临时脚本）—— **一键跑完 15 套静态防线**。
原先手敲 `node tools/test-*.js` 容易漏跑，而漏跑的那套往往就是被改坏的那套。
⚠️ 新增静态套件时**必须**加进它的 `SUITES` 清单，否则永远不会被防线覆盖。

---

## 六、遗留（如实列）

- **#33** 联网补名提升匹配率 —— 端游库命中仍 **47.9%**，别名表只补到 143+344 条，需继续扩库。
- **#46** 三份文档仍停在旧版本：`HANDOFF.md`(v10.10)、`CODEX-HANDOFF.md`(v10)、`DESIGN.md`(v10.3)。
- **新功能（未开工）**：待用户提供「游戏解包 JSON」样本后，实现「提取配置 → 匹配可适配游戏」的搜索，
  形态对齐现有「机型兼容」（`/api/device/*`）。**样本未到，本轮不动。**
- 任务面板 34 条历史积压已核实并关单（详见下方「附」）。

### 附：任务面板收口（36 条 → 2 条）
逐条以源码为准核实：**34 条早已实现**（有文件 + 行号证据），本轮关单；
`#65–#72` 与 `#73–#82` 中 **5 对 10 条为标题逐字重复**（#73↔#78、#74↔#79、#75↔#80、#76↔#81、#77↔#82）；
真正未做的只有 **#33** 与 **#46**（见上）。
